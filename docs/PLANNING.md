# Tablero de Planificación (Kanban Backlog) - MVP CarePulse CRM/ERP

Este archivo centraliza el plan de ejecución y el backlog de actividades para la migración e implementación del **Sistema Clínico Odontológico (MVP)**.

---

## 📊 Estado General del Tablero

```text
+-----------------------+-----------------------+-----------------------+
|  📋 BACKLOG           |  🚧 EN PROGRESO       |  ✅ COMPLETADO        |
|  (1 Ticket)           |  (0 Tickets)          |  (51 Tickets)         |
+-----------------------+-----------------------+-----------------------+
```

---

## 📋 BACKLOG (Por Hacer)

#### `[TASK-046]` Odontograma no aísla el estado entre pacientes (`DOC-06`)
* **Descripción**: encontrado por el agente de TASK-045 en la primera corrida completa de la suite de Playwright — `DOC-06 - aislamiento del odontograma entre pacientes` falla: un diente queda pintado `bg-white` en vez de `bg-blue-500` después de cambiar de paciente en `/doctor/patient/[id]`, con un `ECONNRESET` del servidor justo antes en el log. No se investigó la causa raíz — puede ser un bug real de re-render/estado del componente `Odontogram` al cambiar de paciente, o una condición de carrera. Al estar dentro de un `test.describe.serial`, esta falla también impidió correr los 4 tests siguientes del mismo bloque (`DOC-09`, la aceptación de FLU-02, y dos de FLU-03).
* **Criterios de Aceptación**:
  - [ ] Investigar la causa raíz (bug real de `Odontogram.tsx`/`getOdontogram` vs. condición de carrera del test).
  - [ ] `DOC-06` (y los 4 tests que quedaron sin correr en el mismo bloque serial) pasan en una corrida completa de `pnpm test:e2e`.
* **Prioridad**: Media | **Esfuerzo**: sin estimar (depende de la causa raíz) | **Dependencias**: Ninguna

---

## 🚧 EN PROGRESO

*(No hay tareas en progreso actualmente)*

---

## ✅ COMPLETADO

### EPIC 1: Infraestructura & Migración a MongoDB

#### `[TASK-001]` Configuración de Conexión a MongoDB y ODM
* **Descripción**: Configurar la conexión persistente a MongoDB mediante Mongoose/Prisma en Next.js 14, reemplazando las variables de entorno y cliente de Appwrite.
* **Criterios de Aceptación**:
  - [x] Conexión a MongoDB URI mediante singleton pattern en `lib/db/mongodb.ts`.
  - [x] Manejo adecuado de variables de entorno (`MONGODB_URI`).
  - [x] Script de prueba de conexión ejecutable.
* **Prioridad**: Alta | **Esfuerzo**: Bajo (2 ptos) | **Dependencias**: Ninguna
* **Resultado**: Implementado el singleton de conexión en `lib/db/mongodb.ts` con el patrón de cache global de Next.js (sobrevive HMR/cold starts), validando `MONGODB_URI` en tiempo de ejecución con error claro si falta. Se agregó `scripts/check-db-connection.ts` (`npm run db:ping`) como verificación manual ejecutable. Ciclo TDD completo (RED→GREEN) contra MongoDB local real.
  - **Archivos creados**: `lib/db/mongodb.ts`, `lib/db/mongodb.test.ts`, `scripts/check-db-connection.ts`, `vitest.config.ts`, `vitest.setup.ts`, `.env.local`, `.env.example`
  - **Archivos modificados**: `package.json` (scripts `test`/`test:watch`/`db:ping`; dependencias `mongoose`, `vitest`, `dotenv`, `tsx`)
* **Observaciones**: El proyecto no tenía ningún test runner configurado; se evaluó Jest vs. Vitest y se optó por Vitest (cero-config con TS/ESM, sin tradeoffs arquitectónicos relevantes). Los tests son de integración real contra Mongo local, no mocks — consistente con el resto del proyecto.
  - **Gap detectado y corregido recién en TASK-005**: `vitest.setup.ts` conectaba los tests a la misma base que usa la app en desarrollo (`MONGODB_URI` de `.env.local`), sin aislar una base de test separada. Como varios tests hacen `deleteMany({})` en su limpieza, correr la suite borraba datos reales/sembrados (pasó con el usuario admin de TASK-004). Corregido en TASK-005 — ver esa entrada para el detalle.

#### `[TASK-002]` Definición de Esquemas Mongoose y Migración de Modelos
* **Descripción**: Crear las colecciones base de MongoDB (`User`, `Patient`, `Appointment`, `Doctor`, `Treatment`) traduciendo los tipos de `types/appwrite.types.ts` a esquemas de Mongoose con validación.
* **Criterios de Aceptación**:
  - [x] Esquemas creados en `lib/db/models/`.
  - [x] Validaciones de campos obligatorios y tipos coincidentes con Zod schemas.
  - [x] Índices creados para búsquedas eficientes (`userId`, `patientId`, `status`, `schedule`).
* **Prioridad**: Alta | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: TASK-001
* **Resultado**: Creados los 5 esquemas Mongoose con validaciones de campos obligatorios, enums (`Gender`, `Status`) y los índices pedidos por el ticket en `Appointment` (`userId`, `patientId`, `status`, `schedule`). 16 tests cubriendo validación, valores por defecto, enums e índices, corridos contra Mongo real.
  - **Archivos creados**: `lib/db/models/User.ts` (+`.test.ts`), `Patient.ts` (+`.test.ts`), `Doctor.ts` (+`.test.ts`), `Appointment.ts` (+`.test.ts`), `Treatment.ts` (+`.test.ts`), `lib/db/models/testHelpers.ts`
* **Observaciones**:
  - Se eligió **Mongoose sobre Prisma+conector Mongo** por mejor soporte de documentos embebidos/anidados (relevante para el futuro `odontograma_json` de TASK-009), a pesar de que otro proyecto del usuario (`az-store`) usa Prisma.
  - `userId` en `Patient`/`Appointment` se modeló como `ObjectId` con `ref: "User"` (relación real), no como string suelto como en Appwrite — deja el terreno preparado para TASK-004 (NextAuth suele usar una colección `User` equivalente).
  - Se detectó y corrigió un warning de deprecación de Mongoose (`validateSync`, a eliminarse en Mongoose 10) reemplazándolo por la API async `validate()` antes de dar la tarea por cerrada (requisito de salida "pristina" del proyecto).

#### `[TASK-003]` Refactorización de Server Actions a Patrón Repositorio
* **Descripción**: Reemplazar las llamadas directas de Appwrite en `lib/actions/patient.actions.ts` y `appointment.actions.ts` por repositorios desacoplados que interactúen con MongoDB.
* **Criterios de Aceptación**:
  - [x] Interfaz de repositorio `IPatientRepository` e `IAppointmentRepository`.
  - [x] Creación, lectura y actualización de pacientes y citas funcionando con MongoDB.
  - [x] Eliminación completa de la dependencia `node-appwrite`.
* **Prioridad**: Alta | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: TASK-002
* **Resultado**: Reemplazadas las Server Actions basadas en Appwrite por un patrón de puertos/adaptadores (`lib/repositories/` para las interfaces, `lib/db/repositories/` para las implementaciones Mongo). `node-appwrite` eliminado de `package.json` y `lib/appwrite.config.ts` borrado. 47/47 tests pasando, cero componentes de UI modificados (compatibilidad mantenida vía `lib/actions/serializers.ts`, que mapea los registros Mongo de vuelta al contrato `$id` que ya consumían los formularios).
  - **Archivos creados**: `lib/repositories/{IUserRepository,IPatientRepository,IAppointmentRepository,IFileStorage,INotificationService}.ts`, `lib/db/repositories/Mongo{User,Patient,Appointment}Repository.ts` (+tests), `lib/storage/GridFsFileStorage.ts` (+test), `app/api/files/[fileId]/route.ts` (+test), `lib/notifications/{TwilioNotificationService,buildAppointmentSmsMessage}.ts` (+test), `lib/actions/serializers.ts`
  - **Archivos modificados**: `lib/actions/patient.actions.ts`, `lib/actions/appointment.actions.ts`, `types/appwrite.types.ts`, `types/index.d.ts`, `package.json`
  - **Archivos eliminados**: `lib/appwrite.config.ts`
* **Observaciones**:
  - El ticket original no contemplaba qué reemplaza a **Appwrite Storage** (documento de identificación del paciente) ni **Appwrite Messaging** (SMS) — gaps reales detectados al ejecutar la tarea, no cubiertos por ningún otro ticket de este EPIC. Se resolvieron junto con el usuario: **GridFS** (nativo de Mongo, sin dependencias/credenciales nuevas) para archivos, **Twilio real** para SMS.
  - Se generó **TASK-013** (fuera del alcance de este ticket) al detectar, vía warning de `pnpm install`, una vulnerabilidad de seguridad conocida en Next.js 14.2.3.
  - Se detectó y corrigió un **drift de versión de `react-hook-form`/`@hookform/resolvers`** (causado por la pérdida de `package-lock.json` al migrar de npm a pnpm en paralelo a esta tarea), fijando versiones exactas a las que el template usaba originalmente.
  - Se cablearon las variables de entorno de Sentry (`NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`), que estaban hardcodeadas apuntando a la cuenta del autor original del template — trabajo adicional pedido explícitamente por el usuario durante esta misma sesión, no parte del criterio de aceptación original.
  - Durante la sesión se pegaron credenciales de Twilio en texto plano en el chat (dos veces) y por error una vez en `.env.example` (corregido antes de commitear). Se recomendó regenerar el Auth Token desde la consola de Twilio — **pendiente de confirmación por el usuario**.
  - **Bug post-completado detectado en pruebas manuales (durante TASK-004)**: `patient.actions.ts` y `appointment.actions.ts` nunca llamaban a `connectToDatabase()` antes de usar los repositorios, causando que `/admin` crasheara (`Cannot read properties of undefined (reading 'scheduledCount')`) y que el registro de paciente no hiciera nada — Mongoose bufferea las operaciones y las descarta en silencio a los 10s, capturado por el `try/catch` de cada acción que solo loguea y devuelve `undefined`. Los 69 tests de ese momento no lo detectaron porque cada test conecta la base en su propio `beforeAll`, enmascarando que el código de producción nunca lo hacía. Root cause confirmado reproduciendo el error real con dos tests que simulan un proceso recién arrancado (`lib/actions/patient.actions.test.ts`, `lib/actions/appointment.actions.test.ts`); corregido agregando `await connectToDatabase()` al inicio de las 9 funciones exportadas de ambos archivos, mismo patrón que ya usaba correctamente `GridFsFileStorage.ts`. Verificado en vivo contra el servidor real (`/admin` pasó de crashear a `200`).

### EPIC 2: Autenticación & Control de Acceso por Roles (RBAC)

#### `[TASK-004]` Autenticación Segura con NextAuth.js y MongoDB Adapter
* **Descripción**: Implementar autenticación real por Email/Password usando NextAuth.js (Auth.js) en reemplazo del PIN estático en `PasskeyModal.tsx`.
* **Criterios de Aceptación**:
  - [x] Endpoint `/api/auth/[...nextauth]` configurado con MongoDB Adapter.
  - [x] Hashing de contraseñas con `bcryptjs`.
  - [x] Formulario de Login funcional con redirección según rol.
* **Prioridad**: Alta | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: TASK-003
* **Resultado**: Implementado NextAuth v4 (Credentials Provider + JWT strategy) con `MongoDBAdapter` sobre un `MongoClient` nativo (`lib/db/mongoClientPromise.ts`), separado del singleton de Mongoose usado por el resto de la app. `User` extendido con `role` (enum `Administrador|Secretaria|Doctor|Paciente`, default `Paciente`) y `hashedPassword` (`select: false`, nunca se serializa por accidente). Login funcional en `/login` para Secretaría/Doctor/Administrador, con redirección real según rol. Seeder (`pnpm db:seed-admin`) para crear el admin de arranque leyendo credenciales desde variables de entorno. Verificado end-to-end contra el servidor real: flujo CSRF → credenciales → sesión con `role`/`id` correctos.
  - **Archivos creados**: `lib/db/mongoClientPromise.ts` (+test), `lib/auth/authenticateCredentials.ts` (+test), `lib/auth/authOptions.ts`, `lib/auth/roleHomeRoute.ts` (+test), `app/api/auth/[...nextauth]/route.ts`, `scripts/seed-admin.ts`, `components/forms/LoginForm.tsx`, `components/providers/AuthSessionProvider.tsx`, `app/login/page.tsx`, `types/next-auth.d.ts`
  - **Archivos modificados**: `lib/db/models/User.ts` (+test), `lib/repositories/IUserRepository.ts`, `lib/db/repositories/MongoUserRepository.ts` (+test), `lib/validation.ts` (`LoginFormValidation`), `components/CustomFormField.tsx` (soporte `inputType` para password), `app/layout.tsx` (envuelto en `SessionProvider`), `package.json` (`next-auth`, `@next-auth/mongodb-adapter`, `mongodb`, `bcryptjs`; script `db:seed-admin`), `.env.example`
* **Observaciones**:
  - Se descartó **Auth.js v5** (verificado: sigue en beta, `5.0.0-beta.32`, pese al tiempo transcurrido) en favor de **NextAuth v4 estable** (`4.24.15`) — no corresponde apostar por software beta en un proyecto de salud.
  - Se detectó y evitó a tiempo un mismatch de peer dependencies: `@next-auth/mongodb-adapter@1.1.3` sólo soporta `mongodb ^4||^5`, mientras la última versión del driver nativo es 7.x. Se pineó `mongodb@5.9.2` explícitamente (mismo tipo de trampa que el drift de `react-hook-form` en TASK-003, esta vez evitado verificando versiones antes de instalar en lugar de después).
  - Por decisión explícita del usuario, el **login de pacientes quedó fuera de alcance** — `RegisterForm.tsx` no se tocó. Idea planteada para el futuro (identificación por DNI en vez de password) sin ticket formal todavía.
  - Los destinos de redirección por rol para Secretaria (`/recepcion`) y Doctor (`/doctor`) están implementados pero esas páginas **todavía no existen** (llegan con TASK-006/TASK-008) — van a dar 404 hasta entonces. Comportamiento esperado y documentado, no se ocultó redirigiendo todo a `/admin`.
  - La verificación end-to-end del login se hizo con un script que lee `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` directo del `.env.local` del usuario, sin mostrarlas nunca en la conversación.
  - Se detectaron y terminaron procesos `pnpm dev` huérfanos en puertos 3000/3001, remanentes de sesiones anteriores.

#### `[TASK-005]` Middleware de Protección de Rutas y RBAC Dinámico
* **Descripción**: Restringir el acceso a páginas (`/admin`, `/doctor`, `/recepcion`, `/patients`) mediante middleware de Next.js según el rol asignado (`Admin`, `Secretaria`, `Doctor`, `Paciente`).
* **Criterios de Aceptación**:
  - [x] Roles guardados en el token JWT / Session. *(ya lo hacía TASK-004)*
  - [x] Middleware redirige a `/unauthorized` o `/login` si no cumple permisos.
  - [x] Ocultar elementos de UI inaccesibles según rol.
* **Prioridad**: Alta | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: TASK-004
* **Resultado**: `middleware.ts` protege `/admin` (Administrador), `/doctor` (Doctor) y `/recepcion` (Secretaria) vía `withAuth` de NextAuth v4: sin sesión → redirige a `/login`; sesión con rol incorrecto → redirige a `/unauthorized` (página nueva). Se eliminó por completo el gate inseguro anterior (`PasskeyModal.tsx`, el PIN en `localStorage`, y el chequeo duplicado en `DataTable.tsx`) — era exactamente la deuda técnica que `ARQUITECTURA.md` §4.3 marcaba como crítica, y dejarlo en paralelo al middleware real hubiera sido un doble gate confuso. El link "Admin" de la home ahora apunta a `/login`.
  - **Archivos creados**: `middleware.ts`, `lib/auth/getRequiredRoleForPath.ts` (+test), `app/unauthorized/page.tsx`
  - **Archivos modificados**: `components/table/DataTable.tsx`, `app/page.tsx`, `lib/utils.ts` (removidos `encryptKey`/`decryptKey`), `vitest.setup.ts`
  - **Archivos eliminados**: `components/PasskeyModal.tsx`
* **Observaciones**:
  - `/patients/*` (el flujo público de pacientes) **no se protegió** — es la consecuencia directa de la decisión de TASK-004 de no implementar login de pacientes; proteger esa ruta hubiera roto el registro/turnos públicos que sí funcionan hoy.
  - **Bug real encontrado durante la verificación, con una vuelta en falso antes de dar con la causa**: el login empezó a fallar (401) y el middleware redirigía todo a `/login` incluso con sesión válida. Primer intento (incorrecto): pensé que `withAuth` no encontraba el `NEXTAUTH_SECRET` dentro del middleware y se lo pasé explícito — no cambió nada (quedó igual, es una buena práctica de todos modos así que no se revirtió). Reinicié el servidor por si el middleware no había recompilado — tampoco. Recién aislando el problema (probando `authenticateCredentials` directo contra Mongo, sin pasar por HTTP) apareció la causa real: **el usuario admin sembrado ya no existía en la base**. Motivo: los tests corren contra la misma base que la app (`healthcare-dev`), y varios tests hacen `User.deleteMany({})` en su limpieza — correr `npm run test` durante la verificación de TASK-005 borró el admin. Fix real: `vitest.setup.ts` ahora redirige `MONGODB_URI` a una base hermana `-test` para que la suite nunca pueda tocar datos de desarrollo. Se corrigió también un test de TASK-001 que tenía el nombre de la base hardcodeado. Verificado de punta a punta: sesión válida → `/admin` 200; rol incorrecto → `/unauthorized`; sin sesión → `/login`.
  - Este bug (base de test compartida con desarrollo) estuvo latente desde TASK-001 — pudo haber afectado datos de cualquier sesión anterior, aunque no había nada valioso sembrado hasta el admin de TASK-004.

### EPIC 3: Gestión Dinámica de Doctores y Agendas

#### `[TASK-006]` CRUD de Doctores, Especialidades y Consultorios
* **Descripción**: Reemplazar el listado hardcodeado en `constants/index.ts` por un módulo administrativo de alta/baja/modificación de profesionales.
* **Criterios de Aceptación**:
  - [x] Formulario de Alta de Doctor (Nombre, Especialidad, Matrícula, Foto, Días/Horarios de atención).
  - [x] Persistencia en colección `Doctors`.
  - [x] API / Server Action para listar doctores activos en la interfaz de turnos.
  - [x] *(extendido a pedido del usuario)* CRUD completo (editar, desactivar/reactivar), restringido a rol Administrador.
* **Prioridad**: Media | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: TASK-003, TASK-005
* **Resultado**: Extendido el schema `Doctor` (TASK-002) con `specialty` (ahora requerido), `licenseNumber`, `availability` (array estructurado `{dayOfWeek, startTime, endTime}`, pensado para que TASK-007 lo consuma) e `isActive`. Repositorio + Server Actions (`createDoctor`, `getActiveDoctors`) reutilizando el GridFS de TASK-003 para la foto. Página `/admin/doctors` (protegida por el middleware de TASK-005) con listado + formulario de alta. Los 4 consumidores del `Doctors` hardcodeado (`RegisterForm.tsx`, `AppointmentForm.tsx`, `columns.tsx`, `success/page.tsx`) migrados a datos reales sin cambiar el contrato que ya usaban — `primaryPhysician` se sigue guardando como el **nombre** del doctor (string), no como referencia por ID, para no tener que tocar `Patient`/`Appointment` ni la lógica de matching existente. `constants.Doctors` eliminado. Verificado de punta a punta con un script que crea un doctor real (con foto) y confirma que aparece en el listado activo y que la foto se puede descargar — datos de prueba borrados después.

  **Extensión — CRUD completo**: a pedido explícito del usuario se agregó edición (`updateDoctor`, formulario `EditDoctorForm` con foto opcional) y desactivación/reactivación (`setDoctorActive`, **soft delete** — decisión del usuario tras explicarle que un borrado real dejaría los turnos históricos con imagen rota). Se agregó `lib/auth/requireAdminSession.ts`: las 3 acciones de mutación (`createDoctor`, `updateDoctor`, `setDoctorActive`) ahora verifican la sesión del lado del servidor con `getServerSession`, no solo confían en que el middleware proteja la página — una Server Action de Next.js tiene su propio endpoint HTTP y se puede invocar directo, sin pasar por el middleware de la página que la usa. Verificado: `createDoctor` sin sesión falla y no crea nada en la base (confirmado con script). Se separó la lista de doctores en dos usos distintos en `columns.tsx`: `allDoctors` para mostrar el doctor de turnos históricos (aunque esté desactivado, la foto/nombre se siguen viendo bien) y `activeDoctors` para los selects de reprogramar turno (no se puede asignar un doctor desactivado a un turno nuevo).
  - **Archivos creados**: `lib/repositories/IDoctorRepository.ts`, `lib/db/repositories/MongoDoctorRepository.ts` (+test), `lib/actions/doctor.actions.ts`, `lib/auth/requireAdminSession.ts`, `components/forms/DoctorForm.tsx`, `components/forms/EditDoctorForm.tsx`, `components/forms/DoctorAvailabilityPicker.tsx`, `components/DoctorRow.tsx`, `app/admin/doctors/page.tsx`
  - **Archivos modificados**: `lib/db/models/Doctor.ts` (+test), `lib/validation.ts` (`DoctorFormValidation`, `DoctorEditFormValidation`), `components/forms/RegisterForm.tsx`, `components/forms/AppointmentForm.tsx`, `components/AppointmentModal.tsx`, `components/table/columns.tsx` (`getColumns(allDoctors, activeDoctors)`), `app/admin/page.tsx`, `app/patients/[userId]/register/page.tsx`, `app/patients/[userId]/new-appointment/page.tsx`, `app/patients/[userId]/new-appointment/success/page.tsx`, `constants/index.ts`
* **Observaciones**:
  - El horario de atención se simplificó a un rango de hora único aplicado a los días seleccionados (no horarios distintos por día) — alcanza para lo que pide el ticket y para lo que va a necesitar TASK-007; horarios per-día distintos quedan como posible mejora futura.
  - La foto del doctor reutiliza el mismo bucket GridFS que los documentos de identificación de pacientes (`patientDocuments`) en vez de crear uno nuevo — es solo una etiqueta interna sin implicancia de seguridad distinta, y evita tocar la ruta `/api/files/[fileId]` que tiene el nombre de bucket fijo.
  - El path negativo del chequeo de admin (`requireAdminSession` sin sesión → falla y no crea nada) se verificó con un script real. El path positivo (admin autenticado → sí puede) se apoya en el mismo mecanismo (`getServerSession`) que ya está probado funcionando en TASK-004/005 para proteger `/admin`, pero no se re-verificó end-to-end con un login real en esta vuelta — recomendable probarlo a mano en el navegador.
  - **Bug reportado por el usuario al probar el login como admin, causa NO relacionada al código — corregido en dos vueltas**: `TypeError: getColumns is not a function` en `app/admin/page.tsx`. El código en disco siempre estuvo correcto (`getColumns` bien exportado, único import, confirmado con `git log`).
    - **Primer diagnóstico (incompleto)**: el servidor `pnpm dev` llevaba corriendo desde el 21/7 a las 22:51, casi 3 días antes del commit que introdujo `getColumns` (24/7) — se asumió que un simple reinicio del proceso alcanzaba, y la verificación post-reinicio pasó.
    - **El error volvió** después de que el usuario reiniciara el servidor por su cuenta. Causa real, más profunda: el **caché de build en disco** (`.next/cache/webpack/`) tenía entradas mezcladas del 21/7 y el 24/7, y venía fallando en restaurarse silenciosamente desde el principio de la sesión (warning `[webpack.cache.PackFileCacheStrategy] Restoring pack ... failed: Cannot read properties of undefined (reading 'hasStartTime')`, visible en los logs desde TASK-001 pero descartado entonces por no bloquear nada). Ese caché corrupto persiste en disco entre reinicios del proceso — por eso un simple restart no alcanzaba de forma confiable.
    - **Fix real**: borrar `.next/` por completo (no solo reiniciar el proceso) y arrancar con build limpio. Verificado con 3 requests consecutivos a `/admin` sin error, y confirmado que el warning de caché ya no aparece en el log.
    - **El error volvió por tercera vez** tras borrar `.next/` y reiniciar limpio — a esa altura, con dos remedios de infraestructura agotados (reinicio de proceso, caché de disco borrado), correspondía dejar de asumir que era un problema de entorno y revisar el código de nuevo. Causa real encontrada: `app/admin/page.tsx` es un **Server Component** que llamaba directamente a `getColumns(...)`, una función exportada de `components/table/columns.tsx`, marcado `"use client"`. Invocar una función plana (no renderizar un componente JSX) importada de un módulo cliente, desde código que corre en el servidor, cruza el límite RSC de una forma que Next.js no soporta de manera confiable — puede andar en algunas compilaciones y romperse en otras según cómo el bundler resuelva el módulo, lo cual explica por qué mis verificaciones aisladas (fetch directo al servidor) pasaban mientras que el flujo real del usuario (login → `router.push` del lado del cliente) fallaba.
    - **Fix de código**: nuevo componente cliente `components/table/AppointmentsTable.tsx` que recibe `data`/`allDoctors`/`activeDoctors` (props planas y serializables) y arma `getColumns(...)` **adentro**, en código que ya corre en el cliente. `app/admin/page.tsx` ahora solo pasa datos, nunca llama a una función de un módulo `"use client"`. Verificado con `.next/` borrado y arranque en frío, simulando tanto un request SSR normal como un request RSC con los headers que usa `router.push()` (`RSC: 1`, `Next-Router-Prefetch`) — los 4 casos devolvieron `200` sin error.
    - **Lección operativa**: cuando un error "imposible" sobrevive a dos rondas de remedios de infraestructura (reinicio de proceso, limpieza de caché), dejar de insistir por ese lado y volver a mirar el código — en este caso había un problema real de arquitectura (límite RSC cruzado incorrectamente) que la infraestructura nunca iba a arreglar.
    - **Confirmado por el usuario en el navegador**: login como admin y `/admin` funcionando de punta a punta tras el fix.

#### `[TASK-007]` Selector Dinámico de Horarios Disponibles
* **Descripción**: Actualizar el formulario de citas (`AppointmentForm.tsx`) para que calcule los slots de tiempo libres según la agenda del doctor seleccionado.
* **Criterios de Aceptación**:
  - [x] El paciente/secretaria elige doctor y fecha; el sistema deshabilita horas ocupadas.
  - [x] Validación anti-solapamiento de turnos en backend.
* **Prioridad**: Media | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: TASK-006
* **Resultado**: Función pura `lib/scheduling/getAvailableSlots.ts` (TDD) calcula slots libres de 30 minutos combinando la `availability` del doctor (TASK-006) con los horarios ya ocupados ese día. Server Action `getAvailableSlotsForDoctor` la conecta a datos reales (`MongoDoctorRepository.findByName` + nuevo `MongoAppointmentRepository.findBookedTimes`). `AppointmentForm.tsx` llama a esa acción cuando cambian doctor/fecha y usa `includeTimes` de `react-datepicker` (extendí `CustomFormField` para soportarlo, junto con `filterDate` para deshabilitar días enteros sin disponibilidad). Validación anti-solapamiento real en el backend (`existsOverlapping`, nuevo método del repositorio) corriendo antes de `createAppointment` y `updateAppointment` (caso "schedule") — si el horario ya está ocupado, la creación/reprogramación se rechaza; el formulario ahora muestra un mensaje de error en ese caso en vez de fallar en silencio.
  - **Archivos creados**: `lib/scheduling/getAvailableSlots.ts` (+test)
  - **Archivos modificados**: `lib/repositories/IAppointmentRepository.ts`, `lib/db/repositories/MongoAppointmentRepository.ts` (+test), `lib/repositories/IDoctorRepository.ts`, `lib/db/repositories/MongoDoctorRepository.ts` (+test), `lib/actions/appointment.actions.ts`, `components/forms/AppointmentForm.tsx`, `components/AppointmentModal.tsx`, `components/CustomFormField.tsx`
* **Observaciones**:
  - **Bug real encontrado durante la verificación manual, no durante los tests**: la función pura usa `date.getDay()` (hora local) para el día de la semana, pero `findBookedTimes` calculaba los límites del día y extraía las horas en **UTC** (`getUTCDate`/`getUTCHours`). En un servidor que no corre en UTC+0 (este, en particular), esto desalinea todo — un script de verificación manual mostró `[]` slots disponibles incluso sin ningún turno reservado. Los tests automatizados no lo agarraron porque construían las fechas de prueba con timestamps UTC explícitos (`...Z`) consistentes entre sí, ocultando la inconsistencia real. Corregido: `findBookedTimes` ahora usa hora local en todos lados, igual que `getAvailableSlots` y que el resto del sistema (el `<input type="time">` del selector de disponibilidad de TASK-006 tampoco tiene noción de timezone). Se reescribieron los tests afectados con fechas construidas en local (`new Date(2026, 7, 1, 10, 0)`) en vez de strings ISO UTC, y se verificó el flujo completo (slots antes/después de reservar, rechazo de duplicado) con un script real.
  - Este es el mismo tipo de riesgo que ya se anotó como pendiente en TASK-003/TASK-004 (manejo de timezone flojo en todo el proyecto, más allá del `timeZone` explícito que ya viaja para el armado del SMS) — se resolvió puntualmente acá, pero sigue siendo una limitación de arquitectura para una futura clínica que opere en más de un huso horario.
  - La duración de turno se asume fija en 30 minutos (coincide con el `timeIntervals` por defecto de `react-datepicker`, que no se tocó) — no hay un campo de duración configurable por tratamiento; se podría necesitar más adelante si se agregan prestaciones de duración variable (EPIC 5).

### EPIC 4: Ficha Clínica y Odontograma Interactivo

#### `[TASK-008]` Vista de Perfil Clínico y Historial de Evoluciones
* **Descripción**: Crear la vista exclusiva para el rol `Doctor` donde consulta el antecedente médico del paciente y registra notas de evolución por consulta.
* **Criterios de Aceptación**:
  - [x] Vista `/doctor/patient/[id]` protegida para profesionales.
  - [x] Formulario para agregar notas de evolución ligadas a la cita.
  - [x] Histórico cronológico de atenciones anteriores.
  - [x] *(extendido, ver Observaciones)* Vínculo entre el login del Doctor y su perfil clínico, alta de acceso de doctores, y página `/doctor` con la agenda propia.
* **Prioridad**: Alta | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: TASK-005
* **Resultado**: `/doctor/patient/[id]` muestra los antecedentes médicos del paciente (alergias, medicación actual, antecedentes familiares/personales, ya existentes desde TASK-002), un formulario para cargar una nota de evolución ligada a la cita (`ClinicalNote`, colección nueva) y el histórico cronológico de evoluciones previas. `/doctor` (nueva) lista la agenda propia del doctor logueado con link directo a la ficha de cada paciente.
  - **Gap resuelto (no cubierto por ningún ticket anterior)**: no existía ningún vínculo entre el login de un Doctor (`User`, rol=Doctor, TASK-004) y su perfil clínico (`Doctor`, TASK-006) — sin eso, un doctor logueado no tenía forma de saber "cuáles son mis turnos". Se agregó `doctorId` (ref `Doctor`) a `User`, se propagó a la sesión (JWT + `next-auth.d.ts`), y se agregó un flujo de alta ("Crear acceso") en `/admin/doctors` para que el Administrador cree el login de un doctor ya cargado, vinculándolo a su perfil clínico existente.
  - **Archivos creados**: `lib/db/models/ClinicalNote.ts` (+test), `lib/repositories/IClinicalNoteRepository.ts`, `lib/db/repositories/MongoClinicalNoteRepository.ts` (+test), `lib/actions/clinicalNote.actions.ts`, `lib/auth/requireDoctorSession.ts`, `components/forms/ClinicalNoteForm.tsx`, `components/forms/CreateDoctorAccessForm.tsx`, `app/doctor/page.tsx`, `app/doctor/patient/[id]/page.tsx`
  - **Archivos modificados**: `lib/db/models/User.ts` (+test, `doctorId`), `lib/repositories/IUserRepository.ts`, `lib/db/repositories/MongoUserRepository.ts` (+test), `lib/auth/authenticateCredentials.ts` (+test), `lib/auth/authOptions.ts`, `types/next-auth.d.ts`, `lib/repositories/IDoctorRepository.ts` + `MongoDoctorRepository.ts` (+test, `findById`), `lib/actions/doctor.actions.ts` (`createDoctorAccess`), `components/DoctorRow.tsx`, `lib/repositories/IAppointmentRepository.ts` + `MongoAppointmentRepository.ts` (+test, `findByDoctor`), `lib/actions/appointment.actions.ts` (`getMyAppointments`), `lib/repositories/IPatientRepository.ts` + `MongoPatientRepository.ts` (+test, `findById`), `lib/actions/patient.actions.ts` (`getPatientById`), `lib/validation.ts` (`ClinicalNoteValidation`)
* **Observaciones**:
  - Esto fue una decisión de alcance consultada con el usuario: la alternativa más chica era solo agregar un link "Ver ficha clínica" en `/admin` sin resolver el vínculo Doctor↔User. Se optó por la solución completa porque sin ella el rol Doctor no podía usar su propio login para nada — el ticket original la subestimaba.
  - Todas las acciones de escritura (`createDoctorAccess`, `createClinicalNote`) y de lectura sensible (`getPatientById`, `getClinicalNotesForPatient`) verifican la sesión del lado del servidor (`requireAdminSession`/`requireDoctorSession` nuevo, mismo patrón defensivo de TASK-006) — no dependen solo del middleware de página.
  - Límite conocido, no resuelto: si el Administrador crea un segundo acceso para un doctor que ya tiene uno (con otro email), `createDoctorAccess` no lo detecta y quedan dos cuentas de login apuntando al mismo `doctorId`. No había urgencia de resolverlo para el MVP; anotado para cuando exista una pantalla de gestión de usuarios.
  - Cualquier doctor autenticado puede ver el historial clínico de cualquier paciente (no solo de sus propios turnos) — coincide con cómo suele operar una clínica real (el historial completo es relevante para cualquier profesional que atienda al paciente), pero es una decisión implícita, no pedida explícitamente por el ticket.
  - Verificado de punta a punta con un script que crea un doctor, su acceso vinculado, un paciente y un turno, y confirma: la agenda del doctor lo muestra, la ficha trae los antecedentes, se puede cargar y leer una evolución. Se verificó también contra el servidor real que `/doctor` y `/doctor/patient/[id]` redirigen a `/login` sin sesión y a `/unauthorized` con un rol incorrecto (probado con una sesión de Administrador).

#### `[TASK-009]` Integración del Componente de Odontograma Interactivo
* **Descripción**: Integrar el componente visual de odontograma (piezas dentales 2D/3D) para registrar el estado de cada diente y guardarlo como JSON en MongoDB.
* **Criterios de Aceptación**:
  - [x] Renderizado gráfico de las 32 piezas dentales permanentes *(temporales quedó fuera de alcance, ver Observaciones)*.
  - [x] Selección de estados por pieza/cara (Caries, Obturado, Ausente, Endodoncia, Corona).
  - [x] Guardado y lectura del odontograma en MongoDB *(colección `Odontogram`, no `ClinicalRecord` — ver Observaciones)*.
* **Prioridad**: Alta | **Esfuerzo**: Alto (5 ptos) | **Dependencias**: TASK-008
* **Resultado**: Odontograma de 32 piezas permanentes (notación FDI 11-48), cada una con 5 caras (mesial, distal, vestibular, palatal, oclusal), cada cara pintable con uno de los 5 estados del ticket. Gráfico HTML/CSS (grid de 5 zonas por diente, sin SVG anatómico) integrado en `/doctor/patient/[id]`, debajo de los antecedentes médicos. Un click en una cara rota el estado (sano→Caries→Obturado→Ausente→Endodoncia→Corona→sano); un botón "Guardar odontograma" persiste todo el estado de una vez (no autosave por click). Se crea automáticamente un odontograma vacío la primera vez que se abre la ficha de un paciente sin uno.
  - **Archivos creados**: `lib/odontogram/createEmptyOdontogram.ts` (+test), `lib/db/models/Odontogram.ts` (+test), `lib/repositories/IOdontogramRepository.ts`, `lib/db/repositories/MongoOdontogramRepository.ts` (+test), `lib/actions/odontogram.actions.ts`, `components/Odontogram.tsx`
  - **Archivos modificados**: `app/doctor/patient/[id]/page.tsx`
* **Observaciones**:
  - Alcance acotado con el usuario antes de empezar: el ticket decía "32 piezas permanentes y temporales", ambiguo entre "32 piezas (que son permanentes)" y "necesito ambas denticiones". Se decidió **solo dentición permanente** por ahora — la temporal (20 piezas, numeración FDI 51-85, pacientes pediátricos) queda pendiente si la clínica atiende niños.
  - El modelo se llama `Odontogram` (un documento por paciente, no por turno), no `ClinicalRecord` como sugería el ticket — ya existía `ClinicalNote` de TASK-008 para las evoluciones por turno, y mezclar ambas cosas en una sola colección hubiera sido peor diseño que dos colecciones con responsabilidad única.
  - Simplificación de modelado: estados que en la práctica son de toda la pieza (Ausente, Corona) se guardan igual que los demás, pintando las 5 caras a la vez desde la UI — evita tener un concepto paralelo "estado de pieza completa" en el schema.
  - **Bug real encontrado en la verificación manual, no en los tests automatizados** (mismo patrón que TASK-007): `faces` es un subdocumento de Mongoose, no un objeto plano — sus valores viven detrás de getters, no como propiedades propias enumerables. El mapeo del repositorio hacía `faces: { ...faces }`, que copiaba las propiedades internas de Mongoose (`$__parent`, `_doc`, etc.) en vez de los valores reales, así que **todo lo guardado se leía vacío** al pasar por el repositorio. El test automatizado que debía cubrir esto verificaba contra `Odontogram.find()` (el documento crudo, con getters funcionando), no contra lo que devuelve el repositorio — pasaba igual aunque el bug estuviera presente. Corregido accediendo cada cara explícitamente en vez de spread, y se reforzaron los tests para verificar valores reales devueltos por el repositorio (no por consulta directa a Mongoose), que sí hubieran fallado con el código viejo.
  - Este es el tercer bug de esta sesión que los tests no agarraron por verificar "por el costado" en vez de a través de la ruta real que usa la aplicación (el de TASK-003 con la conexión, el de TASK-007 con la zona horaria, y este). Vale la pena tenerlo presente como categoría de riesgo recurrente, no solo como incidentes aislados.

### EPIC 5: Motor de Tarifas, Obras Sociales y Cobro en Recepción

#### `[TASK-010]` Nomenclador de Prestaciones y Seeder "Particular"
* **Descripción**: Crear el catálogo de tratamientos/prestaciones con sus precios base y pre-cargar la entidad `Particular / Sin Convenio` en MongoDB.
* **Criterios de Aceptación**:
  - [x] Script Seeder que inserta la Obra Social `Particular` y el Nomenclador Base (Consultas, Obturaciones, Limpiezas).
  - [x] Panel CRUD para administrar el catálogo de prestaciones y sus montos.
* **Prioridad**: Alta | **Esfuerzo**: Bajo (2 ptos) | **Dependencias**: TASK-002
* **Resultado**: Extendido `Treatment` (schema base ya creado en TASK-002) con `isActive` (mismo patrón soft-delete de Doctor/TASK-006). Nueva colección `InsuranceProvider` (Obra Social) — solo lo mínimo que pide este ticket: nombre único + `isActive`, sin Planes/Coberturas todavía (eso es alcance de TASK-011/012). Seeder (`pnpm db:seed-nomenclador`, idempotente) crea "Particular / Sin Convenio" y 3 prestaciones base (Consulta Odontológica, Obturación de Resina, Limpieza Dental). Panel `/admin/treatments` con alta, edición y desactivar/reactivar — mismo patrón de UI que `/admin/doctors` (fila con modo edición inline), pero sin foto ni disponibilidad, mucho más simple. Todas las mutaciones protegidas con `requireAdminSession` (mismo patrón defensivo de TASK-006/008).
  - **Archivos creados**: `lib/db/models/InsuranceProvider.ts` (+test), `lib/repositories/ITreatmentRepository.ts`, `lib/db/repositories/MongoTreatmentRepository.ts` (+test), `lib/repositories/IInsuranceProviderRepository.ts`, `lib/db/repositories/MongoInsuranceProviderRepository.ts` (+test), `lib/actions/treatment.actions.ts`, `scripts/seed-nomenclador.ts`, `components/forms/TreatmentForm.tsx`, `components/TreatmentRow.tsx`, `app/admin/treatments/page.tsx`
  - **Archivos modificados**: `lib/db/models/Treatment.ts` (+test, `isActive`), `lib/validation.ts` (`TreatmentFormValidation`), `app/admin/page.tsx` (link "Nomenclador"), `package.json` (script `db:seed-nomenclador`)
* **Observaciones**:
  - No se creó Server Action para `InsuranceProvider` más allá del repositorio — el ticket no pide UI para Obras Sociales todavía (solo el seeder de "Particular"), y TASK-011 es quien realmente va a necesitar leerlas desde un desplegable. Se agregan cuando haga falta, no antes.
  - `TreatmentForm.tsx` maneja alta y edición en un solo componente (a diferencia de `DoctorForm`/`EditDoctorForm`, separados en TASK-006) — la diferencia ahí era la foto opcional en edición; acá no hay archivos de por medio, así que un solo formulario con prop `treatment?` opcional alcanza sin duplicar código.
  - Verificado con script real: alta, edición de precio, desactivar (excluido de `findActive`, presente en `findAll`), reactivar. Seeder verificado idempotente (correrlo dos veces no duplica nada).

#### `[TASK-011]` Selección de Obra Social / Particular en Onboarding
* **Descripción**: Extender el formulario de registro de paciente (`RegisterForm.tsx`) para incluir la selección de Obra Social, Plan y N° de Afiliado (Default: Particular).
* **Criterios de Aceptación**:
  - [x] Campo desplegable de Obra Social *(Plan quedó fuera de alcance, ver Observaciones)* cargado dinámicamente.
  - [x] Por defecto selecciona `Particular / Sin Convenio`.
  - [x] Persistencia en el perfil del paciente.
* **Prioridad**: Media | **Esfuerzo**: Bajo (2 ptos) | **Dependencias**: TASK-010
* **Resultado**: El campo "Insurance provider" de `RegisterForm.tsx` pasó de texto libre a un desplegable cargado dinámicamente desde `InsuranceProvider` (vía nueva `getActiveInsuranceProviders`), con `DEFAULT_INSURANCE_PROVIDER` ("Particular / Sin Convenio", ahora una constante compartida entre el seeder de TASK-010 y este formulario, para que no queden dos copias del mismo string pudiendo desincronizarse) preseleccionado. "N° de Afiliado" ya existía como `insurancePolicyNumber` desde TASK-002 y no necesitó cambios. La persistencia en el perfil del paciente también ya funcionaba (el campo se guardaba tal cual desde antes) — lo único que faltaba era que el valor viniera de un catálogo real en vez de texto libre.
  - **Archivos creados**: `lib/actions/insuranceProvider.actions.ts`
  - **Archivos modificados**: `constants/index.ts` (`DEFAULT_INSURANCE_PROVIDER`), `scripts/seed-nomenclador.ts` (usa la constante compartida), `app/patients/[userId]/register/page.tsx`, `components/forms/RegisterForm.tsx`
* **Observaciones**:
  - El campo "Plan" que menciona la descripción del ticket **no se construyó** — no existe ninguna entidad Plan en el sistema (TASK-010 solo creó `InsuranceProvider`, sin Planes/Coberturas, siguiendo el modelo sugerido en `HEALTHCARE.md` pero acotado), y este ticket solo depende de TASK-010, no de algo que lo incluya. Se interpretó como alcance aspiracional del texto original, no como un requisito real de este ticket.
  - Verificado con script real: el desplegable trae el proveedor sembrado, y un paciente creado con ese valor lo persiste correctamente.

#### `[TASK-012]` Módulo de Recepción: Cálculo de Aranceles, Copagos y Cierre
* **Descripción**: Crear el panel de cobros para `Secretaría` al finalizar una consulta atendida, aplicando las reglas de cobertura (100% cobro en caso Particular).
* **Criterios de Aceptación**:
  - [x] Cálculo automático del saldo a cobrar según las prestaciones marcadas por el doctor.
  - [x] Registro del pago (Efectivo/Transferencia/Tarjeta) y cambio de estado de cita a `Finalizada`.
  - [x] Generación e impresión/descarga de recibo digital de cobro.
* **Prioridad**: Alta | **Esfuerzo**: Alto (5 ptos) | **Dependencias**: TASK-009, TASK-010, TASK-011
* **Resultado**: Se decidió no avanzar con la Fase 2 de `docs/obra-social.md` (Obras Sociales/Planes/Matriz de Coberturas) por decisión explícita del usuario tras completar TASK-011 — solo se implementó la Fase 1 (MVP "Particular First"), que coincide exactamente con los criterios de aceptación reales de este ticket (100% a cargo del paciente, sin copagos diferenciados). Se detectó y resolvió un prerrequisito no listado explícitamente: el doctor no tenía forma de marcar qué prestaciones realizó durante la consulta, así que `ClinicalNote` se extendió con un array `treatments` (snapshot de `treatmentId`/`name`/`price` al momento de marcarlo, no una referencia viva, para que cambios de precio futuros no alteren historial ya facturado). Se agregó el estado `completed` a `Appointment` (mostrado como "Finalizada" solo para ese valor, sin tocar el resto de los badges existentes, que siguen en inglés por ser comportamiento previo no relacionado a este ticket). Se creó el modelo `Payment` (nomenclatura en inglés, consistente con el resto del código; toda la UI nueva de este ticket está en español) con índice único por `appointmentId` para impedir el doble cobro de un mismo turno. El flujo completo: el doctor marca prestaciones al cargar una evolución (`/doctor/patient/[id]`) → `/recepcion` lista los turnos agendados con prestaciones cargadas y sin cobrar → la Secretaría elige medio de pago y cierra el cobro → se genera un recibo imprimible en `/recepcion/recibo/[appointmentId]` (usa `window.print()` del navegador, sin agregar ninguna librería de PDF nueva).
  - **Archivos creados**: `lib/db/models/Payment.ts`, `lib/repositories/IPaymentRepository.ts`, `lib/db/repositories/MongoPaymentRepository.ts`, `lib/db/repositories/MongoPaymentRepository.test.ts`, `lib/auth/requireSecretariaSession.ts`, `lib/actions/payment.actions.ts`, `components/forms/BillingForm.tsx`, `components/PrintButton.tsx`, `app/recepcion/page.tsx`, `app/recepcion/recibo/[appointmentId]/page.tsx`
  - **Archivos modificados**: `lib/db/models/ClinicalNote.ts`, `lib/repositories/IClinicalNoteRepository.ts`, `lib/db/repositories/MongoClinicalNoteRepository.ts`, `lib/db/repositories/MongoClinicalNoteRepository.test.ts` (treatments + `findByAppointmentId`), `lib/actions/clinicalNote.actions.ts`, `components/forms/ClinicalNoteForm.tsx`, `app/doctor/patient/[id]/page.tsx` (selección de prestaciones), `lib/db/models/Appointment.ts`, `types/index.d.ts` (estado `completed`), `components/StatusBadge.tsx`, `constants/index.ts` (`StatusIcon.completed`), `lib/validation.ts` (`PaymentFormValidation`)
* **Observaciones**:
  - No se construyeron `PlanObraSocial`, `MatrizCoberturas` ni `ComprobanteCobro` como entidades separadas (esos nombres en español venían del spec en `docs/obra-social.md`) — se usó nomenclatura en inglés (`Payment`) siguiendo la convención ya establecida en `Appointment`/`Treatment`/`Doctor`, con la UI 100% en español por decisión explícita del usuario. `docs/obra-social.md` queda como referencia conceptual para una futura Fase 2, no como spec de implementación literal.
  - `Payment` guarda snapshots (`patientName`, `doctorName`, ítems con nombre/precio) en vez de solo referencias, mismo patrón ya usado en `ClinicalNote.doctorName` — evita que un recibo ya emitido cambie si se edita el paciente o el precio de una prestación después.
  - El dashboard de `/admin` no fue tocado: los contadores de `getRecentAppointmentList` (scheduled/pending/cancelled) no suman turnos `completed`, quedan simplemente sin contar en ningún stat card. No estaba en el alcance de este ticket agregar un "Finalizadas" al dashboard de admin.
  - Verificado con script real (`scripts/tmp-verify-billing.ts`, borrado tras usarlo): creación de nota clínica con dos prestaciones, agregación correcta del total ($23.000), creación del pago, transición del turno a `completed`, recuperación del recibo por `appointmentId`, y bloqueo real (no solo a nivel de tipos) del doble cobro vía el índice único de Mongo.

#### `[TASK-013]` Actualizar Next.js por vulnerabilidad de seguridad conocida
* **Descripción**: Next.js 14.2.3 tiene una vulnerabilidad de seguridad confirmada por el equipo de Next.js (ver aviso oficial: https://nextjs.org/blog/security-update-2025-12-11, detectado vía warning de pnpm al instalar dependencias). Actualizar a una versión parcheada.
* **Criterios de Aceptación**:
  - [x] Investigar el aviso oficial y determinar la versión mínima parcheada.
  - [x] Actualizar `next` y validar breaking changes de App Router (rutas, `instrumentation.ts`, Sentry).
  - [x] `pnpm build` y `pnpm dev` funcionando sin regresiones tras la actualización.
* **Prioridad**: Alta | **Esfuerzo**: Medio (3 ptos) → terminó siendo Alto en la práctica | **Dependencias**: Ninguna
* **Resultado**: El alcance real terminó siendo mucho mayor al estimado. El aviso citado en el ticket (diciembre 2025) solo pedía `next@14.2.35`, pero al investigar apareció un aviso posterior (mayo 2026, 13 CVEs) donde **la rama 14.x se quedó sin parche** — la única remediación real es una versión mayor. Se investigó con el usuario el trade-off (parche menor insuficiente vs. upgrade mayor con breaking changes) y se optó por el upgrade completo a **Next.js 15.5.21** (última Maintenance LTS de la línea 15.x al momento del aviso de julio 2026) + **React 19.2.8** + `@sentry/nextjs@10.68.0` (la 8.9.2 no declaraba soporte real para Next 15) + `eslint-config-next@15.5.21`. Antes de ejecutar el upgrade se verificó específicamente el riesgo más crítico para esta app — compatibilidad de NextAuth v4 (que sostiene todo el RBAC vía `middleware.ts`) con Next 15 — porque reportes de foros sugerían incompatibilidad; se comprobó que `next-auth@4.24.15` ya declara soporte oficial (`next: "^15 || ^16"`, `react: "^19"`) en su `package.json` publicado, así que no fue necesario migrar a Auth.js v5 (que sigue en beta). Se corrió el codemod oficial `next-async-request-api` para convertir `params`/`searchParams` a `Promise` en las 6 rutas dinámicas afectadas, y se actualizó el tipo ambiental `SearchParamProps` en `types/index.d.ts` para reflejar eso. Al correr `pnpm build` por primera vez en toda la sesión (antes solo se había corrido `tsc --noEmit`), se descubrió que el build de producción nunca había pasado: un bug preexistente y ya documentado desde TASK-007 (`AppointmentForm.tsx` no mandaba `timeZone` a `updateAppointment`) bloqueaba el build entero, y `eslint-config-next@15.5.21` resultó más estricto con `import/order` que la versión 14.x, rompiendo el build en ~20 archivos preexistentes. Se corrigieron ambos de raíz (no se documentó más como "deuda preexistente a ignorar"). Verificación de RBAC/auth bajo Next 15 hecha con usuarios y credenciales descartables creados por script (nunca se leyó ni mostró ningún secreto de `.env.local`, que además el sistema de permisos bloquea leer directamente): login real por HTTP como Administrador/Doctor/Secretaria, confirmando que `middleware.ts` deniega correctamente las rutas de otros roles (307 a `/unauthorized`) y que las Server Actions gateadas por `requireXSession()` funcionan (ej. `/recepcion` como Secretaria devuelve 200 con contenido real).
  - **Archivos modificados**: `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml` (aprobación del build script de `sharp`, necesario para la optimización de imágenes en producción), `types/index.d.ts` (`SearchParamProps` async), `app/api/files/[fileId]/route.ts`, `app/api/files/[fileId]/route.test.ts`, `app/doctor/patient/[id]/page.tsx`, `app/patients/[userId]/new-appointment/page.tsx`, `app/patients/[userId]/new-appointment/success/page.tsx`, `app/patients/[userId]/register/page.tsx`, `app/recepcion/recibo/[appointmentId]/page.tsx`, `components/forms/AppointmentForm.tsx` (fix real del bug de `timeZone`), `lib/db/mongodb.ts`, `lib/db/mongoClientPromise.ts`, `types/next-auth.d.ts` (falsos positivos de `no-unused-vars` en declaraciones ambientales, silenciados puntualmente), y reordenamiento de imports vía `eslint --fix` en ~20 archivos de `lib/db/**`.
* **Observaciones**:
  - Los bypass de middleware más graves del aviso de mayo 2026 (`GHSA-267c-6grr-h53f`, `GHSA-26hh-7cqf-hhc6`, `GHSA-492v-c6pp-mqqv`) requieren Next.js ≥15.2.0/≥15.4.0 — no nos alcanzaban en 14.2.3 —, pero sí nos alcanzaban un DoS de severidad Alta en Server Components (≥13.0.0) y un cache poisoning en respuestas RSC (≥14.2.0, justo nuestra versión). Ambos quedan cerrados con este upgrade.
  - `pnpm build` nunca se había corrido en esta sesión antes de este ticket — toda la verificación previa fue `tsc --noEmit` + `vitest` + servidor de desarrollo. Quedó demostrado que eso no es equivalente: el build de producción llevaba rota toda la sesión por el bug de `timeZone`. **Recomendación para el resto del proyecto**: agregar `pnpm build` a la rutina de verificación de cada ticket, no solo al final.
  - No se migró a Auth.js v5 — sigue en beta (`5.0.0-beta.32`, publicada el 20 de julio de 2026) sin fecha de estabilización confirmada, y no hizo falta porque next-auth v4 ya soporta Next 15/16 oficialmente.
  - Quedan warnings no bloqueantes tras el upgrade: peer dependencies desactualizadas en `react-datepicker`, `react-onclickoutside`, `next-themes` y `lucide-react` (declaran soporte hasta React 18, pero funcionan correctamente en la práctica — se verificó `pnpm build` y el flujo de auth end-to-end sin errores), y un warning preexistente de `react-hooks/exhaustive-deps` en `FileUploader.tsx` no relacionado a este ticket.
  - Verificado con usuarios descartables creados y borrados por script (`scripts/tmp-verify-next15-auth.ts` + `scripts/tmp-cleanup-next15-auth.ts`, ambos borrados tras usarlos): login real vía `/api/auth/callback/credentials` para los tres roles, confirmando altas (200) y denegaciones (307 a `/unauthorized`) correctas de `middleware.ts`, y que `/recepcion` como Secretaria renderiza contenido real (la Server Action `getBillableAppointments`, gateada por `requireSecretariaSession`, funciona en Next 15).

### EPIC 7: Consistencia de Idioma

#### `[TASK-022]` Traducir el portal público del paciente a español
* **Descripción**: `/`, `/patients/[userId]/register`, `/patients/[userId]/new-appointment` y su página de éxito quedaron en inglés (texto original del template CarePulse, decisión explícita de fuera-de-alcance en TASK-004). El resto del sistema (`/admin/**`, `/doctor/**`, `/recepcion/**`, `/login`) ya estaba en español. A pedido del usuario, se unificó todo a español.
* **Criterios de Aceptación**:
  - [x] Todo el texto visible (headers, labels, placeholders, botones) del portal de paciente traducido a español.
  - [x] Los valores de enum (`Gender`, `IdentificationTypes`) mantienen sus valores internos en inglés (persistidos en Mongo, validados por `lib/validation.ts`) — solo se tradujo la etiqueta mostrada, mismo patrón ya usado para `PaymentMethod`.
  - [x] La suite de Playwright (`e2e/*.spec.ts`) actualizada en el mismo commit para los selectores que dependían del texto en inglés.
  - [x] `docs/TESTING.md` actualizado donde citaba texto literal en inglés que dejó de existir.
* **Prioridad**: Media | **Esfuerzo**: Medio (3 ptos) | **Dependencias**: Ninguna
* **Resultado**: El alcance terminó siendo más amplio que "solo el portal del paciente": al inventariar el texto en inglés (delegado a una exploración dedicada, cruzada contra los selectores de `e2e/*.spec.ts` para no romper la suite recién agregada), aparecieron mensajes de validación de Zod en **todo el sistema** (`lib/validation.ts` — usados también por `/admin/doctors`, `/admin/treatments`, la evolución clínica del doctor, etc.), no solo en el portal público. Se tradujeron los ~40 mensajes de error de las 12 schemas de `lib/validation.ts`, y se introdujo el patrón "enum en inglés + label map en español" (`GenderLabels`, `IdentificationTypeLabels`, `StatusLabels` en `constants/index.ts`) para no tocar los valores persistidos en Mongo ni las validaciones de Zod — mismo patrón ya usado para `PaymentMethod` en TASK-012. De paso se corrigió un bug real preexistente en `AppointmentModal.tsx`: los props `title`/`description` se declaraban pero nunca se renderizaban (el componente hardcodeaba su propio texto en inglés) — ahora sí se usan.
  - **Archivos modificados**: `lib/validation.ts` (todos los mensajes), `constants/index.ts` (`GenderLabels`, `IdentificationTypeLabels`, `StatusLabels`), `components/StatusBadge.tsx`, `app/page.tsx`, `app/admin/page.tsx`, `app/patients/[userId]/new-appointment/success/page.tsx`, `components/forms/PatientForm.tsx`, `components/forms/RegisterForm.tsx`, `components/forms/AppointmentForm.tsx`, `components/AppointmentModal.tsx` (+ fix de `title`/`description`), `components/table/columns.tsx`, `components/table/DataTable.tsx`, `components/FileUploader.tsx`, `components/SubmitButton.tsx`, `components/CustomFormField.tsx`, `app/loading.tsx`, `docs/TESTING.md`, `e2e/patient-flow.ts`, `e2e/02-flujo.spec.ts`, `e2e/03-adm-extra.spec.ts`, `e2e/04-security.spec.ts`.
* **Observaciones**:
  - El nombre de marca "CarePluse" (typo del template original de "CarePulse") y el prefijo "Dr." se dejaron sin tocar a propósito — no son un problema de idioma, son una decisión de branding fuera del alcance de este ticket.
  - Verificado de punta a punta: `tsc --noEmit` limpio, 148/148 tests de Vitest, y **35/35 tests de Playwright pasando contra el servidor real** (incluye los 4 flujos de negocio completos de punta a punta, RBAC, y los dos hallazgos de seguridad SEG-01/SEG-02) — la corrida completa confirmó que ningún selector de la suite quedó desalineado con el nuevo texto en español.

### EPIC 6: Hallazgos de la ronda de QA post-TASK-013 (resueltos)

#### `[TASK-014]` Aislar el entorno de datos de pruebas E2E de la base de desarrollo
* **Descripción**: la suite de Playwright agregada en la ronda de QA corría contra el `MONGODB_URI` de `.env.local`, la misma base que usa `pnpm dev` — no existía una base `-test`/`-e2e` separada como sí la hay para Vitest desde TASK-005. Cada corrida dejaba datos reales mezclados con datos de clínica (doctores/pacientes con prefijo `qa.*`).
* **Criterios de Aceptación**:
  - [x] `playwright.config.ts` apunta a una base Mongo separada de desarrollo (mismo patrón que `vitest.setup.ts`, TASK-005).
  - [x] Seed/cleanup automático de esa base antes/después de la suite (`globalSetup`/`globalTeardown` de Playwright).
  - [x] Documentado en `docs/TESTING.md` cómo correr `pnpm test:e2e` sin tocar datos reales.
* **Prioridad**: Alta | **Esfuerzo**: Bajo (2 ptos) | **Dependencias**: Ninguna
* **Resultado**: `playwright.config.ts` ahora levanta su **propio `next dev` en el puerto 3100** (no en el 3000 de `pnpm dev`, así ambos pueden correr en simultáneo) vía `webServer`, con `MONGODB_URI` sobreescrito a una base hermana `-e2e` (`e2e/testDb.ts`, mismo patrón de sufijo que `vitest.setup.ts`). Un `globalSetup` (`e2e/global-setup.ts`) siembra ahí, antes de cada corrida, el Administrador (mismas credenciales de `.env.local`), la Secretaria de QA, la obra social por defecto y el nomenclador base — todo idempotente. Un `globalTeardown` (`e2e/global-teardown.ts`) borra la base `-e2e` entera al terminar. El nomenclador (`BASE_TREATMENTS`) se extrajo a `lib/seedData/baseTreatments.ts` (antes vivía inline en `scripts/seed-nomenclador.ts`) para que tanto el script CLI como el `globalSetup` lo compartan sin duplicar precios/nombres.
  - **Archivos creados**: `lib/seedData/baseTreatments.ts`, `e2e/testDb.ts`, `e2e/global-setup.ts`, `e2e/global-teardown.ts`
  - **Archivos modificados**: `playwright.config.ts` (`webServer`, `globalSetup`, `globalTeardown`, puerto 3100), `scripts/seed-nomenclador.ts` (importa `BASE_TREATMENTS` en vez de definirlo inline), `docs/TESTING.md` (§3.1 nueva)
* **Observaciones**:
  - **Bug real encontrado en la primera corrida de verificación**: `global-teardown.ts` fallaba con `MongooseError: Connection operation buffering timed out after 10000ms` al hacer `dropDatabase()`. Causa: `globalSetup` y `globalTeardown` corren en el mismo proceso raíz de Playwright (a diferencia de los tests, que corren en workers separados) y comparten el caché de conexión de `connectToDatabase()` — `globalSetup` llamaba a `mongoose.disconnect()` al final, dejando ese caché con una conexión muerta que `globalTeardown` recibía tal cual (su chequeo de caché no sabe que se cerró), sin reconectar. Corregido quitando el `disconnect()` de `globalSetup` — la conexión queda viva para que `globalTeardown` la reutilice y la cierre recién al final, después de dropear la base.
  - Verificado con dos corridas completas de punta a punta tras el fix: 35/35 tests de Playwright pasando, `globalTeardown` sin error, y confirmado que Playwright cierra su propio servidor del puerto 3100 solo al terminar (sin procesos huérfanos).

#### `[TASK-016]` Contabilizar turnos `completed` en el dashboard de Admin
* **Descripción**: `getRecentAppointmentList` (`lib/actions/appointment.actions.ts`) solo acumulaba `scheduledCount`/`pendingCount`/`cancelledCount` — los turnos `completed` (ya cobrados por Recepción desde TASK-012) no sumaban en ningún stat card de `/admin`.
* **Criterios de Aceptación**:
  - [x] Agregar `completedCount` al agregado y un stat card "Finalizadas" en `/admin`.
  - [x] No romper los 3 contadores existentes.
* **Prioridad**: Media | **Esfuerzo**: Bajo (2 ptos) | **Dependencias**: TASK-012
* **Resultado**: Agregado `completedCount` al `reduce` de `getRecentAppointmentList` y un cuarto `StatCard` ("Turnos finalizados") en `/admin`. Escrito test-first: se agregó un `describe` nuevo en `appointment.actions.test.ts` que crea 7 turnos con los 4 estados y verifica los 4 contadores + el total, confirmado en rojo (`completedCount` no existía) antes de implementar.
  - **Archivos modificados**: `lib/actions/appointment.actions.ts`, `lib/actions/appointment.actions.test.ts`, `components/StatCard.tsx` (nuevo `type: "completed"`), `app/admin/page.tsx`
* **Observaciones**:
  - `StatCard` no tiene un asset de fondo dedicado para "completed" (los otros 3 usan imágenes PNG vía `tailwind.config.ts`, no colores planos) — se reutilizó `bg-appointments` en vez de generar un asset nuevo, ya que ambos representan un resultado positivo (mismo verde que `StatusBadge` ya usa para `scheduled` y `completed`). Documentado en el código, no es un descuido.
  - Al escribir el test de conteo se encontró (y quedó documentado como comentario en el código) el mismo patrón de caché de conexión corrupta que TASK-014: el describe anterior del mismo archivo (`connection handling`) desconecta Mongoose en su `afterAll`, y como Vitest corre los describes del mismo archivo en el mismo proceso, el test nuevo heredaba una conexión muerta. Se resolvió limpiando `global._mongooseCache` al inicio del test nuevo, mismo fix conceptual que en `global-setup.ts`.

#### `[TASK-019]` Mensaje de error visible cuando un Doctor no tiene perfil vinculado
* **Descripción**: `requireDoctorSession()` (`lib/auth/requireDoctorSession.ts:18`) lanza `"Forbidden: Doctor role with a linked doctor profile required"` cuando un `User` con `role: Doctor` no tiene `doctorId`. Ese error solo quedaba en el log de servidor — en la UI se traducía en listas vacías, sin ningún mensaje explícito.
* **Criterios de Aceptación**:
  - [x] Las Server Actions afectadas devuelven un estado de error legible (no solo `undefined`/lista vacía).
  - [x] `/doctor` muestra un mensaje claro ("Tu usuario no tiene un perfil de doctor vinculado, contactá al Administrador") en vez de fallar en silencio.
* **Prioridad**: Baja | **Esfuerzo**: Bajo (1-2 ptos) | **Dependencias**: TASK-008
* **Resultado**: `getMyAppointments` (única consumidora en todo el proyecto, verificado por búsqueda) ahora devuelve `{ appointments, hasLinkedProfile }` en vez de un array pelado — `hasLinkedProfile: false` cuando `requireDoctorSession()` rechaza específicamente por falta de `doctorId` (o cualquier otro motivo de sesión inválida), `true` en cualquier otro caso, incluida una agenda legítimamente vacía. `/doctor` usa ese flag para mostrar el mensaje específico en vez del genérico "No tenés turnos asignados." (que ahora se oculta en ese caso, para no mostrar los dos mensajes a la vez).
  - **Archivos modificados**: `lib/actions/appointment.actions.ts`, `app/doctor/page.tsx`
* **Observaciones**:
  - No se agregó test unitario nuevo para esta acción — sigue la convención ya establecida en el proyecto de no testear a nivel de Server Action las que dependen de `getServerSession` (requiere mockear next-auth, patrón no usado en ningún otro lado del código). Verificado en cambio con un usuario Doctor real sin `doctorId`, creado y borrado por script, logueado por HTTP contra el servidor real: `/doctor` muestra el mensaje nuevo y **no** muestra el genérico.
  - El resto de las Server Actions del Doctor (`getPatientById`, `getClinicalNotesForPatient`, `createClinicalNote`, odontograma) no se tocaron — hoy solo son alcanzables navegando desde `/doctor`, que ya corta el flujo antes de llegar ahí con el mensaje nuevo. Si en el futuro se linkean directo (sin pasar por `/doctor`), van a necesitar el mismo tratamiento.

#### `[TASK-020]` Alta de usuario Secretaria desde la UI de Administrador
* **Descripción**: a diferencia de Doctor (TASK-008, "Crear acceso" en `/admin/doctors`), no existía ningún flujo de alta para usuarios `role: Secretaria` — se creaba manualmente en Mongo (bcrypt + insert directo), documentado como prerrequisito operativo en `docs/TESTING.md` §3.4. Esto había bloqueado el arranque de la ronda de pruebas de Recepción hasta escribir un script ad-hoc (`scripts/qa-seed-secretaria.ts`).
* **Criterios de Aceptación**:
  - [x] Panel en `/admin` (`/admin/secretarias`, con link nuevo en el header de `/admin`) para crear usuarios `role: Secretaria` con email + contraseña, mismo patrón defensivo (`requireAdminSession`) que el resto de las mutaciones admin.
  - [x] No requiere vínculo a ninguna otra entidad (a diferencia de Doctor).
* **Prioridad**: Media | **Esfuerzo**: Bajo (2 ptos) | **Dependencias**: TASK-004
* **Resultado**: Nueva página `/admin/secretarias` (listado + alta), siguiendo el mismo patrón visual y de protección que `/admin/doctors`. `lib/actions/secretaria.actions.ts` nuevo: `createSecretariaAccess` (crea el `User` directamente, sin entidad intermedia — a diferencia de Doctor no hay un perfil clínico al que vincular) y `getSecretarias`. Se agregó `findByRole(role)` al repositorio de usuarios (TDD: test rojo primero, confirmando que el método no existía, luego implementado), reutilizado por `getSecretarias`.
  - **Archivos creados**: `lib/actions/secretaria.actions.ts`, `components/forms/CreateSecretariaForm.tsx`, `app/admin/secretarias/page.tsx`
  - **Archivos modificados**: `lib/repositories/IUserRepository.ts` + `MongoUserRepository.ts` (+test, `findByRole`), `lib/validation.ts` (`SecretariaFormValidation`), `app/admin/page.tsx` (link "Secretarías" en el header)
* **Observaciones**:
  - `createSecretariaAccess` reutiliza `MongoUserRepository.create()`, que ya tiene fallback de "devolver el usuario existente" ante email duplicado (mismo comportamiento que `createDoctorAccess` desde TASK-008) — si el email ya pertenece a un usuario de otro rol, hoy devuelve ese usuario tal cual sin cambiarle el rol. Es una inconsistencia preexistente, no introducida por este ticket; se mantuvo el mismo comportamiento por consistencia en vez de arreglarlo solo acá.
  - Verificado de punta a punta con un spec descartable de Playwright (reutilizando la infraestructura aislada de TASK-014 — base `-e2e`, puerto 3100 — borrado después de usarlo): login como Administrador → `/admin/secretarias` → alta de una secretaria → aparece en el listado → login con esas credenciales → redirige a `/recepcion`.

#### `[TASK-021]` Evitar accesos de login duplicados para un mismo Doctor
* **Descripción**: `createDoctorAccess` (TASK-008) no validaba si el `Doctor` ya tenía un `User` vinculado — se podía crear un segundo login con otro email apuntando al mismo `doctorId`, dejando dos cuentas activas para el mismo profesional. Documentado como límite conocido desde TASK-008, nunca ticketeado hasta esta revisión.
* **Criterios de Aceptación**:
  - [x] `createDoctorAccess` rechaza (o advierte) si el `doctorId` ya tiene un `User` con acceso creado.
  - [x] Mensaje de error claro en `/admin/doctors` en ese caso.
* **Prioridad**: Baja | **Esfuerzo**: Bajo (1-2 ptos) | **Dependencias**: TASK-008
* **Resultado**: Agregado `findByDoctorId(doctorId)` al repositorio de usuarios (TDD: rojo primero). `createDoctorAccess` lo consulta antes de crear el `User` y devuelve `{ error: "ALREADY_HAS_ACCESS" }` en vez de crear un segundo login. `CreateDoctorAccessForm.tsx` distingue ese caso del error genérico y muestra "Este doctor ya tiene un acceso creado. No se puede crear un segundo login." — importante: el chequeo original `if (user)` para decidir éxito trataba `{ error: ... }` como un resultado truthy (¡falso positivo de éxito!), así que el chequeo del formulario se reescribió para inspeccionar explícitamente la forma del resultado, no solo su truthiness.
  - **Archivos modificados**: `lib/repositories/IUserRepository.ts` + `MongoUserRepository.ts` (+test, `findByDoctorId`), `lib/actions/doctor.actions.ts`, `components/forms/CreateDoctorAccessForm.tsx`
* **Observaciones**:
  - Es un chequeo a nivel de aplicación (consulta antes de insertar), no una constraint única a nivel de base — hay una ventana de carrera teórica (dos "Crear acceso" simultáneos para el mismo doctor) aceptada dado que es una acción de administrador único, no concurrente en la práctica. No se agregó un índice único a `User.doctorId` para no abrir una migración de datos fuera del alcance de este ticket.
  - Verificado con un spec descartable de Playwright: alta de doctor → primer acceso exitoso → segundo acceso para el mismo doctor rechazado con el mensaje específico, formulario sigue abierto (no se trata como éxito). En la primera vuelta del script el test falló por una carrera de sincronización del script mismo (no esperaba a que el doctor apareciera en la lista antes de buscar la fila) — corregido agregando el mismo `await expect(...).toBeVisible()` que ya usa `e2e/00-setup.spec.ts` para el mismo propósito; no era un bug del código bajo prueba.

#### `[TASK-018]` Alta de turno directo desde `/admin`
* **Descripción**: `docs/TESTING.md` (ADM-08) documentaba "crear un turno directo para un paciente" como parte del flujo esperado de Administrador, pero `AppointmentModal.tsx` solo soportaba `type: "schedule" | "cancel"` sobre turnos ya creados por el paciente — no existía ninguna acción de alta de turno nuevo invocable desde `/admin`. Decidido con el usuario: era un gap real de producto, no del documento — y en vez de armar un listado completo de pacientes (no existía ningún `findAll`), se resolvió con búsqueda puntual por email/teléfono exacto.
* **Criterios de Aceptación**:
  - [x] Definir con Producto si el gap es del documento (ADM-08 mal descripto) o del producto (falta la función). → Es del producto.
  - [x] Formulario de alta de turno en `/admin` reutilizando `AppointmentForm.tsx`/`createAppointment` ya existentes, sin pasar por el flujo público del paciente.
* **Prioridad**: Media | **Esfuerzo**: Bajo (2 ptos) | **Dependencias**: TASK-007
* **Resultado**: Nuevo botón "Nuevo turno" en `/admin` que abre un modal (`AdminNewAppointmentModal.tsx`): primero busca al paciente por email o teléfono exacto (`findPatientByContact`, gateado por `requireAdminSession`, nuevo `findByEmailOrPhone` en el repositorio de pacientes — TDD, rojo primero) y, una vez encontrado, muestra el mismo `AppointmentForm` (`type="create"`) que ya usa el flujo público del paciente — sin duplicar lógica de creación de turno.
  - **Archivos creados**: `components/AdminNewAppointmentModal.tsx`
  - **Archivos modificados**: `lib/repositories/IPatientRepository.ts` + `MongoPatientRepository.ts` (+test, `findByEmailOrPhone`), `lib/actions/patient.actions.ts` (`findPatientByContact`), `app/admin/page.tsx`
* **Observaciones**:
  - `AppointmentForm` en modo `create` redirige al enviar a la página de éxito **pública** del paciente (`/patients/[userId]/new-appointment/success`), pensada originalmente para que el paciente vea el turno que acaba de pedir — no para el Administrador. Se dejó así a propósito (no se tocó `AppointmentForm`, componente compartido con el flujo público) en vez de bifurcar su comportamiento de éxito; el administrador ve el turno creado igual, solo que en una pantalla con el copy pensado para el paciente. Anotado como mejora posible, no bloqueante.
  - **Bug real encontrado en la verificación manual, no en ningún test automatizado**: el selector de fecha/hora de `AppointmentForm` presiona `Escape` para cerrar su propio popup (`pickAppointmentDateTime` en `e2e/helpers.ts`) — al envolver el formulario en un `Dialog` (algo que el flujo público nunca hacía), ese mismo `Escape` burbujeaba y cerraba **el modal entero**, descartando la búsqueda del paciente y todo el progreso del formulario en silencio. Corregido con `onEscapeKeyDown={(e) => e.preventDefault()}` en el `DialogContent` de `AdminNewAppointmentModal` (el botón "X" y el click afuera del modal lo siguen cerrando normalmente) — cambio acotado a este modal nuevo, no se tocó el `Dialog` compartido ni `AppointmentModal.tsx`.
  - Verificado de punta a punta con un spec descartable de Playwright: alta de doctor → registro público completo de un paciente (los dos pasos — el `Patient` recién se crea en el segundo) → como Administrador, "Nuevo turno" → buscar por email → elegir doctor, fecha y hora → enviar → el turno aparece `Pendiente` en `/admin`. Corrida completa de los 35 tests de Playwright después del fix, sin regresiones.

#### `[TASK-017]` Decisión de Producto: cobro manual/walk-in en Recepción
* **Descripción**: `/recepcion` solo listaba turnos `scheduled` con al menos una prestación cargada por el doctor en `ClinicalNote.treatments`. Si el doctor no cargaba ninguna evolución con prestaciones, el turno nunca aparecía para cobrar y no había pantalla alternativa de carga manual. Decidido con el usuario: agregar cobro manual simple, reutilizando el nomenclador existente (no montos libres, para no perder trazabilidad de qué prestación se cobró).
* **Criterios de Aceptación**:
  - [x] Producto confirma si es un límite aceptable del MVP o si Recepción necesita poder cargar un cobro sin depender del doctor. → Necesita poder hacerlo.
  - [x] UI en `/recepcion` para seleccionar prestaciones manualmente sobre un turno `scheduled` sin evolución.
* **Prioridad**: Media | **Esfuerzo**: sin estimar (dependía de la decisión) → terminó siendo Bajo-Medio, como se estimaba en la opción elegida | **Dependencias**: TASK-012
* **Resultado**: `getBillableAppointments` ahora lista **todos** los turnos `scheduled` sin pagar (antes filtraba los que no tenían prestaciones cargadas), con un flag nuevo `hasChartedTreatments`. `BillingForm.tsx` bifurca según ese flag: si el doctor cargó prestaciones, se ve el detalle de solo lectura de siempre; si no, la Secretaria tilda prestaciones del nomenclador (mismo patrón de checkboxes que ya usa el doctor en `ClinicalNoteForm`) y ve el total actualizarse en vivo. `closeAppointmentBilling` ahora acepta `manualTreatmentIds` opcional — cuando no hay evolución cargada, resuelve nombre y precio de cada prestación **del lado del servidor** contra el nomenclador vigente (nunca confía en un precio mandado por el cliente), mismo criterio de integridad que ya usa el "fotografiado" de precios en `ClinicalNote`.
  - **Archivos modificados**: `lib/actions/payment.actions.ts` (`getBillableAppointments`, `closeAppointmentBilling`), `components/forms/BillingForm.tsx`, `components/SubmitButton.tsx` (nuevo prop `disabled`, ver Observaciones), `app/recepcion/page.tsx`
* **Observaciones**:
  - Al armar el botón de "Cobrar" para el caso manual (debe quedar deshabilitado hasta tildar al menos una prestación) se encontró que `SubmitButton` solo exponía `isLoading` — reutilizarlo para "deshabilitado por selección vacía" hubiera mostrado el texto "Cargando..." de forma engañosa sin estar cargando nada. Se agregó un prop `disabled?: boolean` genuino en vez de forzar `isLoading`, sin romper ningún llamador existente (prop opcional).
  - El precio se resuelve contra `findActive()` en el momento del cobro — si una prestación se desactiva entre que la Secretaria abre el formulario y confirma el cobro, simplemente no aparece entre las opciones resueltas y ese ítem se cae en silencio del total. Es el mismo comportamiento ya aceptado en otros lados del sistema (ninguna otra prestación in-flight se revalida contra cambios concurrentes); no se agregó un chequeo adicional por ser un caso de carrera de probabilidad mínima para una única Secretaria operando a la vez.
  - Verificado de punta a punta con un spec descartable de Playwright: turno confirmado sin ninguna evolución cargada → `/recepcion` muestra el aviso "El doctor no cargó prestaciones..." → Secretaria tilda 2 prestaciones ($5.000 + $8.000) → total en vivo $13.000 → cobra → recibo muestra ambos ítems y el total correcto. Corrida completa de los 35 tests de Playwright después del cambio, sin regresiones (la ampliación de `getBillableAppointments` no rompió ningún caso existente de turnos con evolución cargada).

### EPIC 8: Cancelación del Portal de Pacientes y Alta Staff-Side

#### `[TASK-015]` Decisión de Producto: autenticación real para el portal de pacientes y descarga de archivos
* **Descripción**: confirmado en la ronda de QA post-TASK-013 (casos SEG-01 y SEG-02 de `docs/TESTING.md`) que `/patients/[userId]/**` y `/api/files/[fileId]` seguían sin ningún control de sesión — dependían únicamente de que el `userId`/`fileId` (ObjectId de Mongo) fuera difícil de adivinar.
* **Criterios de Aceptación**:
  - [x] Producto decide si es aceptable para el MVP actual o si bloquea la salida a producción con pacientes reales. → Bloquea; se corrige.
  - [x] Mecanismo de autenticación de pacientes (DNI+PIN) implementado, y `/api/files/[fileId]` exige sesión.
* **Prioridad**: Alta | **Esfuerzo**: Medio | **Dependencias**: Ninguna
* **Resultado**: Implementado en el commit `b6f4915` ("feat(auth): add patient authentication and login flow") — login de paciente por DNI+PIN (`/patients/login`, `lib/auth/authenticatePatientCredentials.ts`), protección por-recurso de `/patients/[userId]/**` en `middleware.ts`, y exigencia de sesión (cualquier rol autenticado) en `app/api/files/[fileId]/route.ts`.
* **Observaciones**:
  - **Superado por TASK-023**: en la sesión siguiente, el usuario tomó la decisión de producto de eliminar el portal de pacientes por completo — no como limitación técnica (el login de TASK-015 funcionaba correctamente), sino porque el onboarding pasa a ser 100% mediado por el staff. El login DNI+PIN, `/patients/login` y `/patients/[userId]/register`/`new-appointment` fueron removidos en TASK-023. La única pieza de TASK-015 que sobrevive es la exigencia de sesión en `/api/files/[fileId]` (ahora de hecho staff-only, dado que no queda ningún otro rol que pueda autenticarse para pedirlo) — ver TASK-023 más abajo.

#### `[TASK-023]` Eliminación del portal de auto-servicio de pacientes
* **Descripción**: decisión de producto — los pacientes ya no tienen ningún acceso al sistema. Todo el onboarding (alta de ficha, turnos) pasa a ser gestionado por Secretaría/Administrador. Se elimina el login DNI+PIN y el flujo público de registro/turnos agregado en `b6f4915` (TASK-015), así como el flujo de auto-registro previo (`PatientForm`/`RegisterForm`, existente desde antes de TASK-004).
* **Criterios de Aceptación**:
  - [x] `/patients/login`, `/patients/[userId]/register`, `/patients/[userId]/new-appointment` (y su página de éxito) eliminadas — devuelven 404.
  - [x] `middleware.ts` y `authOptions.ts` sin el proveedor/redirect de paciente.
  - [x] `/api/files/[fileId]` sigue exigiendo sesión (regresión, sin tocar el archivo ni su test).
* **Prioridad**: Alta | **Esfuerzo**: Medio | **Dependencias**: Ninguna (combinado con TASK-024, ver Observaciones)
* **Resultado**: Eliminados `app/patients/login/`, `app/patients/[userId]/register/`, `app/patients/[userId]/new-appointment/` (con `success/`), `components/forms/PatientLoginForm.tsx`, `components/forms/RegisterForm.tsx`, `components/forms/PatientForm.tsx`, `lib/auth/authenticatePatientCredentials.ts` (+test). `middleware.ts` perdió la función `patientsRedirect` y el caso especial de `/patients/` en el callback `authorized`, y `/patients/:path*` salió del `matcher`. `authOptions.ts` perdió el `CredentialsProvider` de `patient-credentials`. `app/page.tsx` dejó de renderizar `PatientForm` (que ya no tenía a dónde redirigir tras crear el registro) y ahora es una landing puramente staff (link a `/login`). `app/api/files/[fileId]/route.ts` **no se tocó** — sigue exigiendo cualquier sesión autenticada, lo cual en la práctica ahora es staff-only porque no queda ningún otro rol capaz de autenticarse.
  - **Archivos eliminados**: `app/patients/login/page.tsx`, `app/patients/[userId]/register/page.tsx`, `app/patients/[userId]/new-appointment/page.tsx`, `app/patients/[userId]/new-appointment/success/page.tsx`, `components/forms/PatientLoginForm.tsx`, `components/forms/RegisterForm.tsx`, `components/forms/PatientForm.tsx`, `lib/auth/authenticatePatientCredentials.ts` (+test), `lib/actions/patient.actions.test.ts` (cubría `createUser`, eliminado con él)
  - **Archivos modificados**: `middleware.ts`, `lib/auth/authOptions.ts`, `app/page.tsx`
* **Observaciones**: ver la entrada combinada de TASK-024 — ambos tickets se hicieron en la misma pasada porque los fixtures de e2e (`e2e/patient-flow.ts`) usaban el flujo público como boilerplate de setup para `02-flujo.spec.ts`/`03-adm-extra.spec.ts`/`04-security.spec.ts`; borrar TASK-023 sin tener ya el reemplazo de TASK-024 hubiera dejado esas specs sin ningún fixture de alta de paciente.

#### `[TASK-024]` Alta de paciente desde `/recepcion` y `/admin` (staff-side)
* **Descripción**: nueva Server Action `createPatient`, formulario compartido `CreatePatientForm` y dos páginas (`/recepcion/pacientes/nuevo`, `/admin/pacientes/nuevo`) para que Secretaría o Administrador den de alta una ficha de paciente sin que el paciente pase por ningún formulario propio.
* **Criterios de Aceptación**:
  - [x] `Patient` acepta creación sin `userId`, sin contacto de emergencia, sin obra social (default `DEFAULT_INSURANCE_PROVIDER`).
  - [x] `createPatient` rechaza sin sesión de Secretaria o Administrador; crea el registro con una válida.
  - [x] `CreatePatientForm` sin dependencia de un `User` previo, montada en ambas rutas con link "Nuevo paciente" desde `/recepcion` y `/admin`.
  - [x] Fixtures de e2e migrados al nuevo flujo staff-side; `02-flujo.spec.ts`/`03-adm-extra.spec.ts` siguen pasando.
* **Prioridad**: Alta | **Esfuerzo**: Medio | **Dependencias**: Ninguna
* **Resultado**: `Patient` (`lib/db/models/Patient.ts`) y `Appointment` (`lib/db/models/Appointment.ts`) tienen `userId` opcional ahora — mismo patrón que `Doctor`/`createDoctorAccess` (un perfil clínico puede existir sin ningún login asociado). `emergencyContactName`, `emergencyContactNumber` e `insurancePolicyNumber` de `Patient` también pasaron a opcionales; `insuranceProvider` quedó opcional a nivel de input pero con `default: DEFAULT_INSURANCE_PROVIDER` a nivel de schema Mongoose (no solo Zod), así que un alta sin obra social explícita siempre persiste "Particular / Sin Convenio". Nueva Server Action `createPatient` (`lib/actions/patient.actions.ts`), gateada por `requireSecretariaOrAdminSession` (nuevo, `lib/auth/requireSecretariaOrAdminSession.ts` — mismo patrón defensivo de `requireAdminSession`/`requireSecretariaSession`, pero aceptando cualquiera de los dos roles). No llama a `createUser` ni necesita `userId`. Nuevo componente `CreatePatientForm.tsx` (adaptado de `RegisterForm.tsx`, sin depender de un `user` previo) con los campos requeridos (nombre, email, teléfono, fecha de nacimiento, género, dirección, ocupación, médico primario, documento de identificación tipo+número+archivo) y opcionales (contacto de emergencia, obra social + N° de afiliado) pedidos por el ticket. Montado en `app/recepcion/pacientes/nuevo/page.tsx` (flujo primario) y `app/admin/pacientes/nuevo/page.tsx`, cada una con su link "Nuevo paciente" en el header correspondiente (mismo patrón que "Secretarías"/TASK-020, "Nomenclador"/TASK-010).
  - **Archivos creados**: `lib/auth/requireSecretariaOrAdminSession.ts`, `components/forms/CreatePatientForm.tsx`, `app/recepcion/pacientes/nuevo/page.tsx`, `app/admin/pacientes/nuevo/page.tsx`
  - **Archivos modificados**: `lib/db/models/Patient.ts` (+test), `lib/db/models/Appointment.ts` (+test), `lib/repositories/IPatientRepository.ts`, `lib/db/repositories/MongoPatientRepository.ts` (+test), `lib/repositories/IAppointmentRepository.ts`, `lib/db/repositories/MongoAppointmentRepository.ts`, `lib/actions/patient.actions.ts`, `lib/actions/appointment.actions.ts`, `lib/validation.ts` (`CreatePatientFormValidation`; se eliminaron `UserFormValidation`, `PatientFormValidation`, `PatientLoginValidation` — quedaron sin ningún consumidor), `types/index.d.ts` (se eliminaron `CreateUserParams`/`User`/`RegisterUserParams`; `CreateAppointmentParams`/`UpdateAppointmentParams.userId` opcional), `types/appwrite.types.ts` (`Patient.userId`/`emergencyContact*`/`insurancePolicyNumber` y `Appointment.userId` opcionales), `lib/repositories/IUserRepository.ts` + `MongoUserRepository.ts` (+test, se eliminó `findByIdentificationNumberWithPassword` — solo la usaba el login de paciente eliminado en TASK-023), `components/forms/AppointmentForm.tsx`, `components/AppointmentModal.tsx`, `components/AdminNewAppointmentModal.tsx`, `app/recepcion/page.tsx`, `app/admin/page.tsx`, `package.json` (+`libphonenumber-js`, ver Observaciones), `e2e/patient-flow.ts`, `e2e/02-flujo.spec.ts`, `e2e/03-adm-extra.spec.ts`, `e2e/04-security.spec.ts`, `e2e/helpers.ts`, `e2e/db.ts`, `e2e/state.ts`
* **Observaciones**:
  - **El acoplamiento de `AppointmentForm` documentado en TASK-018** (`type="create"` redirigía a la página de éxito **pública** del paciente) se resolvió de raíz en vez de parchearlo: como esa página ya no existe, y `type="create"` hoy solo se alcanza desde `AdminNewAppointmentModal` (staff), el éxito ahora cierra el modal (`setOpen?.(false)`) y hace `router.refresh()` en vez de navegar — mismo patrón que ya usaban `schedule`/`cancel`. Se agregó el prop `setOpen` (antes no se pasaba) a la instancia de `AppointmentForm` dentro de `AdminNewAppointmentModal`. Verificado con Playwright (`ADM-09`, `PAC-07`, `DOC-06` en la suite) y manualmente: "Nuevo turno" desde `/admin` crea el turno, cierra el modal y el listado se actualiza sin recargar la página.
  - **`Appointment.userId` también se volvió opcional** (no pedido explícitamente por el ticket, pero necesario): existía solo para resolver el teléfono del paciente al mandar el SMS de confirmación/cancelación (`sendSMSNotification`). Como los pacientes de alta staff-side nunca tienen `userId`, mantenerlo `required: true` hubiera roto la creación de turnos por completo. `updateAppointment` ahora omite el envío de SMS cuando no hay `userId` (antes fallaba igual puertas adentro, atrapado por su propio try/catch, pero intentaba la consulta igual). **Limitación conocida, no resuelta acá**: las notificaciones SMS de confirmación/cancelación dejaron de dispararse para cualquier paciente dado de alta desde TASK-024 en adelante, porque ya no hay `User` del que sacar el teléfono — el dato SÍ está disponible (`Patient.phone`), solo que `sendSMSNotification` no lo consulta por ahí. Recomendado un ticket de seguimiento para resolver el teléfono vía `patientId` en vez de `userId`.
  - **Consentimiento de privacidad**: `Patient.privacyConsent` sigue siendo obligatorio a nivel de schema y no se relajó. `createPatient` lo fija en `true` automáticamente — la premisa de este ticket es que el consentimiento se captura fuera del sistema (papel/verbal en la recepción) antes de cargar la ficha, no con un checkbox en pantalla (no hay ningún paciente frente al teclado). No pedido explícitamente por el ticket; decisión tomada para no inventar una UI de consentimiento no solicitada.
  - **Guardia server-side de las páginas**: el ticket pedía que `/recepcion/pacientes/nuevo` y `/admin/pacientes/nuevo` llamaran explícitamente a `requireSecretariaSession`/`requireAdminSession` además de `middleware.ts`. Se verificó que **ningún** `page.tsx` del proyecto hace eso hoy (ej. `/admin/doctors`, `/admin/treatments`) — el patrón real y consistente en todo el código es que solo las Server Actions de mutación llaman a `requireXSession()` (la razón documentada en TASK-006: una Server Action tiene su propio endpoint HTTP, invocable sin pasar por el middleware de la página). Se siguió ese patrón real en vez del texto literal del ticket: `createPatient` ya hace el chequeo server-side, las páginas confían en `middleware.ts` igual que sus pares.
  - **`findByIdentificationNumberWithPassword`** (`IUserRepository`/`MongoUserRepository`, agregado en TASK-015 solo para el login DNI+PIN) se eliminó junto con su test — sin `authenticatePatientCredentials`, no le quedaba ningún consumidor de producción. Mismo criterio de "no dejar código muerto" aplicado explícitamente por el ticket a `createUser`/`registerPatient`.
  - **Dependencia faltante detectada al correr `pnpm build`** (no relacionada al alcance de este ticket, pero bloqueaba la verificación): `components/CustomFormField.tsx` importa `libphonenumber-js/core` directamente, pero el paquete nunca estuvo declarado en `package.json` (solo llegaba como dependencia transitiva de `react-phone-number-input`, y con la resolución estricta de pnpm eso no alcanza para un `import` directo de la app). Se agregó como dependencia directa (`libphonenumber-js@1.13.9`, misma versión ya resuelta en el lockfile) para que `pnpm build` pasara. Mismo tipo de gap que las dependencias de peer ya documentadas en TASK-013.
  - **`docs/TESTING.md` y `docs/ARQUITECTURA.md` no se actualizaron** en este ticket (fuera del alcance pedido, que solo mencionaba `docs/PLANNING.md`) — ambos todavía describen el portal de pacientes eliminado (casos PAC-01/03/07 como flujo público, referencias a `RegisterForm.tsx`, etc.). Recomendado un ticket de seguimiento chico para alinear `TESTING.md` con los casos reales post-TASK-023/024.
  - Verificado de punta a punta: `tsc --noEmit` limpio, `pnpm build` exitoso, **158/158 tests de Vitest** y **33/33 tests de Playwright** (incluye el flujo completo de alta→turno→cobro con el nuevo mecanismo staff-side, el choque de horario ADM-09 con dos sesiones de Administrador en paralelo, y las nuevas verificaciones de 404 del portal eliminado).

#### `[TASK-028]` Decisión de Producto: no se corrige el disparo de SMS de confirmación/cancelación para pacientes sin `userId`
* **Descripción**: `TASK-024` documentó como limitación conocida que las notificaciones SMS de confirmación/cancelación de turno dejaron de dispararse para los pacientes dados de alta desde el flujo staff-side, porque `sendSMSNotification` resuelve el teléfono vía `Appointment.userId` → `User` (ahora inexistente para estos pacientes) en vez de vía `Patient.phone` directamente. Se recomendaba un ticket de seguimiento para decidir si se resolvía.
* **Criterios de Aceptación**:
  - [x] Producto decide si se prioriza reescribir la resolución del teléfono para reactivar el SMS, o si se acepta la pérdida de la notificación. → **No se prioriza: SMS de confirmación/cancelación queda fuera de alcance por decisión de producto — no se usa por ahora.**
* **Prioridad**: Baja | **Esfuerzo**: Ninguno (decisión, sin implementación) | **Dependencias**: TASK-024
* **Resultado**: Sin cambios de código. `updateAppointment` (`lib/actions/appointment.actions.ts`) sigue omitiendo el envío de SMS en silencio cuando el turno no tiene `userId` vinculado — comportamiento introducido en `TASK-024`, confirmado ahora como definitivo y no como deuda pendiente. Ningún paciente de alta staff-side (todos, desde `TASK-024` en adelante) recibe SMS de confirmación/cancelación.
* **Observaciones**:
  - **Decisión de producto, no limitación técnica**: el dato para resolver el teléfono sí existe (`Patient.phone`); la alternativa de reescribir `sendSMSNotification` para consultarlo por `patientId` en vez de `userId` fue considerada y descartada — no se justifica porque la notificación SMS no se usa por ahora.
  - Mismo patrón que `TASK-015`: una limitación técnica documentada en un ticket previo (acá, `TASK-024`) se cierra en un ticket posterior mediante una decisión de producto que la deja fuera de alcance, en vez de una corrección de código.

#### `[TASK-029]` Alinear `docs/TESTING.md` y `docs/ARQUITECTURA.md` con la eliminación del portal de pacientes
* **Descripción**: `TASK-024` dejó documentado que `docs/TESTING.md` y `docs/ARQUITECTURA.md` no se habían actualizado en esa pasada — seguían describiendo el portal de pacientes eliminado (auto-registro, login DNI+PIN, turnos self-service), incluyendo los casos `PAC-*` de `TESTING.md` que probaban ese flujo público paso a paso.
* **Criterios de Aceptación**:
  - [x] `docs/TESTING.md`: casos que probaban el flujo público eliminado, reescritos para el flujo staff-side equivalente donde la capacidad subyacente sigue existiendo, o retirados (documentados como tales) donde no.
  - [x] `docs/TESTING.md`: hallazgos `SEG-01`/`SEG-02` documentados como resueltos (por eliminación de superficie y por staff-only de construcción, respectivamente), no borrados en silencio.
  - [x] `docs/ARQUITECTURA.md`: secciones que describían el flujo de paciente público o referenciaban `RegisterForm.tsx`/`PatientForm.tsx` actualizadas al flujo real actual.
* **Prioridad**: Baja | **Esfuerzo**: Bajo | **Dependencias**: TASK-023, TASK-024
* **Resultado**: Documentación alineada con el estado real del código post-`TASK-023`/`TASK-024`, sin tocar ningún archivo de código.
  - **`docs/TESTING.md`**: tabla de actores (§2) — fila "Paciente" reescrita (ya no es un actor con acceso propio) más nota explicando la eliminación; nota de idioma actualizada. Matriz de RBAC (§5) — columna "Paciente" eliminada, fila `/patients/[userId]/**` reemplazada por `/patients/**` marcada como eliminada (404 para cualquiera), fila `/api/files/[fileId]` actualizada para reflejar que exige sesión desde `TASK-015` y que es staff-only en la práctica desde `TASK-023`. `SEG-01` reescrito como hallazgo **resuelto por eliminación** de la superficie pública (ya no es un caso a ejecutar). `SEG-02` reescrito como **staff-only por construcción**, con pasos actualizados (401 sin sesión / 200 con sesión staff). Sección `6.1` ("Paciente") renombrada a "Alta de pacientes y turnos, staff-side" y reescrita entera: `PAC-01/03` fusionados en un solo caso (alta vía `CreatePatientForm`, documento de identificación ahora obligatorio), `PAC-02` con la tabla de validaciones actualizada a los campos reales de `CreatePatientFormValidation`, `PAC-04` reescrito a "documento obligatorio" (el viejo caso de consentimientos ya no aplica — no hay checkboxes, `createPatient` fija `privacyConsent: true` automáticamente), `PAC-05` adaptado a la nueva ruta, `PAC-06` marcado explícitamente **retirado sin reemplazo** (no existe más el concepto de "paciente a medio registrar"), `PAC-07` reescrito a alta de turno vía "Nuevo turno" desde `/admin`, `PAC-08` sin cambios de fondo. `FLU-01` actualizado al camino staff-side. Resumen de hallazgos (§10) actualizado para reflejar `SEG-01`/`SEG-02` resueltos. `ADM-10` recibió una nota aclarando que, desde `TASK-024`, ningún paciente nuevo tiene `userId`, por lo que el envío de SMS se omite antes de llegar a Twilio (referencia a `TASK-028`).
  - **`docs/ARQUITECTURA.md`**: árbol de directorios (§3) — eliminada la línea `app/patients/`, agregada `app/recepcion/` con nota de alta de pacientes, comentario de `page.tsx` actualizado a landing staff-only, comentario de `components/forms/` actualizado (`PatientForm`/`RegisterForm` → `CreatePatientForm`). Nueva sección `4.5` (mismo patrón `✅ Resuelto (TASK-XXX)` que `4.1`-`4.4`) documentando la eliminación del portal y su reemplazo staff-side, con referencia a la limitación de SMS y a `TASK-028`.
  - Se verificó contra el código real (`e2e/02-flujo.spec.ts`, `e2e/03-adm-extra.spec.ts`, `e2e/04-security.spec.ts`, `e2e/patient-flow.ts`, `components/forms/CreatePatientForm.tsx`, `lib/validation.ts`) para que los casos reescritos de `TESTING.md` coincidieran exactamente con el comportamiento y los IDs de test ya usados por la suite automatizada, no con una reconstrucción aproximada.
* **Observaciones**:
  - No se tocó `docs/MVP.md`, el resto de `docs/PLANNING.md`, `docs/HEALTHCARE.MD` ni `docs/obra-social.md` — fuera de alcance de este ticket.
  - `docs/ARQUITECTURA.md` conserva staleness preexistente no relacionada al portal de pacientes (ej. seguir listando Appwrite/Next.js 14.2.3 como stack actual, pese a `TASK-003`/`TASK-013`) — no corregida acá por estar fuera del alcance específico de este ticket (portal de pacientes), no por descuido.

#### `[TASK-025]` Revocación real de acceso para roles de staff
* **Descripción**: hoy "desactivar" a un Doctor (`setDoctorActive`) solo lo oculta del directorio público — `authenticateCredentials` nunca chequea ningún flag de actividad, así que un Doctor "desactivado" puede seguir logueándose. Hallazgo propio, no de ningún ticket anterior. Hay que corregirlo antes de construir desactivación para Secretaria/Administrador (TASK-026/TASK-027), o esas funciones serían cosméticas.
* **Criterios de Aceptación**:
  - [x] `IUser`/`userSchema` incorpora `isActive: boolean` (default `true`).
  - [x] `authenticateCredentials` rechaza el login si `isActive === false`.
  - [x] `setDoctorActive` actualiza también el `isActive` del `User` vinculado al doctor (hoy son dos flags separados sin relación).
* **Prioridad**: Alta (hallazgo de seguridad) | **Esfuerzo**: Bajo-Medio | **Dependencias**: Ninguna
* **Resultado**: `User`/`IUser` (`lib/db/models/User.ts`) incorpora `isActive?: boolean` con `default: true` a nivel de schema. `IUserRepository`/`MongoUserRepository` exponen el campo en `CreateUserInput`/`UserRecord` y agregan `setActiveByDoctorId(doctorId, isActive)` (mismo patrón `findOneAndUpdate` que `MongoDoctorRepository.setActive`), que devuelve `null` sin error cuando ningún `User` está vinculado a ese `doctorId` — caso esperado, no un fallo, porque "Crear acceso" (TASK-008) es un paso opcional separado del alta de Doctor. `authenticateCredentials` rechaza el login con `user.isActive === false` — chequeo explícito, no `!user.isActive` ni `!== true` (ver Observaciones). `setDoctorActive` (`lib/actions/doctor.actions.ts`) ahora llama también a `userRepository.setActiveByDoctorId(id, isActive)` después de `doctorRepository.setActive`, sin manejo especial de "no encontrado" porque el repositorio ya lo resuelve como no-op.
  - **Archivos modificados**: `lib/db/models/User.ts` (+test), `lib/repositories/IUserRepository.ts`, `lib/db/repositories/MongoUserRepository.ts` (+test), `lib/auth/authenticateCredentials.ts` (+test), `lib/actions/doctor.actions.ts`
* **Observaciones**:
  - **Detalle de corrección crítico, verificado con un test dedicado**: el chequeo es `user.isActive === false`, nunca `!user.isActive`. Los defaults de schema de Mongoose solo aplican a documentos creados a partir de ahora — no rellenan retroactivamente `isActive` en documentos ya persistidos antes de esta migración (el Admin sembrado de TASK-004, los Doctores/Secretarias de TASK-006/TASK-020). Esos usuarios existentes van a leer `isActive: undefined`, no `true`. Un chequeo con `!user.isActive` habría bloqueado a todo usuario preexistente en la base apenas se despliega este cambio. Test agregado en `authenticateCredentials.test.ts` que crea un usuario, le remueve `isActive` directo a nivel de colección (simulando un documento anterior a este campo) y confirma que el login sigue funcionando.
  - No se necesitó ni se escribió ningún script de migración/backfill — `undefined` comportándose como "activo" bajo el chequeo `=== false` es la propiedad de seguridad buscada, no un gap pendiente.
  - No se agregó `lib/actions/doctor.actions.test.ts` para cubrir el cambio en `setDoctorActive` — no existe ese archivo hoy, y el patrón confirmado en TASK-019/TASK-006 es no testear unitariamente Server Actions gateadas por `getServerSession`/`requireAdminSession` en este proyecto (mockear next-auth no es un patrón establecido acá). El seam nuevo (`setActiveByDoctorId`) sí quedó cubierto a nivel de repositorio.
  - **Verificación**: `pnpm build` (compila, tipos y lint OK) y `tsc --noEmit` pasaron limpios. La suite Vitest completa (excluyendo `e2e/**` y el worktree stale en `.claude/worktrees/`) no pudo correrse en este entorno — falta `.env.local`/`MONGODB_URI`, bloqueado por permisos de sandbox — así que los tests nuevos y existentes contra MongoDB real (`User.test.ts`, `MongoUserRepository.test.ts`, `authenticateCredentials.test.ts`, y toda otra suite que dependa de `connectToDatabase`) no se ejecutaron en esta vuelta; se recomienda correrlos a mano con la base de test local antes de dar el ticket por verificado de punta a punta.

#### `[TASK-026]` Editar y desactivar Secretarias
* **Descripción**: `/admin/secretarias` (TASK-020) solo tiene alta + listado. Falta editar y desactivar, mismo patrón que ya existe para Doctor (TASK-006).
* **Criterios de Aceptación**:
  - [x] `updateSecretaria` y `setSecretariaActive` (usa `isActive` de TASK-025), protegidas con `requireAdminSession`.
  - [x] UI de edición/desactivación en `/admin/secretarias`, mismo patrón visual que `DoctorRow`.
* **Prioridad**: Media | **Esfuerzo**: Medio | **Dependencias**: TASK-025
* **Resultado**: Extendidos `IUserRepository`/`MongoUserRepository` con dos métodos de propósito general, keyed por el `_id` propio del `User` (a diferencia de `setActiveByDoctorId` de TASK-025, que resuelve a través de un Doctor vinculado — la Secretaria no tiene perfil intermedio, su `User` ES la entidad gestionada): `update(id, { name, email })` y `setActiveById(id, isActive)`. Nuevas Server Actions `updateSecretaria`/`setSecretariaActive` en `lib/actions/secretaria.actions.ts`, protegidas con `requireAdminSession` (mismo patrón defensivo que `createSecretariaAccess`). UI: `components/SecretariaRow.tsx` (mismo patrón visual que `DoctorRow.tsx` — modo vista con botones "Editar"/"Desactivar"/"Reactivar", y modo edición inline) y `components/forms/EditSecretariaForm.tsx` (mismo patrón que `EditDoctorForm.tsx`, pero sin foto ni disponibilidad — solo nombre y email), montados en `/admin/secretarias`. `SecretariaEditFormValidation = SecretariaFormValidation.omit({ password: true })` en `lib/validation.ts`, ya que la edición no cambia contraseña.
  - **Archivos creados**: `components/SecretariaRow.tsx`, `components/forms/EditSecretariaForm.tsx`
  - **Archivos modificados**: `lib/repositories/IUserRepository.ts`, `lib/db/repositories/MongoUserRepository.ts` (+test), `lib/actions/secretaria.actions.ts`, `lib/validation.ts` (`SecretariaEditFormValidation`), `app/admin/secretarias/page.tsx`
* **Observaciones**:
  - **Email único en la edición, manejado distinto que en el alta**: `MongoUserRepository.create()` hace fallback silencioso al usuario existente ante un email duplicado (TASK-020), pero eso sería incorrecto en una edición — devolver silenciosamente los datos de OTRO usuario sería confuso. En cambio, `update()` rechaza explícitamente con `Error("EMAIL_TAKEN")` ante el índice único de `email`; `updateSecretaria` traduce eso a `{ error: "EMAIL_TAKEN" as const }` (mismo patrón de resultado tipado que `createDoctorAccess`/`ALREADY_HAS_ACCESS` de TASK-008/021), y `EditSecretariaForm` lo muestra como mensaje de error sin cerrar el formulario.
  - **Nomenclatura del seam nuevo**: se usó `setActiveById` (no se reutilizó/renombró `setActiveByDoctorId`) para mantener ambos métodos explícitos sobre qué id reciben — evita que un llamador pase por error un `doctorId` a un método que espera el `_id` del `User`, o viceversa.
  - **Revocación de acceso, verificada con test dedicado**: nuevo test en `MongoUserRepository.test.ts` (`setActiveById`) confirma que tras `setActiveById(id, false)` el `User` queda con `isActive: false` en la base. No se agregó una combinación de test nueva en `authenticateCredentials.test.ts` — ese archivo ya cubre genéricamente el rechazo de login con `isActive === false` desde TASK-025, y esa cobertura no depende de por qué campo se llegó a `isActive: false`; duplicarla acá no habría agregado señal.
  - No se testeó `updateSecretaria`/`setSecretariaActive` como Server Actions unitariamente — mismo patrón confirmado en TASK-006/019/020/025 (mockear `getServerSession`/`requireAdminSession` no es un patrón establecido en este proyecto). El seam nuevo (`update`, `setActiveById`) quedó cubierto a nivel de repositorio, incluyendo el caso `EMAIL_TAKEN`.
  - **Verificación**: `tsc --noEmit` limpio. `eslint` sobre todos los archivos nuevos/modificados de este ticket, limpio. `pnpm build` compila y pasa el paso de tipos/lint de Next.js sin errores nuevos (mismos 2 warnings preexistentes y no relacionados, en `FileUploader.tsx` y `BillingForm.tsx`), pero falla al llegar a "Collecting page data" para `/api/auth/[...nextauth]` porque el sandbox no tiene `MONGODB_URI`/`.env.local` accesible — misma limitación de entorno confirmada en TASK-024/TASK-025, no causada por este cambio. La suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) tampoco pudo correr contra Mongo real por el mismo motivo: los 23 tests que no dependen de `connectToDatabase` pasaron igual que antes de este ticket, pero los tests nuevos de `MongoUserRepository.test.ts` (`update`, `setActiveById`, incluyendo el caso `EMAIL_TAKEN`) no se ejecutaron en esta vuelta — se recomienda correrlos a mano con la base de test local antes de dar el ticket por verificado de punta a punta.

#### `[TASK-027]` Gestión de Administradores desde UI
* **Descripción**: hoy no existe ningún flujo de alta de Administrador en la app — el único se crea vía `scripts/seed-admin.ts`. Se define que puede haber más de un Administrador, y que cualquier Administrador puede crear, editar y desactivar a otro desde la UI.
* **Criterios de Aceptación**:
  - [x] `createAdminAccess`, `updateAdmin` y `setAdminActive` (usa `isActive` de TASK-025), todas protegidas con `requireAdminSession`.
  - [x] Pantalla nueva (`/admin/admins`) con alta + listado + edición + desactivación, mismo patrón visual que `/admin/secretarias`.
  - [x] Salvaguarda server-side: no se puede desactivar al último Administrador activo del sistema.
* **Prioridad**: Media | **Esfuerzo**: Medio | **Dependencias**: TASK-025
* **Resultado**: Nueva Server Action file `lib/actions/adminUser.actions.ts` (mismo patrón que `secretaria.actions.ts` de TASK-020/026, con `role: "Administrador"`), reutilizando directamente los métodos genéricos de `IUserRepository`/`MongoUserRepository` agregados en TASK-026 (`update`, `setActiveById`) y `findByRole` heredado de TASK-020 — no se duplicó ni modificó nada en el repositorio. `createAdminAccess` sigue el patrón de `createSecretariaAccess` (`phone: "N/A"`, sin entidad vinculada). `updateAdmin` traduce `Error("EMAIL_TAKEN")` a `{ error: "EMAIL_TAKEN" as const }`, igual que `updateSecretaria`. UI: `components/AdminRow.tsx`/`components/forms/EditAdminForm.tsx`/`components/forms/CreateAdminForm.tsx` (mismo patrón visual y de edición inline que `SecretariaRow`/`EditSecretariaForm`/`CreateSecretariaForm` de TASK-026), montados en `app/admin/admins/page.tsx`. `AdminFormValidation`/`AdminEditFormValidation` agregadas en `lib/validation.ts`, mismo patrón que `SecretariaFormValidation`/`SecretariaEditFormValidation`. Nuevo link "Administradores" en el header de `/admin`, junto a "Secretarías"/TASK-020.
  - **La salvaguarda (única lógica genuinamente nueva del ticket)**: `isLastActiveAdmin(admins, targetId)`, función pura extraída a `lib/actions/adminSafeguard.ts` (sin imports de auth/DB). `setAdminActive` la invoca solo cuando `isActive` pasa a `false`: llama a `findByRole("Administrador")`, y si `isLastActiveAdmin` da `true` devuelve `{ error: "LAST_ADMIN" as const }` en lugar de llamar a `setActiveById` — el chequeo real vive en el server, no en un botón deshabilitado de la UI. `AdminRow.tsx` sí deshabilita el toggle mientras la request está en curso, pero el rechazo lo decide `setAdminActive`, y `AdminRow` muestra el error devuelto ("No se puede desactivar al último Administrador activo.") en vez de asumir éxito del lado del cliente.
  - **Archivos creados**: `lib/actions/adminUser.actions.ts`, `lib/actions/adminSafeguard.ts` (+test), `components/AdminRow.tsx`, `components/forms/CreateAdminForm.tsx`, `components/forms/EditAdminForm.tsx`, `app/admin/admins/page.tsx`
  - **Archivos modificados**: `lib/validation.ts` (`AdminFormValidation`, `AdminEditFormValidation`), `app/admin/page.tsx` (link "Administradores")
* **Observaciones**:
  - **Por qué la salvaguarda quedó en su propio módulo, no en `adminUser.actions.ts`**: dos motivos técnicos, no solo de estilo. Primero, Next.js exige que todo export de un archivo `"use server"` sea una Server Action `async` — una función pura y síncrona como `isLastActiveAdmin` no puede exportarse desde `adminUser.actions.ts` sin romper el build. Segundo, ese archivo importa en su tope `requireAdminSession` → `authOptions` → el cliente de Mongo, que exige `MONGODB_URI` ya en tiempo de import; con la salvaguarda colocada ahí, hasta un test puramente unitario de la función dispararía ese chequeo y fallaría en este sandbox (se confirmó el error exacto al intentarlo primero así, antes de extraerla). Moviéndola a `lib/actions/adminSafeguard.ts`, sin ningún import de auth/DB, se pudo testear en verde en este entorno.
  - El chequeo replica el algoritmo pedido en el ticket: filtra por `isActive !== false` (mismo criterio "`undefined` = activo" de TASK-025, no `=== true`), excluye el id del propio target del conteo, y solo bloquea cuando el conteo de "otros activos" da 0. No impide crear administradores ilimitados ni exige ningún mínimo más allá de 1, y no restringe nada de la edición (nombre/email) — solo la desactivación, tal como pide el ticket.
  - **Convención de testing seguida**: ni `createAdminAccess`/`updateAdmin`/`setAdminActive`/`getAdmins` (gateadas por `requireAdminSession`) se testearon unitariamente como Server Actions — mismo patrón confirmado en TASK-006/019/020/025/026 de no mockear `requireAdminSession`/`getServerSession` en este proyecto (el seam que ya existía, `update`/`setActiveById` de `MongoUserRepository`, quedó cubierto a nivel de repositorio desde TASK-026, y se reutiliza tal cual acá). Lo que sí es lógica nueva de este ticket, `isLastActiveAdmin`, se testeó directamente TDD (rojo→verde) con 5 casos en `lib/actions/adminSafeguard.test.ts`: última administradora activa (`true`), otra administradora activa presente (`false`), inactivas no cuentan para el total, `isActive === undefined` cuenta como activo, y el propio target no se cuenta a sí mismo como "otro activo".
  - No se agregó restricción de "no podés editarte/desactivarte a vos mismo" — pedido explícitamente así por el ticket (cualquier Administrador puede gestionar a otro, incluido potencialmente a sí mismo); la única restricción que aplica, igual sea auto-desactivación o de un tercero, es la salvaguarda de último-activo.
  - Se evaluó generalizar `SecretariaRow`/`EditSecretariaForm` junto con sus contrapartes de Administrador en un componente `StaffRow` compartido, pero se optó por copiar el patrón (como ya hizo TASK-026 tomando `DoctorRow` como base) — ambas entidades comparten forma visual pero no acciones/imports de destino, y una abstracción genérica no pagaba su complejidad para dos usos concretos.
  - No se tocó gestión de Doctor ni de Secretaria — fuera de alcance de este ticket. No se agregó cambio de contraseña/reset — fuera de alcance, igual que en TASK-026.
  - **Verificación**: `tsc --noEmit` limpio. `eslint` sobre todos los archivos nuevos/modificados de este ticket, limpio. `pnpm build` compila y pasa el paso de tipos/lint de Next.js sin errores nuevos (mismos 2 warnings preexistentes y no relacionados, en `FileUploader.tsx` y `BillingForm.tsx`), pero falla al llegar a "Collecting page data" para `/api/auth/[...nextauth]` porque el sandbox no tiene `MONGODB_URI`/`.env.local` accesible — misma limitación de entorno confirmada en TASK-024/025/026, no causada por este cambio. La suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) sí corrió en este entorno: los 5 tests nuevos de `lib/actions/adminSafeguard.test.ts` pasaron en verde (no dependen de Mongo), junto con el resto de los tests no dependientes de `connectToDatabase` que ya pasaban antes de este ticket (28 tests en total, 6 archivos). Los 23 archivos de test que sí dependen de Mongo real (incluyendo `MongoUserRepository.test.ts`, cuyos métodos reutiliza este ticket) no se ejecutaron por el mismo motivo de entorno — se recomienda correrlos a mano con la base de test local antes de dar el ticket por verificado de punta a punta.

### EPIC 9: Hallazgos de la primera ronda de pruebas locales (post-MVP)

#### `[TASK-030]` Botón de cierre de sesión (logout)
* **Descripción**: no existe ningún botón de logout en ninguna pantalla del sistema — confirmado que `signOut` de NextAuth nunca se invoca en todo el código. No es una regresión de EPIC 8: este gap existe desde que se implementó el login (TASK-004) y nunca se había probado el flujo completo como usuario real hasta ahora.
* **Criterios de Aceptación**:
  - [x] Botón/link de "Cerrar sesión" visible en `/admin`, `/doctor` y `/recepcion`, invocando `signOut()` de NextAuth.
  - [x] Redirige a `/login` tras cerrar sesión.
* **Prioridad**: Alta | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: `/admin`, `/doctor` y `/recepcion` son Server Components (hacen `await` de Server Actions al renderizar), así que `signOut()` — que depende del contexto de `next-auth/react`, cliente-only — no podía llamarse directo desde esas páginas sin convertirlas enteras a Client Components. Se aisló en un componente cliente chico, `LogoutButton`, montado en el `<header className="admin-header">` de cada una junto a los demás links de navegación. Usa `signOut({ callbackUrl: "/login" })`, así que redirige a `/login` tras cerrar sesión. `app/layout.tsx` ya envuelve toda la app en `SessionProvider` (`components/providers/AuthSessionProvider.tsx`, de `TASK-004`), así que no hizo falta ningún provider nuevo.
  - **Archivos creados**: `components/LogoutButton.tsx`
  - **Archivos modificados**: `app/admin/page.tsx`, `app/doctor/page.tsx`, `app/recepcion/page.tsx` (agregado `<LogoutButton />` al header de cada uno)
* **Observaciones**:
  - Se usó un `<button>` con `onClick={() => signOut(...)}`, no un `<Link>`, porque `signOut` dispara una llamada a NextAuth (invalida la sesión) antes de navegar — no es una navegación pura, así que un link no es semánticamente correcto acá.
  - Estilo `className="text-green-500"`, igual que los demás links de navegación de cada header (ej. "Secretarías"/"Nomenclador" en `/admin`), para no introducir un patrón visual nuevo.

#### `[TASK-031]` Navegación "volver" en pantallas internas
* **Descripción**: las pantallas de alta/gestión (`/admin/doctors`, `/admin/secretarias`, `/admin/admins`, `/admin/treatments`, `/admin/pacientes/nuevo`, `/recepcion/pacientes/nuevo`) no tienen forma de volver a la vista anterior sin usar el botón "atrás" del navegador.
* **Criterios de Aceptación**:
  - [x] Cada pantalla interna tiene un link/botón "Volver" hacia su pantalla padre (`/admin` o `/recepcion`).
* **Prioridad**: Media | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: se agregó un `<Link href="/admin">` (o `/recepcion` según corresponda) con texto "Volver" al `<header className="admin-header">` de las seis pantallas listadas en la descripción. No se encontró ningún patrón de "volver" preexistente en el código (`/doctor/patient/[id]` y `/recepcion/recibo/[appointmentId]` solo tienen el logo como link implícito al padre, sin texto) — se usó el mismo estilo `text-green-500` que ya usan los demás links de navegación de cada header, en vez de introducir un componente/patrón nuevo para un requerimiento tan chico.
* **Archivos modificados**: `app/admin/doctors/page.tsx`, `app/admin/secretarias/page.tsx`, `app/admin/admins/page.tsx`, `app/admin/treatments/page.tsx`, `app/admin/pacientes/nuevo/page.tsx`, `app/recepcion/pacientes/nuevo/page.tsx`
* **Observaciones**:
  - Es un `<Link>` directo a la ruta padre conocida, no un hack de `router.back()`/historial — todas estas pantallas solo tienen un padre posible (`/admin` o `/recepcion`), así que no hay ambigüedad que justifique depender del historial del navegador.
  - No se creó un `layout.tsx` compartido para `/admin/*` ni un componente `BackLink` reutilizable: son seis líneas casi idénticas pero de una sola palabra, y las páginas ya difieren en headers (algunas tienen más links, otras no) — el ticket pedía una navegación puntual, no una nueva capa de layout compartido.

#### `[TASK-032]` Ajustes de UX en el alta de paciente
* **Descripción**: tres ajustes sobre `CreatePatientForm` (TASK-024), detectados al probar el alta real de un paciente.
* **Criterios de Aceptación**:
  - [x] El selector de teléfono (`react-phone-number-input`) usa Argentina como país por defecto.
  - [x] El selector de fecha de nacimiento muestra formato `DD/MM/YYYY`.
  - [x] El **archivo escaneado** del documento de identificación pasa a opcional. El **tipo y número de documento** (DNI) siguen siendo obligatorios — es la clave de búsqueda que necesita TASK-033.
* **Prioridad**: Alta | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: Los tres ajustes se hicieron a nivel del componente compartido `CustomFormField.tsx`, no en `CreatePatientForm` puntualmente, porque es el único lugar donde `PHONE_INPUT`/`DATE_PICKER` renderizan de verdad: `PhoneInput` pasó de `defaultCountry="US"` a `"AR"`, y el `dateFormat` por defecto del `DATE_PICKER` pasó de `"MM/dd/yyyy"` a `"dd/MM/yyyy"` cuando el caller no pasa uno explícito. Se verificó primero que ningún otro formulario (`DoctorForm`, `CreateSecretariaForm`, `CreateAdminForm`) usa `PHONE_INPUT` — solo `CreatePatientForm` (teléfono del paciente y del contacto de emergencia) — así que el default global no cambia el comportamiento de ninguna otra pantalla. `AppointmentForm.tsx` sí usa `DATE_PICKER` (fecha/hora de turno), pero ya pasa su propio `dateFormat="MM/dd/yyyy  -  h:mm aa"` explícito, así que el nuevo default no lo afecta — el cambio de formato solo se ve en la fecha de nacimiento. Para el archivo opcional: `identificationDocument` en `CreatePatientFormValidation` pasó de `.refine((files) => files?.length === 1, ...)` a `.optional()` (mismo patrón ya usado por `DoctorEditFormValidation.photo`); `identificationType`/`identificationNumber` no se tocaron. Se confirmó leyendo el código (no se asumió) que `createPatient` (`lib/actions/patient.actions.ts`) ya toleraba la ausencia de archivo: solo sube algo si `FormData.get("blobFile")` y `get("fileName")` existen. El único ajuste real de código hecho falta era en el formulario: `CreatePatientForm.onSubmit` armaba el `Blob`/`FormData` accediendo directo a `values.identificationDocument[0]` sin chequear si había algún archivo, lo que hubiera tirado un `TypeError` en cuanto alguien enviara el formulario sin adjuntar nada — se cambió a construir el `FormData` vacío por defecto y solo agregarle `blobFile`/`fileName` si `values.identificationDocument?.[0]` existe. La etiqueta del campo también se actualizó a "(opcional)", siguiendo el mismo patrón visual que los demás campos opcionales del formulario.
  - **Archivos modificados**: `components/CustomFormField.tsx`, `lib/validation.ts`, `components/forms/CreatePatientForm.tsx`
* **Observaciones**:
  - No se tocó `DoctorFormValidation.photo` (sigue obligatoria) — eso es TASK-036, decisión de producto separada, fuera de alcance acá.

#### `[TASK-034]` Patrón de listado + Dialog para crear/editar (Doctores, Secretarias, Administradores, Nomenclador)
* **Descripción**: hoy `/admin/doctors`, `/admin/secretarias`, `/admin/admins` y `/admin/treatments` mostraban el formulario de alta y el listado en la misma pantalla, siempre visibles. Se pidió separar: la pantalla por defecto muestra solo el listado, con un botón "Crear" que abre el formulario en un `Dialog`; "Editar" en cada fila abre el mismo `Dialog` en modo edición.
* **Criterios de Aceptación**:
  - [x] Las 4 pantallas muestran únicamente el listado por defecto.
  - [x] Botón "Crear" abre un `Dialog` con el formulario de alta correspondiente.
  - [x] "Editar" en cada fila abre el mismo `Dialog` en modo edición, precargado.
* **Prioridad**: Media | **Esfuerzo**: Medio-Alto (tocó 4 pantallas) | **Dependencias**: Ninguna
* **Resultado**: reutilizado el `Dialog` de Radix ya existente (`components/ui/dialog.tsx`, mismo que `AdminNewAppointmentModal`) — sin dependencias nuevas. Trabajo dividido en dos mitades en paralelo, sin superposición de archivos entre ambas: **Doctores + Nomenclador** por un lado, **Secretarias + Administradores** por otro.
  - **Doctores/Nomenclador**: `app/admin/doctors/page.tsx` y `app/admin/treatments/page.tsx` perdieron la sección de alta inline; nuevos `CreateDoctorModal.tsx`/`CreateTreatmentModal.tsx` (botón "Crear" + `Dialog` con `DoctorForm`/`TreatmentForm`). `DoctorRow.tsx`/`TreatmentRow.tsx` perdieron su modo de edición inline (`mode === "edit"`) — "Editar" ahora abre un `Dialog` controlado (`isEditOpen`) con `EditDoctorForm`/`TreatmentForm` (edición) precargado. `DoctorForm.tsx` sumó un `onDone?: () => void` opcional (mismo patrón que ya tenía `TreatmentForm`) para poder cerrar el diálogo tras `router.refresh()`. "Crear acceso" (`CreateDoctorAccessForm`, TASK-008) y "Desactivar"/"Reactivar" quedaron exactamente como estaban — no son formularios de alta/edición, no correspondía moverlos a un diálogo.
  - **Secretarias/Administradores**: mismo tratamiento en `app/admin/secretarias/page.tsx`/`app/admin/admins/page.tsx`, nuevos `CreateSecretariaModal.tsx`/`CreateAdminModal.tsx`, `SecretariaRow.tsx`/`AdminRow.tsx` con `Dialog` controlado para "Editar". `CreateSecretariaForm.tsx`/`CreateAdminForm.tsx` sumaron un `setOpen?` opcional (mismo patrón que ya usa `AppointmentForm` dentro de `AdminNewAppointmentModal`) para cerrar el diálogo solo tras un submit exitoso.
  - **Archivos creados**: `components/CreateDoctorModal.tsx`, `components/CreateTreatmentModal.tsx`, `components/CreateSecretariaModal.tsx`, `components/CreateAdminModal.tsx`
  - **Archivos modificados**: `app/admin/doctors/page.tsx`, `app/admin/treatments/page.tsx`, `app/admin/secretarias/page.tsx`, `app/admin/admins/page.tsx`, `components/DoctorRow.tsx`, `components/TreatmentRow.tsx`, `components/SecretariaRow.tsx`, `components/AdminRow.tsx`, `components/forms/DoctorForm.tsx`, `components/forms/CreateSecretariaForm.tsx`, `components/forms/CreateAdminForm.tsx`
* **Observaciones**:
  - **Manejo de errores preservado sin cambios de lógica**: `EMAIL_TAKEN` (Secretaria/Admin, TASK-026/027) y `LAST_ADMIN` (salvaguarda del último Administrador, TASK-027) siguen resolviéndose exactamente igual — el diálogo de edición/alta solo se cierra cuando el callback `onDone`/`setOpen(false)` se invoca tras un submit realmente exitoso; en el camino de error, el formulario nunca lo llama, así que el mensaje queda visible dentro del `Dialog` en vez de perderse.
  - No se tocó ninguna Server Action, schema de validación, ni la lógica de la salvaguarda de último Administrador — cambio puramente presentacional (dónde vive el formulario), consistente con el patrón container-presentational que ya usa `AdminNewAppointmentModal` desde TASK-018/024.
  - `TreatmentForm.tsx` no se dividió en dos componentes (alta/edición) — sigue siendo un único componente con `treatment?` opcional, decisión ya tomada en TASK-010 y respetada acá.
  - No se agregaron tests — no hay convención de testear componentes de UI/layout en este proyecto (`DoctorRow`/`SecretariaRow`/`AdminRow`/`TreatmentRow` nunca tuvieron test).
  - **Verificación**: `pnpm build` corrido tres veces — una por cada mitad en paralelo (ambas limpias, sin errores nuevos, mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`) y una tercera vez con las dos mitades ya integradas en el mismo working tree, confirmando que no hay conflicto entre los archivos que tocó cada una (`git status` confirmó cero solapamiento). Build completo de punta a punta las tres veces, incluyendo `/api/auth/[...nextauth]` — no se pisó el límite de sandbox de `MONGODB_URI` en esta tanda.

#### `[TASK-035]` Listado y filtro de Pacientes
* **Descripción**: no existe ninguna pantalla para listar pacientes — la única forma de encontrar uno hoy es la búsqueda exacta de "Nuevo turno" (TASK-018/033). Se pide una pantalla de listado con filtros.
* **Criterios de Aceptación**:
  - [x] Pantalla de listado de pacientes (`/recepcion` y `/admin`) con filtros por nombre y DNI.
  - [x] Construida directamente con el patrón de TASK-034 (listado, sin Dialog en este caso porque no hay nada para crear/editar desde esta pantalla).
* **Prioridad**: Media | **Esfuerzo**: Medio | **Dependencias**: TASK-034 (para no construir la pantalla dos veces)
* **Resultado**: nueva pantalla de listado + filtro en `/recepcion/pacientes` (primaria, mismo criterio que TASK-024 sobre dónde vive el alta de pacientes) y `/admin/pacientes` (secundaria), ambas montando el mismo componente presentacional `PatientsList`. Sin Dialog de creación/edición — a diferencia de TASK-034, acá no hay ningún formulario que mostrar: el alta sigue viviendo exclusivamente en `/recepcion/pacientes/nuevo`/`/admin/pacientes/nuevo` (TASK-024), sin tocar.
  - **Query de filtro**: `IPatientRepository.findAll(filters?: { name?: string; identificationNumber?: string })` — un único método con ambos filtros opcionales y combinables (en vez de dos métodos separados), mismo nivel de simplicidad que `findActive`/`findAll` de `IDoctorRepository`. `MongoPatientRepository.findAll` arma un `$regex` case-insensitive por nombre (`$options: "i"`) y un `$regex` sin ancla por DNI (coincidencia parcial, no exacta — se prefirió sobre el matching exacto de `findByIdentificationNumber`/TASK-033 porque acá el caso de uso es "encontrar a alguien en una lista" para un empleado de mostrador que puede no tener el DNI completo a mano, mientras que TASK-033 identifica a un paciente puntual para agendarle un turno). Ambos filtros se combinan con `AND` implícito de Mongo (todas las claves del mismo objeto de query). Valores de filtro escapados con un helper `escapeRegExp` local (nuevo, sin precedente en el código) para que caracteres especiales de regex en el input del usuario se traten como texto literal y no rompan ni sean interpretados como patrón. Resultado ordenado por nombre (`sort({ name: 1 })`, mismo criterio que `findAll`/`findActive` de doctores). Test-first con Mongo real: rojo confirmado (`TypeError: repository.findAll is not a function`) antes de implementar, siguiendo el mismo patrón que `findByIdentificationNumber` (TASK-033) — casos cubiertos: sin filtros (todos, ordenados), filtro por nombre parcial case-insensitive, filtro por DNI parcial, ambos filtros combinados, sin resultados, y caracteres especiales de regex tratados como texto literal.
  - **Server Action**: `listPatients(filters?)` en `lib/actions/patient.actions.ts`, gateada con `requireSecretariaOrAdminSession()` — reutilizada tal cual de TASK-024, sin duplicar lógica de guard — porque esta pantalla, igual que el alta, la usan ambos roles.
  - **Filtrado server-side, no client-side**: cada submit del formulario de filtros vuelve a llamar a `listPatients` (Server Action invocada directo desde el client component, sin ruta HTTP intermedia) en vez de traer toda la tabla una vez y filtrarla en el browser. Se descartó filtrar client-side porque no hay ningún precedente de traer una tabla completa al cliente en este código (`DataTable`/`AppointmentsTable` no filtran; reciben ya los datos que muestran) y porque no escala si la base de pacientes de la clínica crece — el precedente más cercano de UX de búsqueda de pacientes ya existente, `AdminNewAppointmentModal` (TASK-033), también resuelve la búsqueda contra el servidor en cada submit, así que se siguió la misma forma. La carga inicial de la pantalla (`listPatients()` sin filtros) sí se resuelve server-side en el `page.tsx`, mismo patrón que `getAllDoctors()` en `app/admin/doctors/page.tsx` (TASK-034).
  - **Fila de listado**: `PatientRow.tsx` (nuevo) — muestra nombre, tipo+número de documento (o "Sin documento registrado" si no tiene), teléfono y email. A diferencia de `DoctorRow`/`TreatmentRow`/`SecretariaRow`/`AdminRow`, no tiene ningún botón de acción (ni "Editar" ni activar/desactivar) — no hay nada que editar desde esta pantalla, alcance explícitamente excluido.
  - **Nav**: link "Pacientes" agregado al header de `/admin` (`app/admin/page.tsx`, entre "Nuevo paciente" y "Doctores") y de `/recepcion` (`app/recepcion/page.tsx`, entre "Nuevo paciente" y el botón de logout), mismo patrón de links ya usado por TASK-010/020/027.
  - **Archivos creados**: `components/PatientRow.tsx`, `components/PatientsList.tsx`, `app/recepcion/pacientes/page.tsx`, `app/admin/pacientes/page.tsx`
  - **Archivos modificados**: `lib/repositories/IPatientRepository.ts`, `lib/db/repositories/MongoPatientRepository.ts` (+test), `lib/actions/patient.actions.ts`, `app/admin/page.tsx`, `app/recepcion/page.tsx`
* **Observaciones**:
  - No se agregó obra social como filtro — decisión explícita del usuario, fuera de alcance (la mayoría de los pacientes quedan en "Particular" en este MVP, bajo valor como filtro).
  - No se tocó `CreatePatientForm`/`createPatient`/las rutas `/recepcion/pacientes/nuevo`/`/admin/pacientes/nuevo` (TASK-024) — el alta de pacientes sigue exactamente igual.
  - **Verificación**: `tsc --noEmit` limpio. `eslint` sobre todos los archivos nuevos/modificados de este ticket, limpio. `pnpm build` compila y pasa el paso de tipos/lint de Next.js sin errores nuevos (mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`); `/admin/pacientes` y `/recepcion/pacientes` quedaron correctamente marcadas `ƒ` (dinámicas) junto con el resto de las pantallas que dependen de `headers()`/sesión (`/admin/secretarias`, `/admin/admins`, `/recepcion`, `/doctor`) — en este entorno `.env.local`/`MONGODB_URI` sí estuvo accesible, así que el build llegó de punta a punta sin el corte por falta de Mongo visto en otros tickets. La suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) corrió contra Mongo real: 182 tests en 29 archivos, todos en verde, incluyendo los 6 tests nuevos de `MongoPatientRepository.test.ts` (`describe("findAll")`) — rojo confirmado primero, luego verde tras implementar.

#### `[TASK-036]` Foto opcional en alta de Doctor, con avatar de reemplazo
* **Descripción**: `DoctorFormValidation` exige una foto para crear un doctor (`lib/validation.ts:128`), pero el formulario no aclara para qué se usa ni dónde queda (se guarda en GridFS, mismo bucket que los documentos de pacientes, servida vía `/api/files/[fileId]`, y se muestra en el listado de doctores). Al probarlo en vivo, generó confusión. **Decisión de Producto**: se vuelve opcional; cuando no haya foto o falle al renderizar, se muestra un avatar de reemplazo en vez de una imagen rota.
* **Criterios de Aceptación**:
  - [x] `DoctorFormValidation.photo` pasa a opcional (mismo criterio que ya tiene `DoctorEditFormValidation`).
  - [x] En todo lugar donde se muestra la foto del doctor (listado de `/admin/doctors`, selector de doctor en el alta de turno, historial de turnos), si no hay foto o la imagen falla al cargar, se muestra un avatar de reemplazo en vez de un ícono roto.
* **Prioridad**: Baja | **Esfuerzo**: Bajo-Medio | **Dependencias**: Ninguna
* **Resultado**: `DoctorFormValidation.photo` pasó de `.refine((files) => files?.length === 1, ...)` a `.optional()`, igualando a `DoctorEditFormValidation.photo`. A diferencia del caso equivalente de `CreatePatientForm`/TASK-032, acá el gap era más profundo que el formulario: `createDoctor` (`lib/actions/doctor.actions.ts`) llamaba a `uploadPhoto` de forma incondicional, y esa función tiraba `throw new Error("A photo is required")` si no había `blobFile`/`fileName` en el `FormData` — atrapado en silencio por el `try/catch` de la Server Action (solo loguea y devuelve `undefined`), así que crear un doctor sin foto simplemente no hacía nada, sin ningún error visible. Además, `image` era un campo requerido en tres capas más: el schema de Mongoose (`Doctor.ts`, `required: true`), `DoctorRecord.image`/`CreateDoctorInput` (`IDoctorRepository.ts`) y por herencia todos los tipos de `doctor: { image: string }` repetidos en cada componente que recibe una lista de doctores — con esas capas intactas, la creación sin foto hubiera seguido fallando (esta vez a nivel de validación de Mongoose) aun después de arreglar el formulario y la Zod schema. Se corrigieron las tres capas: `uploadPhoto` ahora devuelve `string | undefined` en vez de tirar cuando no hay archivo (mismo patrón inline que ya usaba `createPatient` para `identificationDocument`), `Doctor.ts`/`IDoctorRepository.ts` pasaron `image` a opcional, y cada tipo `{ name: string; image: string }` repetido en los pickers de doctor (`AppointmentForm`, `CreatePatientForm`, `AppointmentModal`, `AdminNewAppointmentModal`, `AppointmentsTable`/`columns.tsx`, `DoctorRow`, `EditDoctorForm`, `app/admin/doctors/page.tsx`) pasó a `image?: string`. `DoctorForm.tsx.onSubmit` tenía exactamente el mismo bug de patrón que TASK-032 detectó en `CreatePatientForm` (acceso directo a `values.photo[0]` sin chequear si había archivo, lo que hubiera tirado un `TypeError` al enviar sin foto) — se corrigió construyendo el `FormData` vacío por defecto y agregándole `blobFile`/`fileName` solo si `values.photo?.[0]` existe; la etiqueta pasó a "Foto (opcional)". Para el avatar de reemplazo se vendorizó `components/ui/avatar.tsx` (wrapper de `@radix-ui/react-avatar`, mismo patrón que `dialog.tsx`/`checkbox.tsx` ya usan para otros primitivos de Radix) y un componente de conveniencia `components/DoctorAvatar.tsx` que arma `Avatar`+`AvatarImage`+`AvatarFallback`: solo renderiza `AvatarImage` si hay `image`, y el `AvatarFallback` (iniciales del nombre del doctor, descartando prefijos como "Dr."/"Dra." antes de calcularlas) cubre tanto la ausencia de foto como la falla de carga — Radix detecta el error de carga de forma nativa (`onLoadingStatusChange`), sin ningún `onError` a mano. Los cuatro lugares donde efectivamente se renderiza la foto de un doctor (no solo los tres mencionados en los criterios de aceptación) se migraron de `next/image`/`Image` a `DoctorAvatar`: `components/DoctorRow.tsx` (listado de `/admin/doctors`), `components/table/columns.tsx` (columna "Doctor" de `AppointmentsTable`, historial de turnos), `components/forms/AppointmentForm.tsx` (selector de doctor en el alta de turno) y, encontrado durante el barrido con `rg`, `components/forms/CreatePatientForm.tsx` (selector "Médico de cabecera" del alta de paciente, con el mismo `<Image src={doctor.image}>` sin guardas) — no estaba en la lista original de "al menos" del ticket pero es el mismo patrón de bug, así que se corrigió igual. De paso, `columns.tsx` tenía un bug preexistente de non-null assertion (`doctor?.image!`) que hubiera roto si no se encontraba el doctor por nombre; ahora usa `doctor?.name ?? appointment.primaryPhysician` como texto/iniciales de respaldo.
  - **Archivos creados**: `components/ui/avatar.tsx`, `components/DoctorAvatar.tsx`
  - **Archivos modificados**: `lib/validation.ts`, `lib/actions/doctor.actions.ts`, `lib/db/models/Doctor.ts` (+test), `lib/repositories/IDoctorRepository.ts`, `lib/db/repositories/MongoDoctorRepository.ts` (+test), `components/forms/DoctorForm.tsx`, `components/forms/EditDoctorForm.tsx`, `components/forms/AppointmentForm.tsx`, `components/forms/CreatePatientForm.tsx`, `components/DoctorRow.tsx`, `components/table/columns.tsx`, `components/table/AppointmentsTable.tsx`, `components/AppointmentModal.tsx`, `components/AdminNewAppointmentModal.tsx`, `app/admin/doctors/page.tsx`, `package.json`/`pnpm-lock.yaml` (`@radix-ui/react-avatar`)
* **Observaciones**:
  - **Dependencia agregada**: `@radix-ui/react-avatar@1.2.6` — versión chequeada contra el resto de los primitivos de Radix ya instalados antes de instalar (mismo cuidado que TASK-004 con `mongodb`/`@next-auth/mongodb-adapter`): el `package.json` declara rangos viejos (`^1.0.5`, `^2.0.0`) para `@radix-ui/react-dialog`/`@radix-ui/react-select`, pero el lockfile ya los tenía resueltos en versiones bastante más nuevas (`1.1.20`, `2.3.4` respectivamente) conviviendo sin problema con React 19.2.8, así que no era una integración nueva sin precedente. `@radix-ui/react-avatar@1.2.6` declara soporte explícito para `react`/`react-dom` `^19.0 || ^19.0.0-rc` en sus `peerDependencies`, y `pnpm add` no arrojó ningún warning de peer dependency nuevo relacionado a este paquete (los unmet peers que sí aparecieron — `react-hook-form`, `lucide-react`, `next-themes`, `react-datepicker`, `@typescript-eslint/eslint-plugin` — son preexistentes, no relacionados a este cambio).
  - No se tocó cómo se almacena la foto (GridFS, `/api/files/[fileId]`) — fuera de alcance explícito de este ticket, solo cambió su opcionalidad y cómo se muestra.
  - No se tocó `TASK-037` (sidebar/navbar) — separada, sin empezar.
  - No se agregaron tests para `DoctorAvatar`/`Avatar` en sí (sin convención de testear componentes de UI/chrome en este proyecto — ni `DoctorRow` ni ningún otro tuvo test nunca) ni para el cambio de `DoctorForm.onSubmit` (mismo criterio que TASK-032 con `CreatePatientForm.onSubmit`: es un cambio de código de submit de formulario, no de lógica de dominio). Sí se agregaron tests en la capa que sí tiene convención de test-first con Mongo real: `Doctor.test.ts` (el test "requires name, image, specialty and licenseNumber" se ajustó a "requires name, specialty and licenseNumber" quitando la aserción de `image`, y se sumó "does not require an image (TASK-036...)") y `MongoDoctorRepository.test.ts` (nuevo caso "creates a doctor without an image").
  - **Verificación**: `tsc --noEmit` limpio. `next lint`/`pnpm build` sin errores nuevos (mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`; un tercer warning de orden de clases de Tailwind apareció en `DoctorAvatar.tsx` durante el desarrollo y se corrigió con `eslint --fix` antes de terminar). `pnpm exec vitest run --exclude "e2e/**" --exclude "**/.claude/**"` corrió contra Mongo real (`.env.local`/`MONGODB_URI` accesible en este entorno): 184 tests en 29 archivos, todos en verde, incluyendo los 2 tests nuevos de este ticket.

#### `[TASK-037]` Reemplazar navbar por sidebar/offcanvas
* **Descripción**: la navegación actual es un header de texto plano, repetido y ligeramente distinto en cada pantalla (`/admin`, `/doctor`, `/recepcion` y las internas). Se pide reemplazarlo por un sidebar u offcanvas consistente entre roles, aprovechando los primitivos de Radix/shadcn ya usados en el proyecto (`components/ui/*`, mismo `Dialog` que ya usa TASK-034).
* **Criterios de Aceptación**:
  - [x] Definir diseño: sidebar fijo vs. offcanvas (drawer), y qué opciones de menú corresponden a cada rol (Administrador/Doctor/Secretaria).
  - [x] Reemplaza los headers actuales de `/admin`, `/doctor`, `/recepcion` y las pantallas internas por el nuevo componente de navegación.
  - [x] El logout (TASK-030) y el "Volver" (TASK-031) quedan integrados naturalmente en la nueva navegación, no como links sueltos en un header que va a desaparecer.
* **Prioridad**: Baja | **Esfuerzo**: Medio-Alto | **Dependencias**: TASK-030, TASK-031, TASK-034 (para no rediseñar la navegación dos veces)
* **Resultado**: diseño ya acordado con el usuario de antemano (offcanvas/drawer, no sidebar fijo persistente): se reutilizó el `Dialog` de Radix ya existente (`components/ui/dialog.tsx`, mismo que TASK-034), sin agregar ninguna dependencia nueva — el "Sheet" de shadcn es, en esencia, el mismo `Dialog` reposicionado. En vez de forzar ese reposicionamiento vía `className` sobre el `<DialogContent>` ya exportado (arriesgado: sus clases de centrado/animación están hardcodeadas en el componente, y varias son utilidades de `tailwindcss-animate` que `tailwind-merge` no reconoce como grupo conflictivo, así que no se puede confiar en que una clase nueva pise a la vieja), el nuevo `components/AppSidebarNav.tsx` compone directamente `Dialog`/`DialogPortal`/`DialogOverlay`/`DialogClose`/`DialogTrigger`/`DialogTitle` (reexportados de `dialog.tsx`) con un `DialogPrimitive.Content` propio (`@radix-ui/react-dialog`, ya una dependencia existente) estilado como panel deslizante fijo a la izquierda (`inset-y-0 left-0`, ancho fijo, `slide-in-from-left-full`/`slide-out-to-left-full`). Esto deja sin tocar el `<DialogContent>` centrado que ya usan `AdminNewAppointmentModal`/`CreateDoctorModal`/`CreateTreatmentModal`/`CreateSecretariaModal`/`CreateAdminModal` y los diálogos de edición (TASK-034) — cero riesgo de regresión sobre esos seis modales existentes.
  - **Menú por rol**: Administrador (`Dashboard`, `Nuevo paciente`, `Pacientes`, `Doctores`, `Secretarías`, `Administradores`, `Nomenclador`, `Cerrar sesión`), Doctor (`Mi agenda`, `Cerrar sesión`), Secretaria (`Recepción`, `Nuevo paciente`, `Pacientes`, `Cerrar sesión`) — tomados 1:1 de los links que ya tenía cada header, sin agregar ni quitar ninguno.
  - **Layouts nuevos**: `app/admin/layout.tsx`, `app/doctor/layout.tsx`, `app/recepcion/layout.tsx`, cada uno renderiza una barra superior fija (logo enlazando a la raíz del rol + `AppSidebarNav` con el menú de ese rol) y envuelve `{children}` — así la navegación queda consistente entre todas las pantallas de cada rol en un solo lugar, en vez de repetida por página.
  - **Headers eliminados**: se quitó el `<header className="admin-header">` completo (links de navegación, "Volver" y `<LogoutButton />`) de las 9 pantallas: `app/admin/page.tsx`, `app/doctor/page.tsx`, `app/recepcion/page.tsx`, `app/admin/doctors/page.tsx`, `app/admin/secretarias/page.tsx`, `app/admin/admins/page.tsx`, `app/admin/treatments/page.tsx`, `app/admin/pacientes/page.tsx`, `app/admin/pacientes/nuevo/page.tsx`, `app/recepcion/pacientes/page.tsx`, `app/recepcion/pacientes/nuevo/page.tsx`. Los links "Volver" (TASK-031) se eliminaron por completo, no se migraron — la navegación persistente los reemplaza, según el propio razonamiento del usuario al aprobar este diseño. El `<div className="mx-auto flex max-w-{4xl|7xl} flex-col space-y-14">` de cada página se dejó intacto (mismo ancho que ya tenía cada una — hay una inconsistencia preexistente, `/admin/pacientes/nuevo` usa `max-w-4xl` mientras el resto de `/admin/*` usa `max-w-7xl`, no introducida por este ticket), solo se le quitó el `<header>` de adentro; el contenido propio de cada pantalla (`<h1>`, botones "Crear Doctor"/"Nuevo turno", listados, formularios) no se tocó.
  - **Archivos creados**: `components/AppSidebarNav.tsx`, `app/admin/layout.tsx`, `app/doctor/layout.tsx`, `app/recepcion/layout.tsx`
  - **Archivos modificados**: `app/admin/page.tsx`, `app/doctor/page.tsx`, `app/recepcion/page.tsx`, `app/admin/doctors/page.tsx`, `app/admin/secretarias/page.tsx`, `app/admin/admins/page.tsx`, `app/admin/treatments/page.tsx`, `app/admin/pacientes/page.tsx`, `app/admin/pacientes/nuevo/page.tsx`, `app/recepcion/pacientes/page.tsx`, `app/recepcion/pacientes/nuevo/page.tsx`
* **Observaciones**:
  - `LogoutButton` (TASK-030) se reutilizó tal cual dentro del drawer, sin duplicar su lógica de `signOut`.
  - Cada link de navegación dentro del drawer está envuelto en `DialogClose asChild`, así que el drawer se cierra solo al navegar, sin manejar estado manual para eso.
  - No se agregó sidebar fijo persistente, breadcrumbs, ni estado de colapso/expansión — fuera de alcance, según lo ya acordado con el usuario.
  - No se agregaron tests para `AppSidebarNav` ni los `layout.tsx` nuevos — sin convención de testear componentes de UI/chrome de navegación en este proyecto (mismo criterio que TASK-034/035/036 con `DoctorRow`/`PatientRow`/`DoctorAvatar`).
  - **Verificación**: `tsc --noEmit` limpio y `eslint` sobre todos los archivos nuevos/modificados de este ticket, limpio. `pnpm build` compiló y renderizó las 19 rutas de punta a punta sin errores nuevos (mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`; `.env.local`/`MONGODB_URI` estuvo accesible en este entorno). `pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`: 184 tests en 29 archivos, todos en verde (sin cambios respecto a TASK-036, esperado ya que este ticket no toca lógica de dominio). Se hizo además una lectura manual del diff de las 11 pantallas modificadas para confirmar que solo se quitó el header/nav — ningún encabezado ni botón de acción propio de cada pantalla se perdió en el proceso.

#### `[TASK-033]` Buscar paciente por DNI en "Nuevo turno"
* **Descripción**: `AdminNewAppointmentModal`/`findPatientByContact` (TASK-018) busca hoy por email o teléfono exacto. El flujo real de mostrador identifica pacientes por DNI, no por esos datos.
* **Criterios de Aceptación**:
  - [x] La búsqueda de "Nuevo turno" acepta el número de documento de identificación como criterio, en **reemplazo** de email/teléfono (palabras explícitas del usuario: "no necesito agregar telefono o mail, debería poder agregar DNI").
* **Prioridad**: Alta | **Esfuerzo**: Bajo-Medio | **Dependencias**: TASK-032
* **Resultado**: `findByEmailOrPhone` en `IPatientRepository`/`MongoPatientRepository` fue **reemplazado** por `findByIdentificationNumber(identificationNumber)` (match exacto sobre `Patient.identificationNumber`, mismo nivel de simplicidad que el método que reemplaza — sin matching difuso/parcial), agregado con TDD (rojo primero: `MongoPatientRepository.test.ts` falló con `TypeError: repository.findByIdentificationNumber is not a function` contra Mongo real antes de implementarlo). La Server Action `findPatientByContact` se renombró a `findPatientByIdentificationNumber` (`lib/actions/patient.actions.ts`), mismo gateo con `requireAdminSession`. `AdminNewAppointmentModal.tsx` actualizado: el input pasó de placeholder "Email o teléfono del paciente" a "DNI del paciente", la descripción del modal a "Buscá al paciente por su DNI para agendarle un turno", el mensaje de error a "No se encontró ningún paciente con ese DNI", y la búsqueda llama a `findPatientByIdentificationNumber`.
  - **Archivos modificados**: `lib/repositories/IPatientRepository.ts`, `lib/db/repositories/MongoPatientRepository.ts` (+test), `lib/actions/patient.actions.ts`, `components/AdminNewAppointmentModal.tsx`
* **Observaciones**:
  - **`findByEmailOrPhone` eliminado, no dejado como código muerto**: se verificó con `rg` que, tras actualizar `AdminNewAppointmentModal`, no quedaba ningún consumidor de `findByEmailOrPhone`/`findPatientByContact` en el código (solo referencias históricas en este mismo archivo, en el `Resultado` de TASK-018). Se eliminó el método de la interfaz, de la implementación y su `describe` de test — mismo criterio de "no dejar código muerto" ya aplicado en TASK-023/024 (`findByIdentificationNumberWithPassword`, `createUser`).
  - No se agregó DNI como criterio "además de" email/teléfono (una tercera opción alternativa) — se siguió la palabra explícita del usuario de **reemplazo**, no de adición, como pedía el criterio de aceptación original (marcado "a definir en diseño").
  - **Verificación real contra MongoDB** (no solo mockeada): en este entorno `.env.local`/`MONGODB_URI` estuvo accesible, así que el test nuevo de `findByIdentificationNumber` corrió contra una base real (`healthcare-test`) — rojo confirmado primero, luego verde tras implementar.

### EPIC 10: Segunda ronda de pruebas locales — turnos y pacientes

#### `[TASK-038]` Formato de fecha DD/MM/YYYY en el modal de turno
* **Descripción**: `AppointmentForm.tsx` pasa un `dateFormat="MM/dd/yyyy  -  h:mm aa"` explícito a su `DATE_PICKER` (quedó afuera cuando TASK-032 cambió el default del componente compartido a `dd/MM/yyyy`, porque este campo ya tenía su propio formato explícito). Se pide `DD/MM/YYYY` acá también.
* **Criterios de Aceptación**:
  - [x] El selector de "Fecha estimada del turno" en `AppointmentForm`/`AdminNewAppointmentModal` muestra `DD/MM/YYYY`.
* **Prioridad**: Media | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: cambio de una línea en `components/forms/AppointmentForm.tsx` — el `dateFormat` explícito del `DATE_PICKER` de "Fecha estimada del turno" pasó de `"MM/dd/yyyy  -  h:mm aa"` a `"dd/MM/yyyy  -  h:mm aa"` (solo se tocó la porción de fecha; la porción de hora `h:mm aa` quedó intacta). No se tocó el default de `CustomFormField.tsx` (ya corregido en TASK-032) ni ningún otro campo `DATE_PICKER`.
  - **Archivos modificados**: `components/forms/AppointmentForm.tsx`
* **Observaciones**:
  - No se tocó TASK-039 (búsqueda/alta de paciente desde el listado) — separada, en curso en paralelo.
  - **Verificación**: `tsc --noEmit` limpio. `pnpm build` compiló y renderizó las 19 rutas de punta a punta sin errores nuevos (mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`); `.env.local`/`MONGODB_URI` estuvo accesible en este entorno. Un primer intento de `pnpm build` falló por una condición de carrera sobre `.next/` (otro agente corriendo TASK-039 en paralelo sobre el mismo working directory modificaba `app/admin/pacientes/page.tsx`/`components/PatientsList.tsx` a la vez, y luego una segunda corrida falló con `ENOENT` al renombrar `.next/export/500.html`, típico de dos builds concurrentes pisando el mismo `.next/`) — no relacionado a este cambio; una tercera corrida, sin nadie más buildeando a la vez, compiló limpio.

#### `[TASK-039]` Buscar y, si no existe, crear paciente desde el listado
* **Descripción**: hoy `/admin/pacientes` y `/recepcion/pacientes` (TASK-035) solo filtran sobre pacientes existentes — para crear uno nuevo hay que ir a "Nuevo paciente" por separado. Se pide integrar ambas acciones: buscar en el listado y, si no aparece, poder crearlo ahí mismo (mismo patrón de búsqueda-y-alta que ya usa "Nuevo turno"/`AdminNewAppointmentModal`, TASK-018/024). Aplica para Administrador y Secretaria por igual.
* **Criterios de Aceptación**:
  - [x] Si el filtro de nombre/DNI no encuentra resultados, se ofrece crear un paciente nuevo directamente desde esa pantalla (Dialog con `CreatePatientForm`, patrón TASK-034), sin navegar a `/admin/pacientes/nuevo` o `/recepcion/pacientes/nuevo` por separado.
  - [x] Disponible tanto en `/admin/pacientes` como en `/recepcion/pacientes`.
* **Prioridad**: Media | **Esfuerzo**: Bajo-Medio | **Dependencias**: TASK-035
* **Resultado**: nuevo `CreatePatientModal.tsx` (mismo patrón `Dialog` que `CreateDoctorModal`/`CreateSecretariaModal`, TASK-034) que envuelve `CreatePatientForm` dentro de un `Dialog`, con `DialogContent` más ancho (`sm:max-w-2xl`) y scrolleable (`max-h-[90vh] overflow-y-auto`) porque el formulario de paciente tiene bastantes más campos que los de Doctor/Secretaria/Admin. `PatientsList.tsx` ahora recibe `doctors`/`insuranceProviders` (mismos datos que ya cargaban las páginas `/nuevo`) y, cuando una búsqueda con nombre y/o DNI no encontrados devuelve cero resultados, muestra el mensaje de "no se encontraron pacientes que coincidan" junto al botón "Crear paciente" que abre el Dialog — pre-cargado con lo que el usuario ya había tipeado en los filtros de nombre/DNI. La carga inicial sin filtros (lista completa) y un "Limpiar" nunca disparan la oferta de creación: se agregó un estado `hasActiveFilters` que solo queda en `true` cuando la última búsqueda ejecutada tenía nombre y/o DNI no vacíos, distinto de simplemente "la lista está vacía" — así se respeta la intención del ticket (buscar-y-no-encontrar, no "lista vacía en general") sin duplicar el botón "Nuevo paciente" ya existente como nav item separado.
  - **`CreatePatientForm` extendido, no reescrito**: se le agregaron tres props opcionales (`setOpen?`, `defaultName?`, `defaultIdentificationNumber?`), mismo criterio `setOpen?` que ya usa `CreateSecretariaForm`/`CreateAdminForm` desde TASK-034. `defaultName`/`defaultIdentificationNumber` solo cambian los `defaultValues` de `name`/`identificationNumber` en el `useForm`; las dos páginas standalone `/admin/pacientes/nuevo` y `/recepcion/pacientes/nuevo` (TASK-024) no pasan ninguna de las tres props nuevas, así que su comportamiento no cambió. El `onSubmit` ahora llama a `router.refresh()` y `setOpen?.(false)` tras una creación exitosa (antes solo mostraba el mensaje de éxito y reseteaba el formulario in-place) — mismo patrón "cerrar + refrescar" que TASK-024/034 establecieron para los demás formularios en `Dialog`; en las páginas standalone esto es inocuo (`setOpen` es `undefined`, y `router.refresh()` solo revalida datos de una página sin listado que mostrar).
  - **Archivos creados**: `components/CreatePatientModal.tsx`
  - **Archivos modificados**: `components/forms/CreatePatientForm.tsx`, `components/PatientsList.tsx`, `app/admin/pacientes/page.tsx`, `app/recepcion/pacientes/page.tsx`
* **Observaciones**:
  - No se tocó `PatientRow.tsx` ni se agregó edición de pacientes desde este listado — fuera de alcance, igual que TASK-035.
  - No se tocó TASK-038 (formato de fecha) ni `AdminNewAppointmentModal`/`AppointmentForm` (rediseño de calendario aparte, en discusión con el usuario) — ambos fuera de alcance de este ticket.
  - Sin tests nuevos — sin convención de testear este tipo de chrome de UI en el proyecto (mismo criterio que TASK-034/035/037).
  - **Verificación**: `tsc --noEmit` limpio. `pnpm build` compiló y renderizó las 19 rutas sin errores nuevos (mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`); `.env.local`/`MONGODB_URI` estuvo accesible en este entorno, así que el build llegó de punta a punta sin el corte por falta de Mongo visto en otros tickets. No se corrió la suite Vitest completa porque este ticket no tocó ninguna Server Action, repositorio ni schema de validación (solo componentes de UI) — mismo criterio de alcance que TASK-034/037.

### EPIC 11: Duración de prestaciones y rediseño de "Nuevo turno" con calendario

Rediseño completo del flujo de asignación de turnos, a partir de hallazgos de la segunda ronda de pruebas locales: hoy no hay ninguna validación server-side de que un turno caiga dentro del horario configurado del doctor (`getAvailableSlots`/`includeTimes` solo sugiere visualmente, se puede saltear escribiendo la fecha a mano), y la duración de turno se asume fija en 30 minutos sin relación a la prestación real (limitación ya anotada en su momento por TASK-007). Se decidió: sin motor de reprogramación automática en cascada (el secretario reagenda a mano si hace falta); bloqueo duro ante superposición real; advertencia con confirmación si cae fuera del horario del doctor. Tickets secuenciales, cada uno depende del anterior — epic completo con TASK-043.

#### `[TASK-040]` Duración estimada por prestación en el Nomenclador
* **Descripción**: `Treatment` no tiene ningún campo de duración — TASK-041/042/043 la necesitan para calcular el fin real de un turno. Se agrega `estimatedDurationMinutes`, obligatoria para prestaciones nuevas.
* **Criterios de Aceptación**:
  - [x] `Treatment`/`ITreatmentRepository` incorporan `estimatedDurationMinutes: number`.
  - [x] `TreatmentForm.tsx` pide la duración estimada (en minutos) al crear/editar una prestación.
  - [x] Prestaciones ya existentes en la base (sin este campo) migran con un default de 30 minutos — mismo valor que ya se asume hoy — hasta que alguien lo actualice manualmente.
* **Prioridad**: Alta | **Esfuerzo**: Bajo-Medio | **Dependencias**: Ninguna
* **Resultado**: `Treatment`/`ITreatment` (`lib/db/models/Treatment.ts`) incorpora `estimatedDurationMinutes: number`, `required: true` con `default: DEFAULT_TREATMENT_DURATION_MINUTES` (30, nueva constante en `constants/index.ts`, mismo criterio que `DEFAULT_INSURANCE_PROVIDER`). Igual que ya se documentó para `User.isActive` en TASK-025, ese `default` de schema solo aplica a documentos creados/hidratados como nuevos — **no** rellena retroactivamente prestaciones ya persistidas antes de esta migración, que van a leer `estimatedDurationMinutes: undefined` a nivel de Mongoose crudo. A diferencia de `isActive` (donde `undefined` tiene un significado propio, "activo"), acá TASK-041 necesita siempre un número real, así que se agregó además un fallback explícito en `MongoTreatmentRepository.toTreatmentRecord` (`doc.estimatedDurationMinutes ?? DEFAULT_TREATMENT_DURATION_MINUTES`) — todo lo que pasa por el repositorio (crear, listar, actualizar) siempre devuelve un número usable, sea la prestación vieja o nueva. `ITreatmentRepository`/`MongoTreatmentRepository` exponen el campo en `TreatmentRecord` (y por lo tanto en `CreateTreatmentInput`/`UpdateTreatmentInput`, que se derivan de él, quedando obligatorio para altas nuevas). `TreatmentFormValidation` (`lib/validation.ts`) agrega `estimatedDurationMinutes` como entero positivo, mínimo 5 minutos. `TreatmentForm.tsx` agrega el campo "Duración estimada (minutos)" (mismo patrón `CustomFormField` numérico que "Precio"), y `TreatmentRow.tsx` muestra la duración junto al precio en el listado (`$5.000 · 30 min · descripción`). `lib/seedData/baseTreatments.ts` (consumido por `scripts/seed-nomenclador.ts` y `e2e/global-setup.ts`) actualizado con duraciones razonables por prestación (Consulta 30, Obturación de Resina 45, Limpieza Dental 40) para seguir compilando contra el nuevo campo obligatorio.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**: `lib/db/models/Treatment.ts` (+test), `lib/repositories/ITreatmentRepository.ts`, `lib/db/repositories/MongoTreatmentRepository.ts` (+test), `lib/validation.ts`, `components/forms/TreatmentForm.tsx`, `components/TreatmentRow.tsx`, `app/admin/treatments/page.tsx`, `lib/seedData/baseTreatments.ts`, `constants/index.ts`
* **Observaciones**:
  - **Migración verificada de punta a punta con un test real, no solo confiando en el schema**: `MongoTreatmentRepository.test.ts` inserta una "prestación previa a la migración" directo por el driver nativo (`Treatment.collection.insertOne`, sin pasar por el modelo ni sus defaults) y confirma que `repository.findAll()` la devuelve con `estimatedDurationMinutes: 30`, no `undefined`. `Treatment.test.ts` agrega el test complementario a nivel de schema (mismo patrón que el de `User.isActive` de TASK-025) que confirma que el documento crudo insertado así **sí** queda con el campo `undefined` — documentando explícitamente que el default de Mongoose por sí solo no alcanza, y que la garantía real vive en el repositorio.
  - No se tocó `Appointment`, `existsOverlapping` ni el flujo de agendado — eso es TASK-041, todavía no iniciado. Tampoco el rediseño de calendario/acordeón de TASK-042/043.
  - No se cambió el comportamiento de `Treatment.price` ni de ningún otro campo existente.
  - **Verificación**: `tsc --noEmit`/`pnpm build` limpios, compilando y renderizando las 19 rutas sin errores nuevos (mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`); `.env.local`/`MONGODB_URI` estuvo accesible en este entorno. Suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) verde: 189/189, incluyendo los tests nuevos de este ticket corridos contra MongoDB real.

#### `[TASK-041]` Turno con prestación estimada y bloqueo duro por superposición real
* **Descripción**: al agendar un turno hay que elegir qué prestación se va a realizar, para poder calcular su duración real. `Appointment` no tiene hoy ningún campo de prestación — eso solo se carga después, cuando el doctor atiende (facturación, TASK-012/017). La prestación estimada al agendar es un concepto distinto e independiente de la/las prestaciones que se terminen facturando.
* **Criterios de Aceptación**:
  - [x] `Appointment` incorpora `treatmentId` + `durationMinutes`, **fotografiados** al crear el turno (mismo patrón que `ClinicalNote.treatments`/`Payment` — un cambio futuro en la duración/precio de la prestación no debe alterar turnos ya agendados).
  - [x] El alta de turno pide seleccionar la prestación estimada (nomenclador activo).
  - [x] `existsOverlapping` (TASK-007) deja de asumir 30 minutos fijos — usa `durationMinutes` real del turno para calcular su fin y detectar superposición.
  - [x] Superposición real con otro turno ya agendado → **bloqueo duro**, no se permite. Sin reprogramación automática de otros turnos — decisión explícita, el secretario reagenda a mano si hace falta.
* **Prioridad**: Alta | **Esfuerzo**: Medio | **Dependencias**: TASK-040
* **Resultado**: `Appointment`/`IAppointment` (`lib/db/models/Appointment.ts`) incorpora `treatmentId` (ref `Treatment`, `required: true`, sin default — no hay una prestación razonable a la que caer por defecto) y `durationMinutes` (`required: true`, `default: DEFAULT_TREATMENT_DURATION_MINUTES`, la misma constante de TASK-040). `createAppointment` (`lib/actions/appointment.actions.ts`) ahora exige `treatmentId`, busca la prestación con el nuevo `ITreatmentRepository.findById` y **fotografía** su `estimatedDurationMinutes` *actual* al momento de agendar — un cambio posterior a la duración de la prestación no altera turnos ya creados. `existsOverlapping` (`IAppointmentRepository`/`MongoAppointmentRepository`) dejó de comparar por igualdad exacta de `schedule` (ni siquiera hacía la cuenta de 30 minutos fijos que se sospechaba — comparaba el timestamp exacto) y ahora recibe `durationMinutes` y calcula el intervalo real `[schedule, schedule + durationMinutes)` de cada turno candidato para detectar superposición genuina, con turnos consecutivos (uno termina justo cuando el otro empieza) permitidos explícitamente. `updateAppointment`, al reprogramar (`type: "schedule"`), lee la `durationMinutes` ya fotografiada del turno existente (no se puede cambiar de prestación al reprogramar) antes de validar la superposición en el nuevo horario. `AppointmentForm.tsx` agrega el selector "Prestación" (mismo patrón `SELECT` que "Doctor", solo visible con `type="create"`) que llega hasta `createAppointment` como `treatmentId`; `AdminNewAppointmentModal`/`app/admin/page.tsx` pasan las prestaciones activas (`getActiveTreatments`, TASK-010).
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**: `lib/db/models/Appointment.ts` (+test), `lib/repositories/IAppointmentRepository.ts`, `lib/db/repositories/MongoAppointmentRepository.ts` (+test), `lib/repositories/ITreatmentRepository.ts` (+`findById`), `lib/db/repositories/MongoTreatmentRepository.ts` (+test, `findById`), `lib/actions/appointment.actions.ts` (+test fixture), `types/index.d.ts` (`CreateAppointmentParams.treatmentId`), `lib/validation.ts` (`CreateAppointmentSchema`/`ScheduleAppointmentSchema`/`CancelAppointmentSchema`), `components/forms/AppointmentForm.tsx`, `components/AdminNewAppointmentModal.tsx`, `app/admin/page.tsx`
* **Observaciones**:
  - **Hallazgo real al leer el código existente, no solo al implementar**: la descripción del ticket suponía que `existsOverlapping` ya calculaba `scheduledEnd = schedule + 30min`. Al leer el código (TASK-007) resultó ser más simple e ingenuo de lo esperado — comparaba el `schedule` por **igualdad exacta**, sin ningún cálculo de fin de turno. Es decir, hoy en producción un turno de 10:00 y otro de 10:15 (con el mismo doctor) **no** se detectan como superpuestos en absoluto, ni siquiera bajo la asunción de 30 minutos fijos. Esto hace el bug real más severo de lo que el ticket estimaba, y el arreglo de TASK-041 lo corrige de raíz junto con la duración real.
  - `treatmentId` en `AppointmentRecord` quedó **opcional** (a diferencia de `durationMinutes`, que el repositorio siempre resuelve con fallback a 30) porque, a diferencia de una duración, no existe una prestación razonable a la que hacer fallback para turnos agendados antes de esta migración — quedan sin referencia a prestación, y no hay problema porque nada depende de leerla para turnos viejos (ni el cálculo de superposición, que solo necesita la duración).
  - `ScheduleAppointmentSchema`/`CancelAppointmentSchema` agregan `treatmentId` como opcional (nunca usado en esos flujos) solo para que el tipo inferido de `AppointmentFormValidation` (unión de los tres schemas según `type`) siga siendo uniforme en `AppointmentForm.tsx` — mismo patrón ya usado ahí para `reason`/`note`/`cancellationReason`.
  - **No se tocó** `e2e/*.spec.ts`: el flujo de agendado por UI (`AdminNewAppointmentModal`) ahora exige elegir una prestación antes de poder enviar el formulario, y las specs de Playwright existentes que agendan un turno no seleccionan ninguna — probablemente rompan al correrlas. Verificación de este ticket se limitó a `pnpm build` + Vitest (según alcance pedido); no se corrió ni se tocó la suite e2e. Queda pendiente actualizar esos flujos para TASK-042/043, que sí van a tocar esta pantalla.
  - Fuera de alcance, tal cual el ticket: la advertencia por horario fuera de disponibilidad (TASK-042) y el rediseño de calendario/acordeón que reemplaza `AdminNewAppointmentModal` (TASK-043).
  - **Verificación**: `tsc --noEmit` limpio. `pnpm build` compiló y renderizó las 19 rutas sin errores nuevos. Suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) verde: 201/201. Además de los tests automatizados, se corrió un script real contra MongoDB (`.env.local`/`MONGODB_URI` accesible en este entorno) que llama a las mismas rutas de repositorio que usa `createAppointment` con una prestación de 45 minutos: confirma que un turno pedido a las 10:30 (dentro de un turno de 10:00 a 10:45) es **rechazado** — algo que la asunción vieja de 30 minutos fijos (e incluso la comparación por igualdad exacta real) habría dejado pasar — y que un turno consecutivo a las 10:45 (justo cuando termina el anterior) es **aceptado**, confirmando que no hay falsos positivos por superposición.

#### `[TASK-042]` Advertencia al agendar fuera del horario configurado del doctor
* **Descripción**: hoy no hay ninguna validación server-side de que un turno caiga dentro de la disponibilidad configurada del doctor (`Doctor.availability`, TASK-006) — el filtrado de horarios en el datepicker es solo visual y se puede saltear escribiendo la fecha a mano. A diferencia de la superposición real (TASK-041), esto pasa a ser una advertencia con confirmación, no un bloqueo.
* **Criterios de Aceptación**:
  - [x] Si el horario/día elegido cae fuera de `Doctor.availability`, se muestra una advertencia explícita antes de confirmar el turno ("este horario no corresponde a la disponibilidad configurada del doctor, ¿desea asignarlo igual?").
  - [x] Confirmando la advertencia, el turno se crea igual (no es un bloqueo).
* **Prioridad**: Media | **Esfuerzo**: Bajo-Medio | **Dependencias**: TASK-041
* **Resultado**: Función pura `isWithinAvailability(date, availability)` agregada como sibling de `getAvailableSlots` en `lib/scheduling/getAvailableSlots.ts` — reutiliza `toMinutes` (que pasó de función privada a exportada) en vez de duplicar el parseo `"HH:MM"` → minutos y la comparación de `dayOfWeek`; a diferencia de `getAvailableSlots`, no está atada a la granularidad de slots de 30 minutos: un horario tipeado a mano que cae dentro de una ventana configurada, aunque no esté alineado a un slot, cuenta igual como "dentro de disponibilidad" (ventana `[startTime, endTime)`, límite superior exclusivo, mismo criterio que ya usa `getAvailableSlots` para generar slots). `AppointmentForm.tsx` la usa solo en su flujo `type="create"`: al enviar el formulario, si el horario elegido cae fuera de la disponibilidad del doctor seleccionado, en vez de enviar directo se guarda el valor pendiente (`pendingValues`) y se muestra un `AlertDialog` (`components/ui/alert-dialog.tsx`, ya existía en el proyecto sin usarse todavía) con el mensaje pedido por el ticket; confirmando ("Asignar igualmente"), se envía ese mismo valor guardado a `createAppointment` sin volver a validar nada. Si el horario cae dentro de la disponibilidad, se envía directo — sin ningún paso extra, sin regresión al camino feliz.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**: `lib/scheduling/getAvailableSlots.ts` (+test), `components/forms/AppointmentForm.tsx`
* **Observaciones**:
  - **Decisión client-side-only, justificada acá tal como pide el ticket**: se evaluó agregar el mismo chequeo en `createAppointment` (server-side) y se descartó. A diferencia del bloqueo duro de TASK-041 (`existsOverlapping`), que protege una invariante real de datos (dos turnos simultáneos con el mismo doctor), esta es una advertencia explícitamente blanda — el propio criterio de aceptación permite crear el turno igual tras confirmar. Saltear la advertencia (por ejemplo manipulando el estado del formulario, o desde una integración futura que llame a `createAppointment` directamente) no corrompe ningún dato ni genera un estado inconsistente: el peor caso es un turno fuera del horario configurado del doctor, algo que ya podía pasar hoy (el `filterDate`/`includeTimes` del datepicker de TASK-007 es solo una sugerencia visual, tal como señala la descripción de este mismo ticket) y que el secretario puede corregir a mano. Duplicar el chequeo en el servidor agregaría una segunda fuente de verdad para una regla que no protege integridad de datos, sin un beneficio real de seguridad — se optó por dejarla exclusivamente client-side.
  - **Extracción vs. lógica nueva**: solo `toMinutes` se reutiliza literalmente entre ambas funciones. El resto de `isWithinAvailability` (recorrer `availability`, comparar `dayOfWeek`) es código nuevo y deliberadamente no comparte el bucle de generación de slots de `getAvailableSlots`, porque resuelven preguntas distintas — "¿qué slots hay disponibles?" vs. "¿este instante exacto cae dentro de la disponibilidad?".
  - **Convención de testing seguida**: la lógica pura (`isWithinAvailability`) se testeó TDD (rojo→verde) con el mismo rigor que `getAvailableSlots.test.ts` — mismo archivo, nuevo `describe`, 7 casos nuevos (día sin disponibilidad, dentro de ventana, antes del inicio, en/después del fin, horario no alineado a un slot de 30 minutos, múltiples ventanas el mismo día, disponibilidad vacía). El `AlertDialog` de confirmación no se testeó — mismo criterio ya establecido en TASK-034/037/039 para chrome de UI sin convención de testing en este proyecto.
  - No se tocó `existsOverlapping` ni el bloqueo duro de TASK-041 — lógica completamente separada y sin cambios.
  - Fuera de alcance, tal cual el ticket: el rediseño de calendario/acordeón y la eliminación de `AdminNewAppointmentModal` (TASK-043) siguen sin empezar; esta advertencia se agregó a la pantalla actual de agendado (`AppointmentForm` vía `AdminNewAppointmentModal`), que TASK-043 va a reemplazar por completo.
  - **Verificación**: `tsc --noEmit` limpio. `pnpm build` compiló las 21 rutas actuales del proyecto sin errores nuevos (mismos mensajes preexistentes de "Dynamic server usage" por rutas que usan `headers()`, no son errores). Suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) verde: 208/208 (los 7 tests nuevos de `isWithinAvailability` incluidos). No se corrió la suite e2e — mismo alcance ya documentado en TASK-041, las specs de agendado por UI todavía no seleccionan una prestación y van a necesitar actualizarse recién en TASK-043.

#### `[TASK-043]` Vista unificada de "Nuevo turno" con calendario (reemplaza el modal actual)
* **Descripción**: reemplaza `AdminNewAppointmentModal` por una vista única (no un modal chico) para agendar turnos: buscar/crear paciente y elegir doctor+prestación en secciones tipo acordeón (para no ocupar espacio de forma permanente), y un calendario semanal (estilo Google Calendar) del doctor elegido para elegir el horario visualmente, viendo su disponibilidad y turnos ya ocupados antes de confirmar.
* **Criterios de Aceptación**:
  - [x] Pantalla nueva (reemplaza la apertura de `AdminNewAppointmentModal`) con: sección "Datos del paciente" (buscar por DNI, reutilizando TASK-033/039; crear si no existe) y sección "Datos del doctor" (elegir doctor + prestación estimada de TASK-041) como acordeones colapsables.
  - [x] Al tener paciente + doctor + prestación seleccionados, se muestra el calendario semanal del doctor (`react-big-calendar` + `date-fns` como localizador — sin `moment`) con sus turnos ocupados y su disponibilidad configurada visualmente distinguible.
  - [x] Click en un horario libre dentro de su disponibilidad confirma directo; fuera de su disponibilidad dispara la advertencia de TASK-042; sobre un turno ya ocupado, el bloqueo duro de TASK-041 impide seleccionarlo.
  - [x] `AdminNewAppointmentModal.tsx` se elimina — esta vista es el único camino para agendar un turno desde `/admin` en adelante.
* **Prioridad**: Alta | **Esfuerzo**: Alto | **Dependencias**: TASK-040, TASK-041, TASK-042
* **Resultado**: Nueva ruta `/admin/turnos/nuevo` (`app/admin/turnos/nuevo/page.tsx`), server component que resuelve `getActiveDoctors`/`getActiveTreatments`/`getActiveInsuranceProviders` y monta `components/NewAppointmentView.tsx` (client). Esta reemplaza por completo a `AdminNewAppointmentModal` (eliminado) como único camino para agendar un turno desde `/admin`; el botón "Nuevo turno" en `app/admin/page.tsx` pasó de abrir un `Dialog` a ser un `Link` a la nueva ruta, y `app/admin/layout.tsx` (`ADMIN_NAV_ITEMS`, TASK-037) suma la entrada "Nuevo turno" (ícono `CalendarPlus`) para que quede accesible como cualquier otra pantalla de `/admin`, no solo desde el dashboard.
  - **Acordeones** (`components/ui/accordion.tsx`, vendorizado igual que `components/ui/avatar.tsx` en TASK-036 — wrapper delgado sobre `@radix-ui/react-accordion`): `NewAppointmentView` controla un único estado `openSections` (`Accordion type="multiple"`) con dos secciones, "Datos del paciente" y "Datos del doctor", ambas abiertas por default. Cada una se auto-colapsa la primera vez que queda completa (paciente encontrado/creado; doctor + prestación elegidos) — un `ref` por sección evita que se vuelva a forzar el colapso si el usuario la reabre después para cambiar la selección — y muestra un resumen en una línea junto al título (nombre del paciente; "Doctor · Prestación") cuando está colapsada.
  - **Datos del paciente**: reutiliza `findPatientByIdentificationNumber` (TASK-033) tal cual — mismo buscador por DNI que ya usaba `AdminNewAppointmentModal`. Si no se encuentra, reutiliza el `CreatePatientModal` de TASK-039 sin modificarlo estructuralmente, solo agregándole (y a `CreatePatientForm`, que es quien realmente lo necesita) un prop opcional nuevo `onCreated?: (patient) => void` — aditivo, ningún llamador existente (`/admin/pacientes/nuevo`, `/recepcion/pacientes/nuevo`, `PatientsList`) lo pasa, así que su comportamiento no cambió. `NewAppointmentView` lo usa para seleccionar automáticamente al paciente recién creado sin obligar a repetir la búsqueda por DNI.
  - **Datos del doctor**: mismo selector "Doctor" + "Prestación" que TASK-041 ya había agregado a `AppointmentForm.tsx` (mismo patrón `Select`/`SelectItem`, mismas prestaciones activas de TASK-010), pero como control standalone (no ligado a `react-hook-form`, porque esta pantalla no es un formulario con validación de Zod, sino tres selecciones independientes que gatillan el calendario) dentro de su propio acordeón en vez de inline en un único formulario.
  - **Calendario** (`components/DoctorWeekCalendar.tsx`): solo se monta cuando paciente + doctor + prestación están los tres seleccionados (se le pasa `key={doctor.name}` desde `NewAppointmentView` para remontarlo limpio si se cambia de doctor). Usa `react-big-calendar` (`Views.WEEK` fijo, sin otras vistas) con `dateFnsLocalizer` (`date-fns`, localizado en `es` — sin `moment`, tal como pedía el ticket). Nuevo método de repositorio `findByDoctorInRange(primaryPhysician, start, end)` (`IAppointmentRepository`/`MongoAppointmentRepository`, mismo criterio de exclusión de cancelados que `findBookedTimes`/`existsOverlapping`) expuesto vía la nueva Server Action `getDoctorAppointmentsInRange` (`lib/actions/appointment.actions.ts`, gateada con `requireAdminSession` igual que `findPatientByIdentificationNumber` porque el resultado trae nombres de pacientes) — se refetchea cada vez que cambia la semana visible (`onRangeChange`) o después de agendar/fallar un turno. `slotPropGetter` colorea cada franja horaria según `isWithinAvailability` (TASK-042, reutilizada tal cual, no una reimplementación): un verde tenue dentro de la disponibilidad configurada del doctor, un patrón rayado oscuro fuera de ella — sin bloquear el click en ninguno de los dos casos, la disponibilidad es solo indicativa/de advertencia, nunca un bloqueo (ese es el rol exclusivo de `existsOverlapping`, TASK-041).
  - **Reserva por click**: `onSelectSlot` decide con `isWithinAvailability` — dentro de la disponibilidad, agenda directo (`createAppointment`, mismo `status: "pending"` que ya usaba `AdminNewAppointmentModal`, con `reason` fijado al nombre de la prestación elegida ya que esta pantalla no tiene ningún campo de texto libre "Motivo del turno" propio — es un click, no un formulario); fuera de ella, dispara la misma advertencia con confirmación de TASK-042. Esa advertencia se extrajo de `AppointmentForm.tsx` a un componente compartido nuevo, `components/OutOfAvailabilityAlertDialog.tsx` (dos llamadores hoy — `AppointmentForm.tsx`, refactorizado para usarlo en vez de tener el `AlertDialog` inline, y `DoctorWeekCalendar.tsx` — no se justificaba una abstracción más grande que este wrapper delgado). `onSelectEvent` (click sobre un turno ya ocupado) no intenta agendar nada — solo muestra un aviso transitorio ("Este horario ya está ocupado.") por 3 segundos; el invariante real sigue siendo el bloqueo duro server-side de `existsOverlapping` (TASK-041), que se ejecuta igual sin importar qué haga la UI.
  - **Tema oscuro de `react-big-calendar`**: su hoja de estilos base (`react-big-calendar/lib/css/react-big-calendar.css`) se importa en `DoctorWeekCalendar.tsx`, mismo criterio que `react-datepicker/dist/react-datepicker.css` en `AppointmentForm.tsx`. Overrides agregados al final de `app/globals.css`, todos bajo el selector `.rbc-dark-theme` (la clase del `div` contenedor en `DoctorWeekCalendar`) para no afectar nada fuera de esta pantalla, siguiendo la misma paleta `dark-*`/`green-500`/`blue-500` ya usada en el resto de la app.
  - **Nuevas dependencias** (verificadas contra React 19.2.8 antes de instalar, mismo criterio que TASK-004/TASK-036): `react-big-calendar@1.20.0` (declara soporte `react`/`react-dom` `^19`), `date-fns@4.4.0` (usado también por el localizador de `react-big-calendar`), `@radix-ui/react-accordion@1.2.20` (declara soporte `react`/`react-dom` `^19.0.0`). `react-big-calendar` **no** trae sus propios tipos (no publica ningún `.d.ts` ni campo `"types"` en su `package.json`), así que se agregó `@types/react-big-calendar@1.16.3` como dependencia de desarrollo. `pnpm add` no introdujo ninguna advertencia de peer dependency nueva — las únicas que aparecen (`react-hook-form`, `lucide-react`, `next-themes`, `react-datepicker`/`react-onclickoutside` declarando soporte hasta React 18, `@typescript-eslint/eslint-plugin` vs `parser`) son el mismo ruido preexistente y no relacionado ya señalado en tickets anteriores.
  - **Archivos creados**: `app/admin/turnos/nuevo/page.tsx`, `components/NewAppointmentView.tsx`, `components/DoctorWeekCalendar.tsx`, `components/OutOfAvailabilityAlertDialog.tsx`, `components/ui/accordion.tsx`
  - **Archivos modificados**: `app/admin/page.tsx`, `app/admin/layout.tsx`, `app/globals.css`, `components/forms/AppointmentForm.tsx`, `components/forms/CreatePatientForm.tsx` (+`onCreated`), `components/CreatePatientModal.tsx` (+`onCreated`), `lib/repositories/IAppointmentRepository.ts` (+`findByDoctorInRange`), `lib/db/repositories/MongoAppointmentRepository.ts` (+test, `findByDoctorInRange`), `lib/actions/appointment.actions.ts` (+`getDoctorAppointmentsInRange`), `package.json`/`pnpm-lock.yaml`, `e2e/patient-flow.ts`, `e2e/02-flujo.spec.ts`, `e2e/03-adm-extra.spec.ts`, `e2e/helpers.ts`
  - **Archivos eliminados**: `components/AdminNewAppointmentModal.tsx`
* **Observaciones**:
  - **TDD real solo en la capa de datos, como pide la skill de este proyecto**: `findByDoctorInRange` se escribió rojo→verde en `MongoAppointmentRepository.test.ts` (dos casos nuevos: turnos dentro/fuera de rango con paciente poblado, y rango vacío) contra MongoDB real, mismo criterio que cualquier otro método de repositorio en este proyecto. No existe (ni se inventó) una convención de testing para el calendario/acordeón en sí — igual que TASK-034/037/039/042 — la verificación de esa parte fue una lectura estructural cuidadosa del diff propio, más una corrida real de Playwright contra un doctor sembrado directamente en Mongo (ver más abajo).
  - **`status: "pending"` al agendar por click, no `"scheduled"`**: se mantuvo el mismo default que ya usaba `AdminNewAppointmentModal`/`AppointmentForm` (`type="create"`) para no romper el flujo posterior de confirmación ("Confirmar turno" en `/admin`, ya cubierto por specs existentes) — este ticket reemplaza la pantalla de carga, no la semántica de estados de un turno.
  - **Brecha de e2e conocida (TASK-041/042), atacada, no solo documentada**: se actualizó `e2e/patient-flow.ts` — `bookAppointmentAsAdmin` ahora navega a `/admin/turnos/nuevo`, busca por DNI y selecciona doctor+prestación (`goToNewAppointmentWithSelection`, nueva), y un helper nuevo `clickCalendarSlot` hace click en una celda del calendario semanal calculando su posición en el DOM de `react-big-calendar` (`step=30`/`timeslots=1` en `DoctorWeekCalendar` hacen que cada `.rbc-timeslot-group` sea exactamente un slot de 30 minutos) con `force: true` (react-big-calendar superpone un `.rbc-events-container` vacío sobre la grilla, que Playwright marca como "interceptando" un click normal aunque sea el comportamiento esperado de la librería). `createStaffPatient` ahora devuelve también el DNI usado (antes se descartaba), porque el nuevo flujo busca por DNI en vez de por email/teléfono como el modal eliminado. `02-flujo.spec.ts` (PAC-07, DOC-06) y `03-adm-extra.spec.ts` (ADM-09, ADM-05) se actualizaron para usar estos helpers nuevos en vez del `Dialog`/react-datepicker eliminados; `pickAppointmentDateTime` (helper de `e2e/helpers.ts` que solo servía para el flujo viejo) se eliminó por quedar sin ningún llamador — mismo criterio de "no dejar código muerto" ya aplicado en TASK-018/023/024. La carrera de choque de horario de ADM-09 se adaptó: en vez de "llenar dos formularios sin enviar y enviarlos después" (ya no existe ese paso intermedio con un calendario de click directo), ambas páginas cargan el calendario del mismo doctor para la misma semana/horario objetivo (ambas hacen su propio fetch de turnos ocupados antes de que cualquiera reserve) y hacen click en esa misma celda en secuencia — la segunda sigue viendo la celda como libre en su propia vista no refrescada, y su intento de reserva sigue fallando server-side por `existsOverlapping`, igual que antes.
  - **Verificado con una corrida real de Playwright, no solo `tsc`**: se encontró y solucionó un bug real (no relacionado a este ticket) que bloqueaba **toda** la suite e2e desde `00-setup.spec.ts`: la creación de doctores vive ahora dentro de un `Dialog` (`CreateDoctorModal`, ya existente en el repo) cuyo contenido no tiene `max-h`/`overflow-y-auto` (a diferencia de `CreatePatientModal`, que sí lo tiene) — los checkboxes de disponibilidad de los últimos días de la semana quedan fuera del viewport y Playwright no puede clickearlos. Arreglar esa pantalla está fuera del alcance de este ticket, así que en vez de tocar `00-setup.spec.ts` (que además no logra desbloquear el resto sin ese arreglo) se sembró un doctor directo contra Mongo (bypaseando esa pantalla) en un spec temporal, descartado antes de terminar este ticket, para validar de punta a punta la pantalla nueva: búsqueda por DNI (encontrado y no encontrado, con el fallback "Crear paciente" visible), selección de doctor+prestación con colapso de acordeones, reserva directa dentro de disponibilidad, advertencia+confirmación fuera de disponibilidad (cancelar y confirmar), aviso de horario ocupado al clickear un turno ya agendado, y la carrera de choque de horario con dos páginas — las seis corrieron en verde contra el server de e2e real (`next dev` + Mongo `-e2e`, TASK-014). No se pudo correr `02-flujo.spec.ts`/`03-adm-extra.spec.ts` tal cual quedaron (dependen de `00-setup.spec.ts` para el fixture del doctor) en esta pasada por ese bug ajeno — quedan con el código ya actualizado y tipado, pendientes de una corrida de punta a punta una vez que `CreateDoctorModal` se corrija (ticket de seguimiento recomendado, no abierto acá por estar fuera de alcance).
  - No se tocó `existsOverlapping` ni `isWithinAvailability` — se reutilizan tal cual (TASK-041/042), tal como pedía el ticket.
  - No se agregó ningún punto de entrada de "Nuevo turno" para Secretaria/`/recepcion` — fuera de alcance, tal cual el ticket.
  - No se agregó reprogramación automática en cascada — decisión ya tomada para todo este epic (TASK-041/042).
  - **Verificación**: `tsc --noEmit` limpio. `pnpm build` compiló las 22 rutas del proyecto sin errores nuevos (mismos 2 warnings preexistentes no relacionados en `FileUploader.tsx`/`BillingForm.tsx`, y los mismos mensajes de "Dynamic server usage" ya vistos en tickets anteriores). Suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) verde: 210/210 (los 2 tests nuevos de `findByDoctorInRange` incluidos), corrida contra MongoDB real (`.env.local`/`MONGODB_URI` accesible en este entorno). Ver la observación anterior para el detalle de la verificación e2e real (6/6 escenarios propios en verde vía spec temporal; `02-flujo`/`03-adm-extra` actualizados pero bloqueados de punta a punta por un bug ajeno en `CreateDoctorModal`).

---

### EPIC 12: Fix del bloqueo e2e hallado en TASK-043

#### `[TASK-044]` `CreateDoctorModal` no permite ver los checkboxes de disponibilidad
* **Descripción**: encontrado por el agente de TASK-043 verificando e2e — el `Dialog` de `CreateDoctorModal` (TASK-034) no tiene `max-h`/`overflow-y-auto` (a diferencia de `CreatePatientModal`, que sí lo tiene), así que los checkboxes de disponibilidad de `DoctorForm` quedan fuera del área visible/interactuable del diálogo. Esto bloqueaba por completo la suite de Playwright (`00-setup.spec.ts` en adelante depende de poder crear un doctor con disponibilidad).
* **Criterios de Aceptación**:
  - [x] `CreateDoctorModal` (y `EditDoctorForm` dentro de su propio `Dialog` en `DoctorRow`, si tiene el mismo problema) permite ver y tildar todos los checkboxes de disponibilidad sin que queden recortados.
* **Prioridad**: Alta (bloquea toda la suite de e2e) | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: Mismo tratamiento que `CreatePatientModal` (TASK-039) — se agregó `max-h-[90vh] overflow-y-auto` al `className` del `DialogContent` de `CreateDoctorModal`. Se verificó por lectura que `DoctorRow` (TASK-034) tiene exactamente el mismo `Dialog` sin acotar para su modo edición (`EditDoctorForm`, que también renderiza `DoctorAvailabilityPicker`) y se le aplicó el mismo fix — no era una suposición, el ticket pedía verificarlo explícitamente y el problema estaba efectivamente presente. Se revisaron además los otros tres `Create*Modal` (`CreateSecretariaModal`, `CreateAdminModal`, `CreateTreatmentModal`) y sus `*Row` de edición correspondientes (`SecretariaRow`, `AdminRow`, `TreatmentRow`): los tres formularios son cortos (3-4 campos simples, sin ningún widget tipo `DoctorAvailabilityPicker`) y ya entran cómodos en un viewport típico, así que se dejaron sin tocar — no se encontró el mismo problema ahí, y forzar el mismo cambio hubiera sido alcance innecesario. `DoctorAvailabilityPicker` en sí no tiene ningún contenedor con altura acotada propia (`space-y-4` + `flex flex-wrap`), así que el único recorte venía del `DialogContent` sin límite, confirmando que el fix en el nivel del diálogo es suficiente.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**: `components/CreateDoctorModal.tsx`, `components/DoctorRow.tsx`
* **Observaciones**:
  - **Verificación e2e real, con un hallazgo adicional y ajeno al alcance**: se intentó correr `e2e/00-setup.spec.ts` (`MONGODB_URI`/`.env.local` accesibles en este entorno, igual que en TASK-043) y siguió fallando, pero por un bug distinto y preexistente en el spec mismo, no por este ticket: `00-setup.spec.ts` nunca hace click en el botón trigger "Crear Doctor" antes de intentar `page.getByLabel("Nombre").fill(...)`, así que el `Dialog` nunca llega a abrirse (confirmado con captura de pantalla del fallo — la página muestra la lista vacía de doctores con el botón "Crear Doctor" sin clickear). Ese bug existe desde el commit original que creó el archivo (`946f41e`, TASK-014/021) y es independiente del `max-h`/`overflow-y-auto` que corrige este ticket. Arreglar `00-setup.spec.ts` está fuera del alcance de este ticket (que es específicamente sobre el recorte visual del `Dialog`, no sobre la corrección del propio spec), así que en vez de tocarlo se armó un spec temporal (`e2e/zz-temp-task044-verify.spec.ts`, borrado antes de cerrar el ticket, igual criterio que el spec temporal descartado en TASK-043) que sí hace click en "Crear Doctor" y confirma que los 7 checkboxes de disponibilidad (Domingo a Sábado) quedan visibles y clickeables con el fix aplicado — corrió en verde contra el server de e2e real (`next dev` + Mongo `-e2e`). La suite completa (`00-setup.spec.ts` en adelante) sigue sin poder correr de punta a punta hasta que se arregle ese bug ajeno del spec — se recomienda un ticket de seguimiento para agregar el `click` faltante en `00-setup.spec.ts`, no abierto acá por estar fuera de alcance.
  - **Verificación**: `tsc --noEmit` limpio. `pnpm build` compiló las 21 rutas del proyecto sin errores nuevos (mismos mensajes preexistentes de "Dynamic server usage"). No se corrieron tests de Vitest nuevos — este ticket es un cambio puramente de clases CSS en JSX existente, sin lógica nueva que testear, mismo criterio ya aplicado a cambios de chrome de UI en tickets anteriores (TASK-034/037/039/042).

#### `[TASK-045]` `e2e/00-setup.spec.ts` no hace click en "Crear Doctor" antes de completar el formulario
* **Descripción**: encontrado por el agente de TASK-044 verificando el fix del `Dialog` — el spec llama a `page.getByLabel("Nombre").fill(...)` sin haber abierto antes el diálogo de alta (falta el click en el botón "Crear Doctor"). Bug preexistente del spec en sí, no del código de la app — existe desde que se escribió (commit `946f41e`, TASK-014/021). Es la razón real por la que la suite completa de Playwright no corre de punta a punta hoy, no el bug de `CreateDoctorModal` que TASK-044 ya resolvió.
* **Criterios de Aceptación**:
  - [x] `e2e/00-setup.spec.ts` abre el diálogo de alta de Doctor (click en "Crear Doctor") antes de completar el formulario.
  - [x] La suite completa de Playwright corre de punta a punta sin bloquearse en este spec.
* **Prioridad**: Media | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: Se agregó `await page.getByRole("button", { name: "Crear Doctor" }).click();` en `e2e/00-setup.spec.ts`, justo después de `page.goto("/admin/doctors")` y antes del primer `page.getByLabel("Nombre").fill(...)`, en el test `ADM-02 - alta de doctor con disponibilidad todos los dias`. El texto exacto del botón se verificó leyendo `components/CreateDoctorModal.tsx`: el `DialogTrigger` envuelve un `Button` con el texto literal "Crear Doctor" (con D mayúscula, distinto del botón de submit dentro del formulario, "Crear doctor" con d minúscula, que el spec ya usaba correctamente en la línea siguiente). Se buscó con `rg` el mismo patrón (`getByLabel("Nombre")`/`Especialidad`/`Matrícula`/creación de doctor) en el resto de specs (`01-auth`, `02-flujo`, `03-adm-extra`, `04-security`, `patient-flow.ts`, `helpers.ts`) y no se encontró ningún otro lugar que cree un doctor a través del formulario — todos los demás specs consumen el doctor ya creado por `00-setup.spec.ts` vía `state.ts`/`writeState`, así que el bug no estaba duplicado en ningún otro archivo.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**: `e2e/00-setup.spec.ts`.
* **Observaciones**:
  - **Verificado con una corrida real y completa de `pnpm test:e2e`, no parcial**: contra el server de e2e real (`next dev` + Mongo `-e2e`, TASK-014, `.env.local`/`MONGODB_URI` accesibles en este entorno). Resultado: **28 passed, 1 failed, 4 did not run** de 33 tests totales (3.3 min). Los 3 tests de `00-setup.spec.ts` (ADM-02, ADM-06, y el login del doctor recién creado) pasan en verde, confirmando que el fix de este ticket funciona y que la suite ya no se bloquea ahí — el bloqueo real descripto en el ticket quedó resuelto.
  - **Falla encontrada, no relacionada a este ticket, reportada sin tocar**: `02-flujo.spec.ts:230` (`DOC-06 - aislamiento del odontograma entre pacientes`) falla por un `expect(locator).toHaveClass(/bg-blue-500/)` que nunca se cumple (el botón de la pieza dental queda con `bg-white` en vez del color esperado tras cambiar de paciente) — timeout de 45s, con un `[Error: aborted] { code: 'ECONNRESET' }` del webserver en el log justo antes. Es un bug de la app (u otra condición de carrera ajena a este ticket) en el odontograma, no del spec de creación de doctor. Al estar `DOC-06` dentro del mismo `test.describe.serial` que los 4 tests siguientes (`DOC-09`, el criterio de aceptación de FLU-02, y dos de `FLU-03`), Playwright los marcó como "did not run" — no fallaron por este ticket, sino por la política de `describe.serial` de abortar el resto del bloque tras la primera falla. El resto de la suite (`03-adm-extra.spec.ts` completo, `04-security.spec.ts` completo, y el resto de `02-flujo.spec.ts` antes de DOC-06) corrió y pasó en verde. No se investigó ni se tocó esta falla por estar fuera del alcance de TASK-045 — queda como candidato a un ticket de seguimiento.
  - **Verificación**: `tsc --noEmit` limpio (los specs de Playwright son TypeScript). `pnpm build` compiló las 22 rutas del proyecto sin errores nuevos (mismos warnings preexistentes de "Dynamic server usage"). No se corrió Vitest nuevo — este ticket es exclusivamente un cambio de un spec e2e, sin lógica de aplicación involucrada.

#### `[TASK-047]` `MissingSchemaError` para el modelo `Patient` en el dashboard de Admin
* **Descripción**: reportado por el usuario probando `/admin` en local — `MongoAppointmentRepository.findRecent()` fallaba con `MissingSchemaError: Schema hasn't been registered for model "Patient"` al hacer `.populate("patientId")`, y como consecuencia directa `AdminPage` crasheaba con `Cannot read properties of undefined (reading 'scheduledCount')` (la acción atrapa el error, loguea y devuelve `undefined`, y la página intenta leer un campo de ese `undefined`).
* **Criterios de Aceptación**:
  - [x] `/admin` carga sin errores de consola ni crashear el dashboard.
* **Prioridad**: Alta (rompe el dashboard de Admin) | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: causa raíz encontrada en `lib/db/repositories/MongoAppointmentRepository.ts` — el archivo solo importaba `IPatient` con `import type { IPatient } from "../models/Patient"`, un import **de tipo**, que TypeScript borra por completo en tiempo de compilación. Como resultado, el módulo `lib/db/models/Patient.ts` (cuya única línea a nivel de módulo registra el schema en Mongoose: `models.Patient || model<IPatient>("Patient", patientSchema)`) nunca se ejecutaba en esa ruta de código, así que `.populate("patientId")` fallaba al no encontrar "Patient" registrado en el registro global de Mongoose. Corregido agregando un import real, solo por su efecto secundario: `import "../models/Patient";`, junto al `import type` existente (que se mantiene, ya sigue haciendo falta para el tipado).
  - **Archivos modificados**: `lib/db/repositories/MongoAppointmentRepository.ts`.
* **Observaciones**:
  - Se verificó con `rg` que `.populate()` solo se usa en este archivo (3 ocurrencias, las tres contra `patientId`) — no hay otro lugar del código con el mismo patrón de bug.
  - `tsc --noEmit` limpio después del fix.

#### `[TASK-048]` Crash de navegación en el calendario de "Nuevo turno" (`Cannot read properties of undefined (reading 'range')`)
* **Descripción**: reportado por el usuario probando `/admin/turnos/nuevo` — al intentar cambiar de día o usar los botones "Hoy"/"Atrás"/"Siguiente" del calendario (`DoctorWeekCalendar.tsx`), la app crasheaba. Mismo síntoma que "los botones Hoy/Atrás/Siguiente no funcionan" — un solo bug, no dos.
* **Criterios de Aceptación**:
  - [x] Cambiar de semana y usar los botones de navegación del calendario no crashea la app.
* **Prioridad**: Alta (rompe la navegación del calendario recién construido en TASK-043) | **Esfuerzo**: Bajo | **Dependencias**: Ninguna
* **Resultado**: causa raíz encontrada leyendo el código fuente de `react-big-calendar` (`node_modules/react-big-calendar/lib/Calendar.js`), no adivinada. `DoctorWeekCalendar.tsx` usaba `defaultView={Views.WEEK}` (no controlado) sin `onView`. `Calendar` está envuelto en el HOC `uncontrollable(Calendar, { view: "onView", date: "onNavigate", selected: "onSelectEvent" })`, y su lógica interna de navegación (`handleNavigate`) resuelve el componente de vista actual vía `this.getView()` → `views[this.props.view]`. Como el calendario de este proyecto solo permite una vista (`views={[Views.WEEK]}`), no había ninguna razón para dejar `view` sin controlar — se fijó explícito: `view={Views.WEEK}` + `onView={() => {}}` (nunca cambia, así que no hace falta manejarlo) y, de paso, se controló también `date` (`date`/`onNavigate` con estado propio) en vez de `defaultDate`, para sacar toda ambigüedad sobre la sincronización del HOC en esta combinación de versiones.
  - **Archivos modificados**: `components/DoctorWeekCalendar.tsx`.
* **Observaciones**:
  - `tsc --noEmit` y `pnpm build` limpios después del fix, 22 rutas compiladas sin errores nuevos.

### EPIC 13: Feedback de uso de "Nuevo turno" y consistencia de botones

#### `[TASK-049]` Estado de carga integrado a `Button`, reemplazo de `<button>` crudo en todo el proyecto
* **Descripción**: `components/SubmitButton.tsx` ya tenía el patrón correcto de loading (spinner + "Cargando...", deshabilitado mientras carga) pero era un wrapper aparte de `components/ui/button.tsx` — el componente base que debería usarse en todo el proyecto. Se pidió: (1) mover el estado de carga al `Button` base, (2) auditar el proyecto y reemplazar cada etiqueta `<button>` cruda por `Button`, (3) eliminar `SubmitButton` una vez sin usos.
* **Criterios de Aceptación**:
  - [x] `Button` (`components/ui/button.tsx`) acepta un prop de carga (mismo comportamiento visual que `SubmitButton`: ícono girando + texto "Cargando...", deshabilitado mientras está en ese estado).
  - [x] Todo `<button>` crudo del proyecto (`components/`, `app/`) se reemplaza por `Button`, preservando el estilo visual que tenía cada uno (clases existentes).
  - [x] Los botones que disparan una acción real con espera (Server Actions — submits de formularios, "Desactivar"/"Reactivar", "Crear acceso", etc.) quedan cableados con el nuevo estado de carga del `Button`, no solo con el cambio de etiqueta.
  - [x] `components/SubmitButton.tsx` se elimina una vez reemplazados todos sus usos.
* **Prioridad**: Alta | **Esfuerzo**: Alto (toca gran parte del proyecto) | **Dependencias**: Ninguna
* **Resultado**: `ButtonProps` (`components/ui/button.tsx`) ganó `isLoading?: boolean` y `loadingText?: string` (default `"Cargando..."`, para no forzar un solo texto en todos los call sites). Con `isLoading` true y `asChild` false, `Button` reemplaza sus `children` por el mismo patrón spinner+texto que tenía `SubmitButton` (`/assets/icons/loader.svg` con `animate-spin`) y fuerza `disabled` (`disabled || isLoading`, así un `disabled` explícito del caller se sigue respetando de forma independiente). Con `asChild` true, `isLoading` se ignora por completo a propósito: `asChild` renderiza como el único hijo vía Radix `Slot`, que exige exactamente un elemento hijo — inyectar el wrapper spinner+texto rompería ese contrato, y los usos de `asChild` en este proyecto son de navegación (`Link`), no acciones async con estado de espera.
  Se auditó el proyecto completo con `rg -n "<button" components app` (inventario, no muestreo): 4 archivos tenían `<button>` crudo. Los 12 usos existentes de `SubmitButton` (forms de Doctor/Secretaria/Admin/Treatment/Patient/Appointment/Billing/ClinicalNote/Login) migraron a `Button isLoading={isLoading}` con el mismo `type="submit"` y, donde `SubmitButton` aplicaba su default `"shad-primary-btn w-full"` (`Button` no tiene ese default propio), se lo agregó explícito en cada call site para no cambiar el estilo. Además, 4 botones "Desactivar"/"Reactivar" que ya usaban `Button` pero solo con `disabled={isToggling}` (sin feedback visual) se cablearon a `isLoading={isToggling}` para dar la misma señal de carga que el resto de las acciones async — el criterio de aceptación pide esa consistencia explícitamente para esos botones, no solo el cambio de etiqueta.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**:
    - `components/ui/button.tsx` — prop `isLoading`/`loadingText`.
    - `app/sentry-example-page/page.tsx` — `<button>` → `Button`, más `isLoading` cableado al `fetch`/`Sentry.startSpan` del botón de prueba (única acción async con espera real de la página). Estilo preservado vía el mismo `style` inline (mayor especificidad que las clases del `Button`, así que el look no cambia).
    - `components/LogoutButton.tsx` — `<button>` → `Button`, `isLoading` cableado a `signOut()` (acción async real).
    - `components/AppSidebarNav.tsx` — `<button>` (trigger del menú offcanvas, dentro de `DialogTrigger asChild`) → `Button variant="ghost" size="icon"`, solo cambio de etiqueta (abrir el menú es instantáneo, no hay espera). `hover:text-green-500` agregado para neutralizar el `hover:text-slate-900` del variant `ghost` y no cambiar el color del ícono al pasar el mouse.
    - `components/Odontogram.tsx` — dos botones: (1) la celda de cara de diente en `ToothGrid` (`<button>` → `Button variant="ghost"` con `rounded-none p-0` agregados para neutralizar el `rounded-md`/padding por defecto del componente — toggle instantáneo de color, sin espera, solo cambio de etiqueta); (2) el botón "Guardar odontograma" (ya era `Button`) se migró de `disabled={isSaving}` + texto condicional manual a `isLoading={isSaving} loadingText="Guardando..."`.
    - `components/DoctorRow.tsx`, `components/SecretariaRow.tsx`, `components/AdminRow.tsx`, `components/TreatmentRow.tsx` — botón "Desactivar"/"Reactivar" (ya `Button`): `disabled={isToggling}` → `isLoading={isToggling}`.
    - `components/forms/AppointmentForm.tsx`, `components/forms/BillingForm.tsx`, `components/forms/ClinicalNoteForm.tsx`, `components/forms/CreateAdminForm.tsx`, `components/forms/CreateDoctorAccessForm.tsx`, `components/forms/CreatePatientForm.tsx`, `components/forms/CreateSecretariaForm.tsx`, `components/forms/DoctorForm.tsx`, `components/forms/EditAdminForm.tsx`, `components/forms/EditDoctorForm.tsx`, `components/forms/EditSecretariaForm.tsx`, `components/forms/LoginForm.tsx`, `components/forms/TreatmentForm.tsx` — `SubmitButton` → `Button type="submit" isLoading={isLoading}` (mismo `className`/`disabled` que cada uno ya pasaba).
  - **Archivos eliminados**: `components/SubmitButton.tsx` (sin usos restantes, verificado con `rg -n "SubmitButton" components app`).
* **Observaciones**:
  - **`components/DoctorWeekCalendar.tsx` quedó fuera de alcance a propósito**: el ticket lo menciona como ejemplo de acción async con espera ("el click de reserva del calendario"), pero ese archivo no tiene ningún `<button>` ni `Button` propio — la reserva se dispara clickeando un slot vacío del calendario (`onSelectSlot`/`handleSelectSlot` de `react-big-calendar`), no un botón. El estado `isBooking` ya existe ahí (agregado en TASK-043) para evitar doble-booking, pero no hay ningún elemento de tipo botón al que cablearle `isLoading` — no se inventó una UI nueva para esto, por estar fuera del alcance explícito del ticket ("no rediseñar cómo se ve ningún botón").
  - **Decisión de diseño en los dos botones "decorativos" convertidos** (celda de diente en `Odontogram.tsx`, trigger del menú en `AppSidebarNav.tsx`): usar el `Button` base con `variant`/tamaño por defecto los hubiera roto visualmente (fondo sólido oscuro del variant `default`, padding de 16/8px, o el destello de `hover:bg-slate-900/90` en hover sobre una casilla de color de 16×16px). Se optó por `variant="ghost"` (sin fondo propio en reposo) más clases de override puntuales (`rounded-none p-0`, `size="icon"`) en vez de dejar el `<button>` crudo sin convertir, cumpliendo el criterio de aceptación de reemplazar *todo* `<button>` crudo sin al mismo tiempo inventar una variante visual nueva no pedida.
  - `npx tsc --noEmit`: limpio. `pnpm build`: las 22 rutas compilaron sin errores nuevos (mismos warnings preexistentes de "Dynamic server usage" en rutas que usan `headers()`, no relacionados con este cambio).
  - Sin tests nuevos: este ticket es exclusivamente de chrome de UI (cambio de etiqueta/estado visual, sin lógica de negocio ni Server Actions tocadas), mismo criterio ya aplicado a TASK-034/037/039/042/044.

#### `[TASK-050]` Búsqueda de paciente mejorada en "Nuevo turno"
* **Descripción**: la búsqueda de paciente en `/admin/turnos/nuevo` (`NewAppointmentView.tsx`) hoy es un input + botón "Buscar", busca solo por DNI exacto, y al encontrar un paciente solo muestra su nombre.
* **Criterios de Aceptación**:
  - [x] La búsqueda se dispara en tiempo real (mientras se escribe, con debounce razonable) o al presionar Enter — no requiere clickear un botón aparte.
  - [x] Acepta buscar por nombre (parcial) o por DNI, no solo DNI exacto.
  - [x] Al encontrar un paciente, se muestran más datos que solo el nombre (ej. DNI, teléfono, email, médico primario — a definir en la implementación qué campos son más útiles para confirmar que es la persona correcta).
* **Prioridad**: Media | **Esfuerzo**: Medio | **Dependencias**: TASK-049
* **Resultado**: `NewAppointmentView.tsx`'s "Datos del paciente" section reemplazó el input + botón "Buscar" (DNI exacto vía `findPatientByIdentificationNumber`, TASK-033) por un único cuadro de texto ("Buscar por nombre o DNI") que dispara la búsqueda solo, con un debounce de 350ms vía `useEffect`/`setTimeout` (sin librería nueva — no existía ningún patrón de debounce en el proyecto, TASK-035/`PatientsList` busca por submit explícito). Se eligió debounce puro por sobre Enter: es el que más directamente cumple "no requiere clickear un botón aparte" para una caja de búsqueda continua que un empleado de mostrador tipea con el paciente presente, y no había ningún precedente de búsqueda-por-Enter en el código para seguir en su lugar. Un mínimo de 2 caracteres (`MIN_QUERY_LENGTH`) evita disparar una consulta contra Mongo en cada tecla de una caja casi vacía.
  - **Búsqueda por nombre O DNI, reusando `findAll`/`listPatients`**: `IPatientRepository.findAll`/`MongoPatientRepository.findAll` (TASK-035) ganó un tercer filtro opcional, `search`, que arma un `$or: [{name: regex}, {identificationNumber: regex}]` (mismo `escapeRegExp`/case-insensitive ya existente) en vez de reusar `name`+`identificationNumber` tal cual, que **AND**-ean entre sí (dos cajas de filtro separadas de TASK-035) — pasar el mismo texto a ambos habría exigido que un paciente matcheara su nombre y su DNI a la vez con el mismo texto, lo cual nunca ocurre para una búsqueda por nombre. `search` toma precedencia sobre `name`/`identificationNumber` cuando está presente, dejando la semántica AND de TASK-035 intacta para `PatientsList`, que no se tocó. `listPatients` (`lib/actions/patient.actions.ts`) expone el nuevo filtro tal cual (`ListPatientsFilters.search`) y es la Server Action que `NewAppointmentView` ahora llama en vez de `findPatientByIdentificationNumber` — mismo gateo (`requireSecretariaOrAdminSession`), reusada sin cambios de firma para los llamadores existentes.
  - **Selección entre múltiples resultados**: a diferencia de la búsqueda exacta por DNI (0 o 1 resultado), una búsqueda por nombre parcial puede devolver varios pacientes. Se muestra una lista de tarjetas clickeables (nombre + documento + teléfono, estilo similar a `PatientRow.tsx` pero no reusada literalmente porque `PatientRow` no es interactiva/seleccionable — se armó un `Button variant="ghost"` por resultado dentro de un `<li>`, mismo patrón ya usado para elementos "decorativos convertidos" en TASK-049) — clickear uno lo selecciona como el paciente del turno, igual que antes.
  - **Resumen del paciente seleccionado con más datos**: "Datos del paciente" ahora muestra nombre, documento (tipo + número, o "Sin documento registrado") y teléfono en vez de solo el nombre — mismos dos campos (DNI, teléfono) que ya usa `PatientRow.tsx`/TASK-035 para su fila de listado, elegidos por ser los más útiles para que un empleado de mostrador confirme que es la persona correcta (el DNI en particular, por ser la clave de búsqueda real de todo este flujo desde TASK-033). El flujo de "no se encontró, crear paciente" (`CreatePatientModal`, TASK-039/043) también quedó con el mismo resumen: `onCreated` (en `CreatePatientForm.tsx`/`CreatePatientModal.tsx`) se amplió de forma aditiva con `identificationNumber`/`phone` (además de `$id`/`name` ya existentes) para que un paciente recién creado muestre el mismo resumen que uno encontrado por búsqueda, sin cambiar la firma para ningún otro llamador (ninguno pasa `onCreated` hoy salvo `NewAppointmentView`). El texto por defecto pre-cargado en `CreatePatientModal` (`defaultName`/`defaultIdentificationNumber`) ahora se elige según si lo tipeado es todo numérico (`/^\d+$/`) — antes siempre era DNI porque la caja vieja solo aceptaba DNI.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**: `lib/repositories/IPatientRepository.ts`, `lib/db/repositories/MongoPatientRepository.ts` (+test), `lib/actions/patient.actions.ts`, `components/NewAppointmentView.tsx`, `components/forms/CreatePatientForm.tsx`, `components/CreatePatientModal.tsx`, `e2e/patient-flow.ts`, `e2e/03-adm-extra.spec.ts`
* **Observaciones**:
  - **`findPatientByIdentificationNumber` no se tocó, tal como pedía el ticket**: se verificó con `rg` que sigue sin ningún otro llamador en el código (solo un comentario en `appointment.actions.ts` que lo menciona de paso) — queda sin uso directo desde la UI tras este cambio, pero no se eliminó porque el alcance explícito de este ticket lo prohibía tocarlo; una futura limpieza de código muerto queda fuera de este ticket.
  - **e2e actualizado, no dejado roto**: `e2e/patient-flow.ts` (`goToNewAppointmentWithSelection`) y `e2e/03-adm-extra.spec.ts` (ADM-05) referenciaban el placeholder viejo ("DNI del paciente") y el botón "Buscar" eliminados — se actualizaron para tipear en la nueva caja ("Buscar por nombre o DNI") y clickear la tarjeta de resultado que contiene el DNI buscado (el locator por rol de Playwright espera automáticamente a través del debounce + round trip, sin necesidad de un `waitFor` explícito). No se corrió la suite e2e completa en este ticket (fuera del alcance de verificación pedido, mismo criterio que TASK-041/042), pero se dejó el código actualizado y tipado en vez de con referencias rotas a elementos que ya no existen.
  - No se tocó TASK-051 (calendario en popup) ni TASK-052 (confirmación explícita de guardado) — ambos sin empezar, fuera de alcance.
  - No se agregó edición de pacientes desde esta búsqueda — sigue fuera de alcance, mismo criterio que TASK-035/039.
  - **Verificación**: `npx tsc --noEmit` limpio. `pnpm build` compiló las 21 rutas del proyecto sin errores nuevos (mismos warnings preexistentes de "Dynamic server usage" en rutas que usan `headers()`). Suite Vitest completa (`pnpm exec vitest run --exclude "e2e/**" --exclude ".claude/worktrees/**"`) verde: 215/215 (los 5 tests nuevos de `findAll`'s filtro `search` incluidos) — TDD real contra MongoDB (`.env.local`/`MONGODB_URI` accesible en este entorno): rojo confirmado primero (4 tests fallando esperando el filtro `search`), verde después de implementarlo.

#### `[TASK-051]` Calendario de "Nuevo turno" como popup a pantalla casi completa
* **Descripción**: el calendario vivía embebido en la pantalla, compartiendo espacio con los acordeones de paciente/doctor, y se veía apretado. Se pidió que el calendario se muestre en un popup grande (80vw × 90vh), y que una vez que se llega a esa instancia (paciente + doctor + prestación ya elegidos) se elimine el efecto acordeón de esas secciones — dejan de competir por espacio con el calendario.
* **Criterios de Aceptación**:
  - [x] El calendario se muestra en un `Dialog` (u overlay similar) de aproximadamente 80vw de ancho y 90vh de alto, en vez de embebido en el flujo de la página.
  - [x] Las secciones de "Datos del paciente"/"Datos del doctor" dejan de comportarse como acordeón una vez que se abre el calendario — se resuelve un layout final coherente con el popup (a definir en la implementación: resumen fijo arriba del popup, por ejemplo).
* **Prioridad**: Media | **Esfuerzo**: Medio | **Dependencias**: TASK-049
* **Resultado**: `NewAppointmentView.tsx`'s render ahora rama en `patient && selectedDoctor && selectedTreatment` (el mismo trigger que antes montaba el calendario inline, TASK-043): mientras falte alguno, se ve el `Accordion` de siempre ("Datos del paciente"/"Datos del doctor"); apenas los tres están completos, el `Accordion` deja de renderizarse por completo (no solo auto-colapsado) y se reemplaza por una barra compacta no colapsable ("Paciente: X · Doctor: Y · Prestación: Z") con dos botones — "Cambiar paciente"/"Cambiar doctor" — y un `Button` "Ver calendario" que abre un `Dialog`/`DialogContent` (`components/ui/dialog.tsx`, TASK-034/039) con `className="shad-dialog flex h-[90vh] w-[80vw] max-w-none flex-col overflow-hidden"`. Dentro del `Dialog`: un `DialogHeader` fijo (`shrink-0`) con el mismo resumen "Paciente: X · Doctor: Y · Prestación: Z" (útil para confirmar la selección sin tener que cerrar el popup) y, debajo, `DoctorWeekCalendar` dentro de un contenedor `min-h-0 flex-1 overflow-y-auto` (para que el header fijo no se vea aplastado por la altura fija de 600px que `DoctorWeekCalendar` ya le da internamente al `Calendar` de `react-big-calendar`). El cierre del popup usa la `X` que `DialogContent` ya trae de fábrica (mismo patrón que `CreatePatientModal`) — no se agregó un botón "Cerrar" redundante.
  El requisito de "layout final coherente, resumen fijo arriba, con una forma de volver a cambiar una selección" se resolvió sin un estado de UI aparte ("modo acordeón" vs. "modo popup"): "Cambiar paciente" reusa la función `changePatient` ya existente (TASK-043/050, ahora también cierra el `Dialog` si estaba abierto) y la nueva `changeDoctor` limpia `doctorName`/`treatmentId` (y cierra el `Dialog`) — al limpiar cualquiera de los dos, la condición `patient && selectedDoctor && selectedTreatment` vuelve a ser falsa y el `Accordion` reaparece solo, con el estado de `openSections`/los refs de auto-colapso intactos (nunca se desmontan al pasar a modo resumen, viven en el componente padre). El estado del `Dialog` (`calendarOpen`) es independiente de la selección — cerrarlo con la `X` solo oculta el calendario, nunca borra `patient`/`doctorName`/`treatmentId`.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**:
    - `components/NewAppointmentView.tsx` — reestructuración del render descrita arriba: import de `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogTrigger`, estado `calendarOpen`, función `changeDoctor`, barra de resumen + `Dialog` reemplazando el `Accordion` una vez completa la selección.
    - `e2e/patient-flow.ts` — `goToNewAppointmentWithSelection` (usada por `bookAppointmentAsAdmin`/`clickCalendarSlot` y por ADM-09 en `02-flujo.spec.ts`) ahora clickea el nuevo botón "Ver calendario" antes de esperar `.rbc-time-content` visible, ya que el calendario dejó de montarse automáticamente al completar la selección.
* **Observaciones**:
  - **TASK-048 (navegación del calendario) verificado, no roto**: `DoctorWeekCalendar.tsx` no se tocó — `view={Views.WEEK}` + `onView={() => {}}` y `date`/`onNavigate` controlados siguen exactamente igual. Se revisó con lectura estructural que envolver el componente en un `Dialog` no cambia sus props ni introduce un `key` nuevo que lo remonte mientras el popup permanece abierto (el único `key={selectedDoctor.name}` ya existía antes de este ticket, para el caso de cambiar de doctor). El `Dialog` de Radix desmonta su `DialogContent` al cerrarse, así que cerrar y reabrir el popup sí remonta `DoctorWeekCalendar` (vuelve a la semana actual) — comportamiento esperado y no un requisito de este ticket preservar la semana entre aperturas, solo que Hoy/Atrás/Siguiente y el cambio de semana sigan andando mientras el popup está abierto, lo cual no depende de nada que este ticket haya tocado.
  - **TASK-052 explícitamente fuera de alcance**: no se tocó `handleSelectSlot`/`onSelectSlot` de `DoctorWeekCalendar.tsx` — clickear un horario libre sigue reservando directamente (o disparando la advertencia de fuera-de-disponibilidad), sin paso de selección/confirmación intermedio.
  - **TASK-050 no tocado**: la búsqueda de paciente (debounce, caja única nombre/DNI, tarjetas de resultado) sigue exactamente igual — el `Accordion` que la contiene simplemente deja de renderizarse una vez completa la selección, pero su contenido/lógica interna no cambió.
  - **e2e actualizado, no dejado roto**: igual que TASK-050, se prefirió actualizar el helper compartido (`goToNewAppointmentWithSelection`) para reflejar el nuevo trigger en vez de dejar referencias rotas a un calendario que ya no monta solo — no se agregó ningún test nuevo (sin convención de test para este tipo de cambio de UI/layout, mismo criterio que TASK-034/037/043/049), y no se corrió la suite e2e completa (fuera del alcance de verificación pedido para este ticket, mismo criterio de fallback que TASK-037/043).
  - **Verificación**: `npx tsc --noEmit` limpio. `pnpm build` compiló las 21 rutas del proyecto sin errores nuevos (mismos warnings preexistentes de "Dynamic server usage" en rutas que usan `headers()`); `/admin/turnos/nuevo` (77.4 kB) compiló sin cambios de tamaño relevantes. Lectura estructural completa del diff de `NewAppointmentView.tsx` para confirmar el flujo acordeón → resumen → popup → vuelta a acordeón end-to-end, mismo fallback de verificación que TASK-037/043 cuando una corrida e2e en vivo no era práctica de incluir.

#### `[TASK-052]` Confirmación explícita para guardar un turno (en vez de reservar al click)
* **Descripción**: hoy clickear un horario libre en el calendario reserva el turno inmediatamente (o dispara la advertencia de fuera-de-disponibilidad y reserva al confirmar esa advertencia). Se pide un paso intermedio: clickear un horario lo **selecciona** (queda resaltado), y un botón explícito ("Guardar turno" o similar) es el que efectivamente confirma la reserva.
* **Criterios de Aceptación**:
  - [x] Clickear un horario libre lo marca como seleccionado sin reservarlo todavía.
  - [x] Un botón explícito confirma la reserva del horario seleccionado (reutilizando la lógica ya existente de bloqueo duro por superposición y advertencia por fuera de disponibilidad, TASK-041/042 — sin cambiar esas reglas, solo el momento en que se disparan).
* **Prioridad**: Media | **Esfuerzo**: Medio | **Dependencias**: TASK-049, TASK-051 (conviene resolverlo ya con el layout de popup, no embebido)
* **Resultado**: `DoctorWeekCalendar.tsx` gana un estado `selectedSlot: Date | null`. `onSelectSlot` (`handleSelectSlot`) ya no reserva — solo selecciona: clickear un horario libre lo marca (si no había nada seleccionado, o si había uno distinto, la selección se mueve); clickear el mismo horario ya seleccionado la limpia (toggle). El horario seleccionado se resalta reutilizando el mecanismo que TASK-042/043 ya usaba para pintar disponibilidad — `slotPropGetter` agrega la clase `rbc-slot-selected` (nueva, `app/globals.css`) encima de `rbc-slot-available`/`rbc-slot-unavailable` cuando la celda coincide con `selectedSlot` — en vez de un evento "fantasma" separado, evitando manejar un segundo tipo de evento sintético en `events`/`CalendarEvent`.
  Mientras hay un horario seleccionado aparece una barra nueva arriba del calendario ("Horario seleccionado: `<fecha/hora formateada con date-fns/es>`") con dos `Button` (TASK-049, con `isLoading`/`loadingText` cableados en el de guardar): "Cancelar selección" (limpia `selectedSlot` sin más, la vía explícita y descubrible pedida por el criterio de aceptación, además del toggle por click) y "Guardar turno" (dispara `confirmBooking`). `confirmBooking` es exactamente la lógica que antes vivía en `handleSelectSlot` al clickear, sin cambios: `isWithinAvailability` (TASK-042, reutilizada tal cual) decide si reserva directo (`book(selectedSlot)`) o abre `OutOfAvailabilityAlertDialog` primero (`setPendingSlot`), cuyo `onConfirm` (`confirmOutOfAvailability`, sin tocar) llama a ese mismo `book` recién se confirma la advertencia — solo cambió el momento en que se dispara (botón en vez de click), tal como pedía el ticket, ni `existsOverlapping` ni `isWithinAvailability` ni `createAppointment` se tocaron. `book` limpia `selectedSlot` al terminar (éxito o error) para que la barra desaparezca en vez de quedar apuntando a un horario ya reservado o rechazado.
  - **Archivos creados**: Ninguno.
  - **Archivos modificados**:
    - `components/DoctorWeekCalendar.tsx` — estado `selectedSlot`, `handleSelectSlot` reescrito para seleccionar/mover/togglear en vez de reservar, nueva función `confirmBooking` con la lógica movida, barra de selección con los botones "Cancelar selección"/"Guardar turno", `slotPropGetter` extendido para resaltar la celda seleccionada.
    - `app/globals.css` — clase `.rbc-dark-theme .rbc-slot-selected` (fondo azul + anillo `box-shadow` inset), aplicada junto a `rbc-slot-available`/`rbc-slot-unavailable` existentes.
    - `e2e/patient-flow.ts` — `clickCalendarSlot` ahora clickea también el nuevo botón "Guardar turno" después de clickear la celda, ya que un click de celda solo selecciona desde este ticket. Sin este ajuste, `bookAppointmentAsAdmin` (`02-flujo.spec.ts`) y ambas ramas de la carrera ADM-09 (`03-adm-extra.spec.ts`) habrían quedado rotos — no reservaban nada, solo seleccionaban — mismo criterio de "e2e actualizado, no dejado roto" ya aplicado en TASK-050/051. Ningún test nuevo se agregó (sin convención de test dedicado para este tipo de cambio de interacción, mismo criterio que TASK-034/037/043/049/051); esto es mantener helpers e2e existentes funcionalmente correctos, no una excepción a ese criterio.
* **Observaciones**:
  - **UX de deselección elegida**: clickear la celda ya seleccionada la deselecciona (toggle discreto para quien lo descubra clickeando) *y* el botón "Cancelar selección" en la barra (afordancia explícita y visible, para quien no pruebe el toggle) — el ticket pedía "tu criterio, pero que sea descubrible, no un toggle implícito que nadie encontraría", así que se ofrecen ambos caminos en vez de uno solo.
  - **Sin evento fantasma**: se evaluó agregar un evento temporal a `events`/`CalendarEvent` para representar la selección visualmente (alternativa que el ticket sugería como posible) y se descartó a favor de extender `slotPropGetter` — ya existía y ya resolvía exactamente este tipo de resaltado por celda (disponibilidad, TASK-042/043); un evento fantasma habría exigido inventar un segundo tipo de "evento sintético" distinguible de los turnos reales en `onSelectEvent`/render, sin ninguna ventaja visual adicional para una sola celda de 30 minutos.
  - **TASK-041/042 sin cambios**: no se tocó `existsOverlapping`, `isWithinAvailability` ni `createAppointment` — `confirmBooking` es un movimiento literal del cuerpo que antes tenía `handleSelectSlot`, no una reimplementación.
  - **TASK-051 (layout del popup) sin tocar**: no se modificó `NewAppointmentView.tsx` — la barra de selección y sus botones viven enteramente dentro de `DoctorWeekCalendar.tsx`, arriba del calendario y dentro del mismo contenedor `overflow-y-auto` que ya usaba el Dialog, sin tocar el `DialogHeader` fijo ni la barra de resumen/"Ver calendario" que TASK-051 ya había resuelto.
  - **TASK-048 (navegación del calendario) verificado, no roto**: `view={Views.WEEK}` + `onView={() => {}}` y `date`/`onNavigate` controlados (líneas ya existentes de TASK-048) no se tocaron — se revisó con lectura estructural que el nuevo estado `selectedSlot` y la barra condicional no introducen ningún remount ni cambian esas props. Hoy/Atrás/Siguiente y el cambio de semana siguen dependiendo únicamente de `date`/`onNavigate`/`onRangeChange`, ninguno de los tres tocado.
  - **`.env.local`/`MONGODB_URI` no verificado en este entorno para este ticket**: a diferencia de tickets anteriores donde el acceso a Mongo estuvo disponible, no se pudo confirmar el acceso al archivo `.env.local` en esta sesión (comando denegado por la sandbox), así que no se corrió la suite e2e en vivo — mismo fallback de verificación que TASK-037/043/051 (lectura estructural completa del diff en vez de una corrida real) cuando eso no es práctico.
  - **Verificación**: `npx tsc --noEmit` limpio. `pnpm build` compiló las 21 rutas del proyecto sin errores nuevos (mismos warnings preexistentes de "Dynamic server usage" en rutas que usan `headers()`). Lectura estructural completa del diff de `DoctorWeekCalendar.tsx` para confirmar el flujo click → selección → confirmar → reservar (directo o vía advertencia) end-to-end, y del diff de `e2e/patient-flow.ts` para confirmar que `clickCalendarSlot` sigue resultando en una reserva real para sus tres llamadores.
